/** @type {import('tailwindcss').Config} */
// Tailwind v4 configuration - content paths are handled by @source in index.css
export default {
  // Note: Tailwind v4 uses @source directives in CSS for content scanning
  // Most theme configuration should be in @theme blocks in CSS
  plugins: [],
}
