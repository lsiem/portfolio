---
milestone: v1.0
audited: 2026-07-25T00:00:00.000Z
status: tech_debt
scores:
  requirements: 24/24
  phases: 4/6            # 05 + 06 have no VERIFICATION.md (workflow-driven, no REQ-IDs assigned)
  integration: 15/15     # checked exports wired, 0 orphaned, 0 missing
  flows: 6/6             # all E2E flows complete
gaps: {}                 # no critical blockers
tech_debt:
  - phase: 04-signature-moment-launch-hardening
    items:
      - "04-VERIFICATION.md is STALE (2026-07-17, status gaps_found). Both BLOCKERs it names are resolved at HEAD: dom-visibility-wrapper.tsx deleted (zero navigator.webdriver refs in src/), ungated GlobalCanvas replaced by the gated stage layer. Not re-verified after the revert + PR #21 + phases 05/06."
      - "03-VERIFICATION.md still reads status: human_needed; the blocking four-dimension walkthrough was performed 2026-07-08 (human UAT 4/4, recorded in REQUIREMENTS.md) but the report was never updated."
  - phase: 05-threejs-kontinuum
    items:
      - "No VERIFICATION.md / SUMMARY.md — phase ran workflow-driven. Only DECISIONS.md + DESIGN-SPEC.md exist. No REQ-IDs assigned, so requirements coverage is unaffected; this is an artifact-completeness gap."
  - phase: 06-solid-3d-kern
    items:
      - "No VERIFICATION.md / SUMMARY.md — same workflow-driven pattern. Evidence lives in CARRYOVER-CLOSURE.md + PR #26."
      - "Real-device mid-tier Android profile still open (needs physical hardware; emulated 4x CPU + Slow 4G is a proxy). Non-blocking; FALLBACK path eval-covered under SwiftShader."
      - "Stale architecture comments referencing deleted v1 particle artifacts: src/components/scene/stage/seeded.ts:8-10, stage-gate.tsx:41, stage-canvas.tsx:30."
  - phase: ci
    items:
      - "CI (.github/workflows/ci.yml) runs only check:content, build, lhci autorun — the 159 Playwright evals that encode every P2-P6 cross-phase contract (MODE-02, WOW-01/04, TECH-03/05, D-10 gate semantics) never run on push/PR. Largest regression exposure in the milestone."
      - "DSGVO tripwire scripts/check-stage-chunk.ts is documented as CI-run but no workflow invokes it (TECH-06, WOW-01)."
      - "src/components/scene/stage/progress.test.ts + kern-invariants.test.ts (17 producer-parity assertions) have no test runner and no CI hook — a desktop/touch producer divergence would ship silently."
      - "lighthouserc.json audits /de and /en without ?webgl=off; the 184,643-byte eager budget stays honest only because CI's SwiftShader resolves to tier none — behavior pinned by an eval that itself isn't in CI."
      - "Build emits a metadataBase-not-set warning from routes without generateMetadata (e.g. /_not-found). No shipped page has a relative OG URL. Cosmetic."
nyquist: skipped   # no validate-phase step hook active at verify:post
---

# Milestone v1.0 — Audit Report

**Project:** Portfolio Neubau — lsiem.de
**Milestone:** v1.0 (6 phases, 21 plans)
**Audited:** 2026-07-25 at HEAD `2b1939c`
**Status:** `tech_debt` — all requirements satisfied, no critical blockers, accumulated debt needs review

## Headline

The central risk going in — that phases 05 and 06 rewrote the visual and 3D layer *after* phases 03 and 04 were verified, and therefore broke earlier contracts — **did not materialize**. The integration checker ran a full `pnpm build` (exit 0) plus 159 Playwright evals against the production build: 159 passed, 15/15 checked cross-phase exports wired, 0 orphaned, 0 missing.

All remaining findings are documentation drift and CI enforcement gaps.

## Requirements Coverage — 24/24 satisfied

Three-source cross-reference (REQUIREMENTS.md traceability × phase VERIFICATION.md × SUMMARY frontmatter) plus a live integration map at HEAD.

| Group | REQ-IDs | Status |
|---|---|---|
| Content & Substanz | CONT-01 … CONT-08 | 8/8 satisfied |
| Immersives Erlebnis | WOW-01 … WOW-04 | 4/4 satisfied |
| Dual-Mode | MODE-01, MODE-02 | 2/2 satisfied |
| Internationalisierung | I18N-01, I18N-02 | 2/2 satisfied |
| Technische Basis | TECH-01 … TECH-08 | 8/8 satisfied |

**Orphans:** none. Every REQ-ID in the traceability table has at least one verified inter-phase touchpoint at HEAD.

**TECH-06 caveat:** wiring is present (Vercel-gated cookieless `<Analytics/>`), but the CDN-string tripwire that guards it is not invoked by CI — counted satisfied, tracked as debt.

## Phase Verification Status

| Phase | VERIFICATION.md | Reported Status | Assessment |
|---|---|---|---|
| 01 bilingual-content-foundation | ✓ | passed (4/4, human-verified) | Current |
| 02 recruiter-overview-live | ✓ | passed (13/13) | Current |
| 03 design-direction-immersive | ✓ | human_needed (5/5 implemented) | **Stale** — the blocking walkthrough was completed 2026-07-08 (human UAT 4/4); report never updated |
| 04 signature-moment-launch | ✓ | gaps_found (2 BLOCKERs) | **Stale** — both blockers resolved at HEAD; see below |
| 05 threejs-kontinuum | ✗ | — | Workflow-driven; no REQ-IDs assigned |
| 06 solid-3d-kern | ✗ | — | Workflow-driven; evidence in CARRYOVER-CLOSURE.md + PR #26 |

### Why Phase 04's `gaps_found` is not a blocker

The 2026-07-17 report flagged two BLOCKERs from unplanned commits `56daa9d` / `d9b8e57`:

1. `dom-visibility-wrapper.tsx` — `navigator.webdriver` test-evasion hiding the DOM from real users.
   **Resolved:** file deleted; `grep -rn "navigator.webdriver" src/` → zero hits.
2. Ungated full-screen `GlobalCanvas` on every route.
   **Resolved:** that layer is gone. The current stage gate (`src/components/scene/stage-gate.tsx:129`) returns `null` unconditionally on `reducedMotion || tier === "none"` — it beats even `?webgl=force`.

The rewrite was reverted, the 04-06 FALLBACK fix shipped via PR #21, and phases 05/06 replaced the stage entirely. The report predates all of it.

## Cross-Phase Integration — no blockers

| Flow | Status | Evidence |
|---|---|---|
| Content model → page → overview → CV PDF | Complete | `prebuild` runs `check:content` + `generate:cv`; PDFs regenerated per build; link at `page.tsx:425`, present in prerendered `de.html` |
| i18n routing + hreflang + per-locale OG under new transitions | Complete | `de.html` and `de/case-studies/elia.html` emit de/en/x-default alternates; absolute `og:image`; 8 seo + i18n evals green |
| Reduced-motion quiet variant under new stage-gate | Complete | `stage-gate.tsx:129`; 6 MODE-02 evals green |
| WOW-01 lazy 3D discipline | Complete | three enters only via `dynamic(() => import("./stage-canvas"), {ssr:false})`; zero `<canvas>`/`three` in prerendered HTML; context-loss eval green |
| TECH-03 real DOM text under canvas | Complete | `stage-slot.tsx` renders `aria-hidden` / `pointer-events-none` / `-z-10`; 9 a11y evals green incl. first-Tab-lands-on-real-control |
| Route transition ↔ stage object constancy | Complete | `transition-link.tsx` → `sceneBridge.transition`, watchdog-committed at 700ms, dead-letter when no canvas |

**Note on MODE-01:** the dense overview is the homepage itself (first-fold `<nav>` anchors + CV button), not a separate route. This matches the Phase-2 SUMMARY — an interpretation, not a gap.

## Tech Debt — 13 items across 4 areas

### CI enforcement (highest value, one commit)

The single largest regression exposure: the 159 evals encoding every cross-phase contract never run on push or PR, and two phases rewrote the visual layer after the phases those evals protect were verified.

- Append `pnpm test` and `pnpm check:stage-chunk` to `.github/workflows/ci.yml` after the Build step
- Add a `test:unit` script (`node --test` via tsx) for `progress.test.ts` + `kern-invariants.test.ts`
- Add `?webgl=off` to the LHCI URLs so the eager-budget gate is self-contained

### Documentation drift

- Re-verify or supersede `04-VERIFICATION.md`; update `03-VERIFICATION.md` status to reflect the completed 2026-07-08 walkthrough
- Backfill VERIFICATION artifacts for phases 05/06, or record explicitly that workflow-driven phases close via PR + CARRYOVER-CLOSURE evidence
- Refresh stale comments in `seeded.ts:8-10`, `stage-gate.tsx:41`, `stage-canvas.tsx:30`

### Open, hardware-blocked

- Real-device mid-tier Android profile — needs physical hardware or a device lab. Non-blocking.

### Cosmetic

- `metadataBase` build warning from routes without `generateMetadata`

---

*Audited: 2026-07-25 — Claude (gsd-audit-milestone), integration check by gsd-integration-checker*
