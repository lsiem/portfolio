---
phase: 02-recruiter-overview-live
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified:
  - next.config.ts
  - lighthouserc.json
  - .github/workflows/ci.yml
  - .planning/STATE.md
  - evals/a11y.spec.ts
autonomous: true
requirements: [TECH-01, TECH-03]
tags: [security-headers, core-web-vitals, lighthouse, accessibility]

must_haves:
  truths:
    - The Lighthouse budget asserts LCP <= 2500ms (matching ROADMAP criterion 4 / TECH-01 "good"), plus an INP or total-blocking-time proxy assertion, and passes against a production build (TECH-01; resolves REVIEW finding 1)
    - The CI budget comment in .github/workflows/ci.yml matches the actual lighthouserc.json thresholds (no drift; resolves REVIEW finding 1)
    - GITHUB_TOKEN presence in the Vercel Production environment is confirmed as a blocking pre-cutover check before promotion (heatmap ships real data, not the fallback; resolves REVIEW finding 6)
    - Every page ships security response headers (HSTS, nosniff, Referrer-Policy, frame-ancestors/X-Frame-Options, Permissions-Policy) consistent with the zero-runtime-third-party posture
    - The full Playwright eval suite (home, theme, seo, activity) is green
    - The site is keyboard-operable end-to-end with visible focus and correct landmarks/lang (TECH-03)
  artifacts:
    - next.config.ts (headers() security block)
    - lighthouserc.json (LCP <= 2500 + INP/TBT assertion)
    - .github/workflows/ci.yml (budget comment corrected)
    - .planning/STATE.md (CSP-gap blocker entry if the CSP fallback path is taken)
    - evals/a11y.spec.ts
  key_links:
    - lighthouserc.json budget runs via lhci autorun against pnpm start (production build); LCP max 2500 + INP/TBT proxy
    - security headers do not break the inline no-flash theme script or the shipped Vercel analytics beacon
    - production GITHUB_TOKEN gate blocks cutover if the token is absent from the Vercel Production env
---

<objective>
Harden and verify the complete site before cutover: security headers, Core Web Vitals budget, and accessibility (TECH-01, TECH-03).

**Slice contribution:** the launch-readiness gate. After this plan the whole overview + dark mode + SEO + heatmap + CV is proven CWV-"good" on mobile, keyboard-accessible, and security-headed — all on a Vercel preview build, before the production promotion (Plan 07, per CTX-06). This applies RESEARCH §5 (keep generators off the client bundle, static first viewport, reduced-motion guard) and the ECC web performance/security rules.

Purpose: prove TECH-01 and reinforce TECH-03 with automated gates, and add the security-headers pass the threat model calls for.
Output: a `headers()` security block in next.config.ts, a passing LHCI budget run, an accessibility eval, and a green full eval suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-recruiter-overview-live/02-CONTEXT.md
@.planning/phases/02-recruiter-overview-live/02-RESEARCH.md
@next.config.ts
@lighthouserc.json
@src/app/[locale]/layout.tsx
@evals/home.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security response headers via next.config headers()</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current config — withContentCollections(withNextIntl(nextConfig)); nextConfig is empty)
    - ~/.claude/rules/ecc/web/security.md (HTTPS + headers block: HSTS preload, X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy; CSP guidance)
    - src/app/[locale]/layout.tsx (the inline no-flash script + @vercel/analytics + @vercel/speed-insights — any CSP must not break these)
    - AGENTS.md (DSGVO: zero runtime third-party except the sanctioned cookieless Vercel analytics)
    - .planning/STATE.md (`## Blockers/Concerns` — where the CSP-gap follow-up is recorded if the CSP fallback path is taken; STATE.md is in this plan's files_modified so the acceptance is satisfiable — REVIEW finding 7)
  </read_first>
  <action>
    Add an async `headers()` function to `nextConfig` in next.config.ts (keeping withContentCollections + withNextIntl as the outermost wrappers) that applies to all routes a security header set: `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, and a `Permissions-Policy` disabling camera/microphone/geolocation. For Content-Security-Policy: because the site is fully static (SSG) a per-request nonce is not available, and the no-flash theme script is a single static inline `<script>` — take the pragmatic, non-breaking approach: EITHER add a CSP whose `script-src` includes `'self'` plus the sha256 hash of the exact inline theme-script string (compute and pin it) and whose `connect-src`/`script-src` allowlists the sanctioned Vercel analytics/speed-insights origins so the shipped beacon keeps working, `style-src 'self' 'unsafe-inline'` (Tailwind), `img-src 'self' data:`, `frame-ancestors 'none'`, `object-src 'none'`, `base-uri 'self'`; OR, if the hash/allowlist cannot be validated within this task without breaking analytics or the theme script, ship the non-CSP headers above and leave a clearly-commented CSP TODO with the exact blockers (inline-script hash maintenance + Vercel analytics origins) rather than shipping a CSP that breaks the site. Do not weaken to `'unsafe-inline'` on `script-src`.
  </action>
  <verify>
    <automated>pnpm build && node -e "const s=require('fs').readFileSync('next.config.ts','utf8');for(const h of ['Strict-Transport-Security','X-Content-Type-Options','Referrer-Policy','Permissions-Policy']){if(!s.includes(h)){console.error('missing '+h);process.exit(1)}}console.log('security headers present')"</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts exports a `headers()` block applying HSTS, nosniff, X-Frame-Options DENY, Referrer-Policy, and Permissions-Policy to all routes
    - Prefer a working CSP (static-script hash + Vercel analytics allowlist, no `unsafe-inline` on script-src). Fall back to a commented CSP TODO documenting the exact blockers ONLY if the hash/allowlist demonstrably breaks analytics or the theme script — never ship a CSP that breaks the site. If the fallback is taken, record the CSP gap in STATE.md `## Blockers/Concerns` as a tracked follow-up (STATE.md is in this plan's files_modified — REVIEW finding 7; do not close it silently in a code comment alone)
    - withContentCollections/withNextIntl wrapping is preserved; `pnpm build` succeeds
    - The inline no-flash theme script and Vercel analytics still function after the headers change
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tighten CWV budget to the goal (LCP 2500 + INP/TBT), sync CI comment, full eval suite</name>
  <files>lighthouserc.json, .github/workflows/ci.yml</files>
  <read_first>
    - lighthouserc.json (existing budget: `largest-contentful-paint` maxNumericValue 3000, script `resource-summary:script:size` 184643, `categories:performance` minScore 0.9; startServerCommand pnpm start; urls /de and /en)
    - .github/workflows/ci.yml (L41 comment "TECH-07 performance budget: LCP <= 2500ms, script bytes <= 153600, perf >= 0.9" — currently drifted from lighthouserc.json actuals)
    - .planning/ROADMAP.md (Phase 2 Success Criterion 4 — CWV "good" on mobile; the verbatim wording to align acceptance to) + .planning/REQUIREMENTS.md (TECH-01: LCP/INP/CLS "good", INP < 200ms)
    - .planning/phases/02-recruiter-overview-live/02-RESEARCH.md (§5 CWV — first viewport static SSR, generators off client bundle, only new client JS is the theme toggle + tiny inline script; keep the ~150KB budget)
    - ~/.claude/rules/ecc/web/performance.md (CWV targets LCP < 2.5s, INP < 200ms, CLS < 0.1; TBT < 200ms; landing JS budget < 150kb)
  </read_first>
  <action>
    Align the Lighthouse budget with the phase's own success criterion (REVIEW finding 1 — the current 3000ms allowance lets CI pass while failing ROADMAP criterion 4 / TECH-01). In lighthouserc.json: (1) set `largest-contentful-paint` `maxNumericValue` to `2500` (was 3000) so the gate enforces LCP < 2.5s "good"; (2) ADD an INP proxy assertion — Lighthouse lab cannot measure field INP, so add `total-blocking-time` with `maxNumericValue` `200` (the standard lab proxy for INP < 200ms per the ECC performance rules) as an `error`-level assertion; optionally add `cumulative-layout-shift` `maxNumericValue` `0.1` for CLS "good"; (3) keep the script-size and `categories:performance` (>= 0.9) assertions. These are hard targets, not "tighten if comfortable" — LCP 2500 and TBT 200 must pass. Run the budget against a production build (`pnpm build` then `pnpm exec lhci autorun`, which starts `pnpm start` per the config) for both `/de` and `/en`. If LCP/TBT/JS-size fails, fix the regression (e.g. an accidental client import of a build/server-only generator — react-pdf, the next/og route, github.ts must stay off the client bundle); do NOT relax the budget to hide a regression.

    Then sync the CI comment (REVIEW finding 1): update the `.github/workflows/ci.yml` comment above the "Lighthouse CI budget" step so it states the ACTUAL final lighthouserc.json thresholds verbatim (LCP <= 2500ms, TBT <= 200ms, the real script-bytes value, perf >= 0.9). The comment and the config must not drift.

    Finally run the full Playwright eval suite (`pnpm test`) and confirm home, theme, seo, and activity specs are all green.
  </action>
  <verify>
    <automated>node -e "const c=require('./lighthouserc.json');const a=(c.ci&&c.ci.assert&&c.ci.assert.assertions)||{};const lcp=a['largest-contentful-paint'];const v=Array.isArray(lcp)?lcp[1]:lcp;if(!v||v.maxNumericValue!==2500){console.error('LCP not set to 2500');process.exit(1)}if(!a['total-blocking-time']){console.error('missing INP/TBT proxy assertion');process.exit(1)}console.log('LCP 2500 + TBT assertion present')" && pnpm build && pnpm exec lhci autorun 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - lighthouserc.json asserts `largest-contentful-paint` maxNumericValue 2500 (LCP < 2.5s "good", matching ROADMAP criterion 4 / TECH-01) — REVIEW finding 1
    - lighthouserc.json adds a `total-blocking-time` maxNumericValue 200 assertion as the lab proxy for INP < 200ms (error-level); the previous config had no INP gate
    - `.github/workflows/ci.yml` budget comment states the actual final thresholds verbatim (LCP 2500, TBT 200, real script bytes, perf 0.9) — no drift
    - `pnpm exec lhci autorun` passes LCP (2500), TBT (200), performance-score (0.9), and script-size for /de and /en against a production build; no build/server-only generator (react-pdf, next/og OG route, github.ts) leaked into the client bundle
    - `pnpm test` (full Playwright suite: home, theme, seo, activity) is green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Accessibility eval — landmarks, focus, keyboard, lang</name>
  <files>evals/a11y.spec.ts</files>
  <read_first>
    - evals/home.spec.ts (per-locale Playwright structure to mirror)
    - src/app/[locale]/layout.tsx (semantic landmarks: header/nav, main, footer; html lang={locale})
    - .planning/phases/02-recruiter-overview-live/02-UI-SPEC.md (Interaction States — global :focus-visible ring; TECH-03 semantic HTML, keyboard, contrast, real DOM text)
  </read_first>
  <action>
    Create evals/a11y.spec.ts (mirror the per-locale shape of home.spec.ts) asserting the accessibility baseline for both locales. Prefer stable role/landmark/rel assertions over a brittle full Tab-order sequence (REVIEW — strict Tab-order tests flake across browsers/extensions): (1) `<html lang>` equals the locale; (2) exactly one `<main>` landmark and a `<header>`/`<footer>` are present; (3) each `<section>` in the overview has an accessible name (aria-labelledby resolves to a heading) — spot-check `#career`, `#projects`, `#skills`, `#about`, `#contact`, `#activity`; (4) keyboard reachability — assert each key control (header Contact affordance, theme toggle radiogroup, locale switcher, CV download button) EXISTS and is individually focusable (call `locator.focus()` then assert `locator(':focus')` matches it and a visible `:focus-visible` style applies) rather than asserting one rigid Tab-through DOM sequence; do a single light Tab smoke-check that the first Tab lands on a real interactive control, but do NOT assert the entire tab order. (5) all `target="_blank"` anchors carry `rel="noopener noreferrer"`. Keep assertions deterministic (no timeouts). Contrast is verified manually (documented in Verification) since automated contrast needs tooling not in the stack.
  </action>
  <verify>
    <automated>pnpm exec playwright test evals/a11y.spec.ts --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - evals/a11y.spec.ts passes for both locales: html lang correct, single main + header/footer, named sections, keyboard reaches all controls with visible focus, external links carry rel="noopener noreferrer"
    - The eval is added to the suite and runs under the existing playwright config
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN/edge → browser | HTTP response headers govern transport security and framing |
| static assets → browser | The whole shipped surface being verified for zero-runtime-third-party |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Information Disclosure / Tampering | missing security response headers | medium | mitigate | Add HSTS, X-Content-Type-Options, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy via next.config headers(); CSP added with a static-script hash + Vercel analytics allowlist where feasible, else documented TODO (no site-breaking CSP) |
| T-02-17 | Elevation of Privilege (clickjacking) | site framed by a third party | medium | mitigate | `X-Frame-Options: DENY` + `frame-ancestors 'none'` in CSP |
| T-02-18 | Information Disclosure | runtime third-party network call sneaking into the bundle | high | mitigate | LHCI JS-budget assertion + manual network check confirm only same-origin + sanctioned cookieless Vercel analytics requests; generators stay build/server-only |
</threat_model>

<artifacts>
## Artifacts this plan produces

- `next.config.ts` — `headers()` security block (+ CSP or documented TODO)
- `lighthouserc.json` — LCP <= 2500 + `total-blocking-time` <= 200 (INP proxy) + preserved script-size/perf assertions
- `.github/workflows/ci.yml` — budget comment synced to the actual thresholds
- `.planning/STATE.md` — CSP-gap `## Blockers/Concerns` entry (only if the CSP fallback path is taken)
- `evals/a11y.spec.ts` — landmarks/focus/keyboard/lang/external-link accessibility assertions
- (verification) passing LHCI budget run + green full Playwright suite + confirmed production GITHUB_TOKEN gate
</artifacts>

<verification>
- `pnpm build` + `pnpm exec lhci autorun` pass the CWV budget (LCP <= 2500, TBT <= 200, perf >= 0.9, script size) for /de and /en on a production build
- The `.github/workflows/ci.yml` budget comment matches the lighthouserc.json thresholds (no drift — REVIEW finding 1)
- `pnpm test` green (home, theme, seo, activity, a11y)
- Production GITHUB_TOKEN gate (REVIEW finding 6, blocking pre-cutover): confirm `GITHUB_TOKEN` (read-only `read:user`, account lsiem) is present in the Vercel PRODUCTION environment BEFORE promotion — without it the production build bakes the fallback activity line. This is a blocking gate handed to the 02-07 human cutover checkpoint; do not treat the phase as launch-ready until it is confirmed set.
- Manual: run a mobile Lighthouse against the Vercel preview URL; confirm LCP < 2.5s, INP < 200ms, CLS < 0.1, no render-blocking regressions
- Manual: verify muted-on-background and accent-link contrast pass AA in both light and dark themes
- Manual: confirm response headers are present on the preview URL (browser devtools / curl)
- Note (REVIEW, out of scope): CodeRabbit flagged `scripts/check-content-parity.ts` (only the first blocklist.txt scanned; parity silently "passes" if both locale roots are missing). That is Phase-1 code and is the confidentiality/i18n gate this phase relies on, but hardening it is out of Phase-2 scope — tracked as a Phase-1 follow-up, not fixed here (it does not block Phase-2 cutover since the locale roots exist and CI runs the gate).
</verification>

<success_criteria>
- ROADMAP Phase-2 Success Criterion 4 verbatim — "Site erreicht Core Web Vitals \"good\" auf Mobilgeräten, ist per Tastatur vollständig bedienbar (semantisches HTML, Kontraste) und bietet Dark Mode (Systemerkennung + Toggle)" — the CWV-good + keyboard-accessible parts are verified here with automated gates (LCP < 2.5s, TBT < 200ms proxy for INP, a11y eval)
- TECH-01 proven on a production build; TECH-03 reinforced; security-headers threat mitigated
- Site is launch-ready pending the human cutover (Plan 07), including the confirmed production GITHUB_TOKEN gate
</success_criteria>

<output>
Create `.planning/phases/02-recruiter-overview-live/02-06-SUMMARY.md` when done
</output>
