---
phase: 02-recruiter-overview-live
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified:
  - next.config.ts
  - evals/a11y.spec.ts
autonomous: true
requirements: [TECH-01, TECH-03]
tags: [security-headers, core-web-vitals, lighthouse, accessibility]

must_haves:
  truths:
    - The site meets Core Web Vitals "good" on mobile — the CI Lighthouse budget passes against a production build (TECH-01)
    - Every page ships security response headers (HSTS, nosniff, Referrer-Policy, frame-ancestors/X-Frame-Options, Permissions-Policy) consistent with the zero-runtime-third-party posture
    - The full Playwright eval suite (home, theme, seo, activity) is green
    - The site is keyboard-operable end-to-end with visible focus and correct landmarks/lang (TECH-03)
  artifacts:
    - next.config.ts (headers() security block)
    - evals/a11y.spec.ts
  key_links:
    - lighthouserc.json budget runs via lhci autorun against pnpm start (production build)
    - security headers do not break the inline no-flash theme script or the shipped Vercel analytics beacon
---

<objective>
Harden and verify the complete site before cutover: security headers, Core Web Vitals budget, and accessibility (TECH-01, TECH-03).

**Slice contribution:** the launch-readiness gate. After this plan the whole overview + dark mode + SEO + heatmap + CV is proven CWV-"good" on mobile, keyboard-accessible, and security-headed — all on a Vercel preview build, before the production promotion (Plan 07, per CTX-06). This applies RESEARCH §5 (keep generators off the client bundle, static first viewport, reduced-motion guard) and the ECC web performance/security rules.

Purpose: prove TECH-01 and reinforce TECH-03 with automated gates, and add the security-headers pass the threat model calls for.
Output: a `headers()` security block in next.config.ts, a passing LHCI budget run, an accessibility eval, and a green full eval suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-recruiter-overview-live/02-CONTEXT.md
@.planning/phases/02-recruiter-overview-live/02-RESEARCH.md
@next.config.ts
@lighthouserc.json
@src/app/[locale]/layout.tsx
@evals/home.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security response headers via next.config headers()</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current config — withContentCollections(withNextIntl(nextConfig)); nextConfig is empty)
    - ~/.claude/rules/ecc/web/security.md (HTTPS + headers block: HSTS preload, X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy; CSP guidance)
    - src/app/[locale]/layout.tsx (the inline no-flash script + @vercel/analytics + @vercel/speed-insights — any CSP must not break these)
    - AGENTS.md (DSGVO: zero runtime third-party except the sanctioned cookieless Vercel analytics)
  </read_first>
  <action>
    Add an async `headers()` function to `nextConfig` in next.config.ts (keeping withContentCollections + withNextIntl as the outermost wrappers) that applies to all routes a security header set: `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, and a `Permissions-Policy` disabling camera/microphone/geolocation. For Content-Security-Policy: because the site is fully static (SSG) a per-request nonce is not available, and the no-flash theme script is a single static inline `<script>` — take the pragmatic, non-breaking approach: EITHER add a CSP whose `script-src` includes `'self'` plus the sha256 hash of the exact inline theme-script string (compute and pin it) and whose `connect-src`/`script-src` allowlists the sanctioned Vercel analytics/speed-insights origins so the shipped beacon keeps working, `style-src 'self' 'unsafe-inline'` (Tailwind), `img-src 'self' data:`, `frame-ancestors 'none'`, `object-src 'none'`, `base-uri 'self'`; OR, if the hash/allowlist cannot be validated within this task without breaking analytics or the theme script, ship the non-CSP headers above and leave a clearly-commented CSP TODO with the exact blockers (inline-script hash maintenance + Vercel analytics origins) rather than shipping a CSP that breaks the site. Do not weaken to `'unsafe-inline'` on `script-src`.
  </action>
  <verify>
    <automated>pnpm build && node -e "const s=require('fs').readFileSync('next.config.ts','utf8');for(const h of ['Strict-Transport-Security','X-Content-Type-Options','Referrer-Policy','Permissions-Policy']){if(!s.includes(h)){console.error('missing '+h);process.exit(1)}}console.log('security headers present')"</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts exports a `headers()` block applying HSTS, nosniff, X-Frame-Options DENY, Referrer-Policy, and Permissions-Policy to all routes
    - Prefer a working CSP (static-script hash + Vercel analytics allowlist, no `unsafe-inline` on script-src). Fall back to a commented CSP TODO documenting the exact blockers ONLY if the hash/allowlist demonstrably breaks analytics or the theme script — never ship a CSP that breaks the site. If the fallback is taken, record the CSP gap in STATE.md `## Blockers/Concerns` as a tracked follow-up (do not close it silently in a code comment alone)
    - withContentCollections/withNextIntl wrapping is preserved; `pnpm build` succeeds
    - The inline no-flash theme script and Vercel analytics still function after the headers change
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Core Web Vitals budget + full eval suite</name>
  <files>lighthouserc.json</files>
  <read_first>
    - lighthouserc.json (existing budget: LCP maxNumericValue 3000, script size 184643, performance minScore 0.9; startServerCommand pnpm start; urls /de and /en)
    - .planning/phases/02-recruiter-overview-live/02-RESEARCH.md (§5 CWV — first viewport static SSR, generators off client bundle, only new client JS is the theme toggle + tiny inline script; keep the ~150KB budget)
    - ~/.claude/rules/ecc/web/performance.md (CWV targets LCP < 2.5s, INP < 200ms; landing JS budget < 150kb)
  </read_first>
  <action>
    Verify (and only tighten if the measured build comfortably allows) the Lighthouse budget in lighthouserc.json reflects the phase's CWV-"good" goal on mobile. Confirm the script-size assertion still holds after adding the theme toggle client component (the only new client JS besides the ~150-byte inline script) — the CV/OG/GitHub generators are build/server-only and must not have entered the client bundle. Run the budget against a production build (`pnpm build` then `pnpm exec lhci autorun`, which starts `pnpm start` per the config) and confirm LCP, performance score, and JS-size assertions pass for both `/de` and `/en`. Then run the full Playwright eval suite (`pnpm test`) and confirm home, theme, seo, and activity specs are all green. If the JS budget is exceeded, trace the regression (likely an accidental client import of a generator) and fix by keeping the offending module server/build-only — do NOT relax the budget to hide a regression.
  </action>
  <verify>
    <automated>pnpm build && pnpm exec lhci autorun 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec lhci autorun` passes the LCP, performance-score, and script-size assertions for /de and /en against a production build
    - The client JS budget is not exceeded; no build/server-only generator (react-pdf, next/og OG route, github.ts) leaked into the client bundle
    - `pnpm test` (full Playwright suite) is green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Accessibility eval — landmarks, focus, keyboard, lang</name>
  <files>evals/a11y.spec.ts</files>
  <read_first>
    - evals/home.spec.ts (per-locale Playwright structure to mirror)
    - src/app/[locale]/layout.tsx (semantic landmarks: header/nav, main, footer; html lang={locale})
    - .planning/phases/02-recruiter-overview-live/02-UI-SPEC.md (Interaction States — global :focus-visible ring; TECH-03 semantic HTML, keyboard, contrast, real DOM text)
  </read_first>
  <action>
    Create evals/a11y.spec.ts (mirror the per-locale shape of home.spec.ts) asserting the accessibility baseline for both locales: (1) `<html lang>` equals the locale; (2) exactly one `<main>` landmark and a `<header>`/`<footer>` are present; (3) each `<section>` in the overview has an accessible name (aria-labelledby resolves to a heading) — spot-check `#career`, `#projects`, `#skills`, `#about`, `#contact`, `#activity`; (4) keyboard reachability — Tab from the top reaches the header Contact affordance, the theme toggle, the locale switcher, and the CV download button, and the focused element exposes a visible focus style (assert the active element matches expected controls in DOM order using `page.keyboard.press("Tab")` + `locator(':focus')`); (5) all `target="_blank"` anchors carry `rel="noopener noreferrer"`. Keep assertions deterministic (no timeouts). Contrast is verified manually (documented in Verification) since automated contrast needs tooling not in the stack.
  </action>
  <verify>
    <automated>pnpm exec playwright test evals/a11y.spec.ts --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - evals/a11y.spec.ts passes for both locales: html lang correct, single main + header/footer, named sections, keyboard reaches all controls with visible focus, external links carry rel="noopener noreferrer"
    - The eval is added to the suite and runs under the existing playwright config
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN/edge → browser | HTTP response headers govern transport security and framing |
| static assets → browser | The whole shipped surface being verified for zero-runtime-third-party |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Information Disclosure / Tampering | missing security response headers | medium | mitigate | Add HSTS, X-Content-Type-Options, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy via next.config headers(); CSP added with a static-script hash + Vercel analytics allowlist where feasible, else documented TODO (no site-breaking CSP) |
| T-02-17 | Elevation of Privilege (clickjacking) | site framed by a third party | medium | mitigate | `X-Frame-Options: DENY` + `frame-ancestors 'none'` in CSP |
| T-02-18 | Information Disclosure | runtime third-party network call sneaking into the bundle | high | mitigate | LHCI JS-budget assertion + manual network check confirm only same-origin + sanctioned cookieless Vercel analytics requests; generators stay build/server-only |
</threat_model>

<artifacts>
## Artifacts this plan produces

- `next.config.ts` — `headers()` security block (+ CSP or documented TODO)
- `evals/a11y.spec.ts` — landmarks/focus/keyboard/lang/external-link accessibility assertions
- (verification) passing LHCI budget run + green full Playwright suite
</artifacts>

<verification>
- `pnpm build` + `pnpm exec lhci autorun` pass the CWV budget for /de and /en on a production build
- `pnpm test` green (home, theme, seo, activity, a11y)
- Manual: run a mobile Lighthouse against the Vercel preview URL; confirm LCP < 2.5s, INP < 200ms, CLS < 0.1, no render-blocking regressions
- Manual: verify muted-on-background and accent-link contrast pass AA in both light and dark themes
- Manual: confirm response headers are present on the preview URL (browser devtools / curl)
</verification>

<success_criteria>
- ROADMAP success criterion 4 (part): CWV "good" on mobile + keyboard-accessible verified with automated gates
- TECH-01 proven on a production build; TECH-03 reinforced; security-headers threat mitigated
- Site is launch-ready pending the human cutover (Plan 07)
</success_criteria>

<output>
Create `.planning/phases/02-recruiter-overview-live/02-06-SUMMARY.md` when done
</output>
