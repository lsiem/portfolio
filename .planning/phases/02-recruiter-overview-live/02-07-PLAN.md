---
phase: 02-recruiter-overview-live
plan: 07
type: execute
wave: 5
depends_on: [02-06]
files_modified:
  - src/lib/seo.ts
autonomous: false
requirements: [TECH-01]
tags: [cutover, production, launch-verification, ctx-06, ctx-07]

must_haves:
  truths:
    - siteMetadataBase resolves to https://lsiem.de so all absolute OG/canonical/hreflang URLs point at production (CTX-06)
    - The full Phase-2 site is promoted to production on lsiem.de only after verification passes; visitors never saw a half-built state (CTX-06)
    - An external tester passes the 30-second stopwatch test on the live production URL, in both locales (ROADMAP criterion 1)
    - Shared-link OG cards, dark-mode toggle, GitHub heatmap (with PAT set), and CV download all work on the live site
  artifacts:
    - src/lib/seo.ts (siteMetadataBase flipped to https://lsiem.de)
  key_links:
    - GITHUB_TOKEN is confirmed set in the Vercel project before the production build so the heatmap shows real data, not the fallback
    - one PR from phase/02-recruiter-overview-live to main carries the full cutover (CTX-07)
---

<objective>
Cut the completed, verified Phase-2 site over to production on lsiem.de (CTX-06 / CTX-07) — the last step of the phase.

**Slice contribution:** the go-live. Everything shipped through Plans 01-06 has been built on Vercel previews and verified (Plan 06). This plan flips `siteMetadataBase` to `https://lsiem.de` (the one-line CTX-06 change), promotes to production only when verification passes, and confirms the live site with the 30-second stopwatch test and a share/dark-mode/heatmap/CV walkthrough. Commits land on branch `phase/02-recruiter-overview-live` (renamed from `fix/interim-portfolio-styling`), and one PR to `main` carries the cutover (CTX-07). Visitors never see a half-built state — lsiem.de serves the interim site until this promotion.

Purpose: realize the phase goal — the complete recruiter site live in production — the antidote to the rebuild that never ships.
Output: the domain flip + a verified production deployment on lsiem.de.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-recruiter-overview-live/02-CONTEXT.md
@src/lib/seo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip siteMetadataBase to the production domain</name>
  <files>src/lib/seo.ts</files>
  <read_first>
    - src/lib/seo.ts (siteMetadataBase L10-14 — the localhost/VERCEL fallback that flips to production; localeAlternates depends on it for absolute hreflang URLs)
    - .planning/phases/02-recruiter-overview-live/02-CONTEXT.md (CTX-06 — flip to https://lsiem.de as a one-line change; promote only when verification passes)
  </read_first>
  <action>
    Make the CTX-06 one-line change in src/lib/seo.ts so `siteMetadataBase` resolves to `https://lsiem.de` for production builds (set the base to `https://lsiem.de`, keeping the localhost fallback for local dev — e.g. prefer an explicit production origin over the `VERCEL_PROJECT_PRODUCTION_URL` fallback). This makes all absolute OG image URLs, canonical URLs, and hreflang alternates (via `localeAlternates`) resolve to the real domain. Do not change `localeAlternates` logic. Verify the build emits `https://lsiem.de/...` in the hreflang/canonical/OG absolute URLs.
  </action>
  <verify>
    <automated>pnpm build && node -e "const s=require('fs').readFileSync('src/lib/seo.ts','utf8');if(!/lsiem\\.de/.test(s)){console.error('siteMetadataBase not flipped to lsiem.de');process.exit(1)}console.log('domain flip present')"</automated>
  </verify>
  <acceptance_criteria>
    - `siteMetadataBase` resolves to `https://lsiem.de` for production builds; local dev still works
    - Absolute OG/canonical/hreflang URLs in the built output point at https://lsiem.de
    - `localeAlternates` behavior is unchanged; `pnpm build` succeeds
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production cutover — promote to lsiem.de and verify live</name>
  <action>
    Human-gated cutover (CTX-06). Confirm GITHUB_TOKEN is set in the Vercel Production env, open one PR from `phase/02-recruiter-overview-live` to `main` (CTX-07) with green CI, promote the verified build to production, then verify the live https://lsiem.de site per the steps below. Do NOT promote until Plan 06 verification and CI are green.
  </action>
  <what-built>
    The complete Phase-2 recruiter site — overview (hero/value-prop, career, projects/case-studies, skills, about, one-click contact), CV-PDF download per locale, 3-state dark mode, OG images + Person JSON-LD, and the build-time GitHub heatmap — built and verified on a Vercel preview, with siteMetadataBase now flipped to https://lsiem.de. Ready to promote to production.
  </what-built>
  <how-to-verify>
    1. Confirm `GITHUB_TOKEN` (read-only `read:user`, account lsiem) is set in the Vercel project environment for Production, so the production build bakes real contribution data (without it the heatmap shows the fallback line). Vercel → Project → Settings → Environment Variables.
    2. Ensure work is on branch `phase/02-recruiter-overview-live` (renamed from `fix/interim-portfolio-styling`) and open one PR to `main` carrying the full cutover (CTX-07). Confirm CI is green (content-parity gate, LHCI budget, Playwright evals).
    3. Promote the verified deployment to production (Vercel promote / merge → production deploy) so lsiem.de serves the new site.
    4. On the LIVE https://lsiem.de/de and /en, run the 30-second stopwatch test with a fresh external tester: they must find who (name/role), what (value prop + skills/timeline), and contact within 30 seconds. Confirm the first viewport shows name + role + value prop with no intro gate.
    5. Verify one-click contact (header Contact from the bottom of the page → #contact), the CV download button downloads Lasse-Siemoneit-CV-de.pdf / -en.pdf (selectable text, locale-correct), the 3-state theme toggle works and persists with no flash, and the GitHub heatmap shows real data (monochrome, no accent).
    6. Paste https://lsiem.de/de and /en into a link-preview validator: confirm the designed OG card renders per locale, and validate the Person JSON-LD. Confirm hreflang (self + x-default → de) on the live pages.
    7. Run a mobile Lighthouse against https://lsiem.de: confirm CWV "good" (LCP < 2.5s, INP < 200ms, CLS < 0.1) and no runtime third-party network calls beyond the sanctioned cookieless Vercel analytics.
  </how-to-verify>
  <resume-signal>Type "approved" once lsiem.de serves the verified Phase-2 site and all checks pass, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preview → production | Promotion moves the verified build to the public production domain |
| Vercel env → production build | GITHUB_TOKEN presence at build time governs whether real contribution data is baked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-19 | Information Disclosure | promoting an unverified/half-built build | medium | mitigate | Gate promotion behind the blocking human-verify checkpoint + green CI (Plan 06 budget/evals); CTX-06 "promote only when verification passes" |
| T-02-01 | Information Disclosure | GITHUB_TOKEN in Vercel env | high | mitigate | Confirm the token is a read-only `read:user` PAT set as a server-only (unprefixed) Vercel env var; never surfaced to the client bundle (re-verified live in step 7) |
| T-02-18 | Information Disclosure | runtime third-party call on production | high | mitigate | Step 7 live network check confirms only same-origin + sanctioned cookieless Vercel analytics requests (DSGVO) |
</threat_model>

<artifacts>
## Artifacts this plan produces

- `src/lib/seo.ts` — `siteMetadataBase` flipped to `https://lsiem.de`
- A verified production deployment of the Phase-2 site on lsiem.de (via PR from `phase/02-recruiter-overview-live` to `main`, CTX-07)
</artifacts>

<verification>
- `pnpm build` emits https://lsiem.de absolute URLs in OG/canonical/hreflang
- Human checkpoint confirms: GITHUB_TOKEN set in Vercel; PR to main + green CI; production promotion; live 30-second stopwatch test (both locales); one-click contact; CV download; dark-mode toggle; OG card + Person JSON-LD; mobile CWV "good"; zero runtime third-party beyond cookieless Vercel analytics
</verification>

<success_criteria>
- ROADMAP goal met: a hurried visitor finds all facts (who/what/skills/timeline/contact/CV) in under 30 seconds on the complete, fast, bilingual site — live in production on lsiem.de
- All five Phase-2 success criteria verified on the production URL
- TECH-01 launch-verified on production; CTX-06/CTX-07 satisfied
</success_criteria>

<output>
Create `.planning/phases/02-recruiter-overview-live/02-07-SUMMARY.md` when done
</output>
