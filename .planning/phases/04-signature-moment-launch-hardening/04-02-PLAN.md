---
phase: 04-signature-moment-launch-hardening
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/motion/hero-scene-canvas.tsx
  - src/components/motion/hero-scene-topology.tsx
  - src/components/motion/hero-scroll-bridge.tsx
  - src/lib/hero-scene-progress.ts
  - src/app/[locale]/page.tsx
  - evals/hero-3d.spec.ts
autonomous: true
requirements: [WOW-01]

must_haves:
  truths:
    - "The hero background renders the Infrastructure Topology scene (D-20): 12 nodes, 18 edges, one accent pulse path — fully procedural, no external model URLs"
    - "Scene colors read from CSS tokens (--border, --accent, --background transparent) — no hardcoded theme hex unrelated to globals.css"
    - "Scroll progress through #hero drives subtle camera/group rotation via heroSceneProgress ref (D-22) — GSAP ScrollTrigger writes, R3F useFrame reads, frameloop=demand invalidate on update"
    - "No drei ScrollControls — Lenis+ScrollTrigger remain the only scroll authority (D-08, ARCHITECTURE anti-pattern)"
    - "The signature moment is decorative only (aria-hidden layer, pointer-events-none) and does not obscure hero text contrast"
  artifacts:
    - path: "src/components/motion/hero-scene-topology.tsx"
      provides: "procedural topology graph scene"
      contains: "heroSceneProgress"
    - path: "src/components/motion/hero-scroll-bridge.tsx"
      provides: "ScrollTrigger writer for hero progress"
      contains: "ScrollTrigger"
    - path: "src/components/motion/hero-scene-canvas.tsx"
      provides: "Canvas hosting Topology + bridge mount"
      contains: "HeroSceneTopology"
  key_links:
    - from: "src/components/motion/hero-scroll-bridge.tsx"
      to: "src/lib/hero-scene-progress.ts"
      via: "onUpdate writes progress ref"
      pattern: "heroSceneProgress"
    - from: "src/components/motion/hero-scene-topology.tsx"
      to: "src/lib/hero-scene-progress.ts"
      via: "useFrame reads progress ref"
      pattern: "useFrame"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/motion/hero-scroll-bridge.tsx"
      via: "mounted inside hero when gate can run (client sibling)"
      pattern: "HeroScrollBridge"
---

## Phase Goal

*(This plan delivers the creative core of WOW-01: the identity-bound topology lattice and scroll-linked motion.)*

<objective>
Replace the placeholder canvas content with the full Infrastructure Topology procedural scene (D-20), wire the GSAP→R3F scroll bridge (D-22), and mount the scroll bridge from the hero section. The result is the single signature WebGL moment — visible on gated desktop only, synchronized with native/Lenis scroll.

Purpose: Fulfill the product promise of an engineered, identity-specific 3D hero background.
Output: `hero-scene-topology.tsx`, `hero-scroll-bridge.tsx`, integrated canvas, scroll-driven motion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-signature-moment-launch-hardening/04-UI-SPEC.md
@.planning/phases/04-signature-moment-launch-hardening/04-RESEARCH.md
@.planning/phases/04-signature-moment-launch-hardening/04-PATTERNS.md
@src/components/motion/hero-scene-canvas.tsx
@src/components/motion/hero-intro.tsx
@src/app/[locale]/page.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure Topology scene (procedural)</name>
  <files>src/components/motion/hero-scene-topology.tsx, src/components/motion/hero-scene-canvas.tsx</files>
  <read_first>
    - 04-UI-SPEC.md §"Topology Scene Parameters" — node positions, edge count, accent path 0→4→8→11
    - 04-RESEARCH.md D-20, D-24 performance budget
    - globals.css — --border, --accent token values (read at runtime, do not duplicate hex in comments as SSOT)
  </read_first>
  <action>
    Implement `HeroSceneTopology` in `hero-scene-topology.tsx`:
    - Read CSS custom properties once on mount for line/node colors.
    - 12 nodes at UI-SPEC seed positions (slight Y jitter).
    - 18 edges via `drei` `Line` between defined pairs (include accent path chain).
    - Accent pulse animation on the highlight path only — `useFrame` advances dash offset or emissive intensity; call `invalidate()` from within when animating OR tie pulse to scroll progress only for v1 simplicity.
    - `useFrame`: apply `rotation.y = heroSceneProgress.current * 0.14` and subtle `position.x` parallax per UI-SPEC.
    - `useMemo` geometries/materials; dispose on unmount.
    - Keep draw calls &lt; 50.

    Wire into `hero-scene-canvas.tsx` replacing placeholder mesh.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `hero-scene-topology.tsx` exists with 12 node positions from UI-SPEC
    - Uses `drei` Line (not hand-rolled THREE.Line boilerplate in page.tsx)
    - No `fetch()` or `useGLTF` external URLs
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Signature topology scene renders in gated desktop canvas.</done>
</task>

<task type="auto">
  <name>Task 2: Scroll bridge — ScrollTrigger → progress ref → invalidate</name>
  <files>src/components/motion/hero-scroll-bridge.tsx, src/lib/hero-scene-progress.ts, src/components/motion/hero-scene-canvas.tsx, src/app/[locale]/page.tsx</files>
  <read_first>
    - 04-RESEARCH.md §"Pattern 3: Scroll Progress Bridge"
    - career-spine.tsx — lazy gsap/ScrollTrigger import pattern
    - motion-provider.tsx — Lenis+ScrollTrigger already synced at root
  </read_first>
  <action>
    Create `HeroScrollBridge` client component:
    - Only mount when same gate conditions as 3D OR always mount but only create ScrollTrigger when motion gate open (prefer: mount in page hero next to HeroSceneSlot; internally no-op when reduced-motion/coarse).
    - Lazy `import("gsap/ScrollTrigger")` + register plugin if not already (guard duplicate registration).
    - `ScrollTrigger.create({ trigger: "#hero", start: "top top", end: "bottom top", onUpdate: (self) => { heroSceneProgress.current = self.progress; invalidateFn?.(); } })`.
    - Export module-level `setHeroSceneInvalidate(fn)` from `hero-scene-progress.ts` or sibling; canvas registers on mount.

    Add `<HeroScrollBridge />` inside `#hero` in `page.tsx` (client import) — must not block SSR hero content.

    **Forbidden:** `ScrollControls` from drei.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && rg -n "ScrollControls" src/ --glob "*.tsx" && test $? -ne 0 || true; rg -q "ScrollControls" src/ && exit 1 || echo "no ScrollControls"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r ScrollControls src/` returns empty
    - `hero-scroll-bridge.tsx` writes `heroSceneProgress.current` on scroll
    - Canvas `frameloop="demand"` receives `invalidate()` on progress change
    - Hero scroll still works with Lenis on desktop (manual smoke: no jitter)
  </acceptance_criteria>
  <done>Hero scroll progress drives 3D parallax without a second scroll engine.</done>
</task>

<task type="auto">
  <name>Task 3: Visual QA evals + contrast spot-check</name>
  <files>evals/hero-3d.spec.ts</files>
  <read_first>
    - 04-UI-SPEC.md §"Hero 3D Layer Placement"
  </read_first>
  <action>
    Extend hero-3d.spec.ts:
    - On desktop gate-forced path: canvas exists in #hero after idle wait.
    - Hero H1 text remains visible and has opacity ≥ 0.99.
    - Optional screenshot assertion deferred to human UAT (04-04).

    Run immersive + home suites for regressions.
  </action>
  <verify>
    <automated>pnpm exec playwright test evals/hero-3d.spec.ts evals/immersive.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All targeted evals pass both locales
    - No WOW-04 regression (first-paint hero tests in immersive.spec.ts still green)
  </acceptance_criteria>
  <done>Automated coverage for topology mount + hero text always on top.</done>
</task>

</tasks>

Create `.planning/phases/04-signature-moment-launch-hardening/04-02-SUMMARY.md` when done.
