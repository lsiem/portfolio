---
phase: 04-signature-moment-launch-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/components/motion/hero-scene-slot.tsx
  - src/components/motion/hero-scene-canvas.tsx
  - src/lib/hero-scene-progress.ts
  - evals/hero-3d.spec.ts
autonomous: false
requirements: [WOW-01]

must_haves:
  truths:
    - "A capability gate (D-23) decides whether WebGL may mount — requires prefers-reduced-motion: no-preference, pointer: fine, hardwareConcurrency >= 4, and WebGL2 availability"
    - "The gate reads browser signals via useSyncExternalStore with a false server snapshot — never useState+useEffect for matchMedia (matches MotionProvider / ThemeToggle convention)"
    - "three.js / R3F are NOT present in the homepage initial JS chunk — only an async lazy chunk after the gate opens"
    - "HeroSceneSlot preserves Phase-3 geometry (absolute inset-0 -z-10 pointer-events-none aria-hidden) and renders the same empty layer when the gate is closed"
    - "When the gate opens, a Canvas mounts lazily after requestIdleCallback with frameloop=demand and dpr capped (D-21, D-24) — even if the scene is a minimal placeholder in this plan"
    - "First-paint hero SSR content (H1, value-prop, nav) is unchanged and never blocked by 3D load (WOW-04 / D-17)"
  artifacts:
    - path: "src/components/motion/hero-scene-slot.tsx"
      provides: "client capability gate + idle-deferred dynamic import of hero-scene-canvas"
      contains: "useSyncExternalStore"
    - path: "src/components/motion/hero-scene-canvas.tsx"
      provides: "R3F Canvas shell (dynamic import target) with D-24 renderer settings"
      contains: "Canvas"
    - path: "src/lib/hero-scene-progress.ts"
      provides: "module-scope scroll progress ref for GSAP→R3F bridge (D-22)"
    - path: "evals/hero-3d.spec.ts"
      provides: "gate-closed contracts (no canvas under reduced-motion / coarse pointer)"
  key_links:
    - from: "src/components/motion/hero-scene-slot.tsx"
      to: "src/components/motion/motion-provider.tsx"
      via: "same useSyncExternalStore + lazy import gate pattern"
      pattern: "useSyncExternalStore"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/motion/hero-scene-slot.tsx"
      via: "hero section background layer (unchanged import site)"
      pattern: "HeroSceneSlot"
---

## Phase Goal

**As a** visitor on a capable desktop, **I want to** see a signature 3D moment behind the hero **without** slowing first paint, **so that** the site feels engineered and memorable while recruiters still get facts instantly.

*(This plan delivers the architectural foundation: capability gate, lazy R3F mount, progress ref seam — the vertical slice that proves three.js stays out of the initial bundle.)*

<objective>
Install the R3F stack behind a human-approved package-legitimacy gate, convert `HeroSceneSlot` into a client capability gate that lazy-loads a minimal `Canvas` shell after `requestIdleCallback`, and add the `heroSceneProgress` ref module. Ship a placeholder scene (single wireframe box or empty scene + clear lights) to prove the mount contract before the signature topology lands in 04-02.

Purpose: Lock the load-bearing boundaries — gate, lazy chunk, no LCP regression — so the expensive creative work in Plan 02 cannot accidentally ship three.js in the critical path.
Output: pinned R3F deps, gated lazy canvas, progress ref, initial Playwright gate tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-signature-moment-launch-hardening/04-CONTEXT.md
@.planning/phases/04-signature-moment-launch-hardening/04-RESEARCH.md
@.planning/phases/04-signature-moment-launch-hardening/04-UI-SPEC.md
@.planning/phases/04-signature-moment-launch-hardening/04-PATTERNS.md
@.planning/phases/04-signature-moment-launch-hardening/04-SECURITY.md
@src/components/motion/hero-scene-slot.tsx
@src/components/motion/motion-provider.tsx
@src/app/[locale]/page.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Package legitimacy gate — three + R3F + drei</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - 04-RESEARCH.md §"Package Legitimacy Audit"
    - 04-SECURITY.md T-04-SC
    - package.json — React pin must stay ~19.2.0 (R3F peer &lt;19.3)
  </read_first>
  <action>
    Present evidence to the human before any install. On explicit approval run:
    `pnpm add three@0.185.1 @react-three/fiber@9.6.1 @react-three/drei@10.7.7` and `pnpm add -D @types/three@0.185.0`.
    Re-run `npm view three scripts.postinstall`, `npm view @react-three/fiber scripts.postinstall`, `npm view @react-three/drei scripts.postinstall` — expect empty.
    Confirm github.com/mrdoob/three.js and github.com/pmndrs/* repos.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');for(const k of ['three','@react-three/fiber','@react-three/drei']){if(!p.dependencies[k]){console.error('missing '+k);process.exit(1)}}console.log('3d deps present')"</automated>
  </verify>
  <acceptance_criteria>
    - Pinned versions match 04-RESEARCH.md
    - React remains ~19.2.0
    - postinstall scripts empty for all three runtime packages
    - Human approval recorded in 04-01-SUMMARY.md
  </acceptance_criteria>
  <done>three/R3F/drei pinned in package.json; lockfile updated; legitimacy gate passed.</done>
</task>

<task type="auto">
  <name>Task 2: Capability gate + heroSceneProgress ref + lazy Canvas shell</name>
  <files>src/lib/hero-scene-progress.ts, src/components/motion/hero-scene-slot.tsx, src/components/motion/hero-scene-canvas.tsx</files>
  <read_first>
    - 04-RESEARCH.md §"Pattern 1: Capability Gate" and §"Pattern 2: Lazy Mount Stack"
    - 04-UI-SPEC.md §"Capability Gate UX" and §"Loading / Mount Choreography"
    - motion-provider.tsx — useSyncExternalStore + dynamic import pattern
    - hero-scene-slot.tsx — current empty Server Component (convert to client gate)
  </read_first>
  <action>
    Create `src/lib/hero-scene-progress.ts` exporting `{ current: number }` mutable ref (D-22).

    Convert `hero-scene-slot.tsx` to `"use client"`:
    - Implement `useSyncExternalStore` gate per D-23 (reduced-motion, coarse pointer, hardwareConcurrency &lt; 4, WebGL2 probe). Server snapshot always `false`.
    - When gate closed: return the **identical** empty `div` as Phase 3 (`aria-hidden`, `absolute inset-0 -z-10 pointer-events-none`).
    - When gate open: after `requestIdleCallback` (2000ms timeout fallback), set local `ready` state and render a dynamically imported `HeroSceneCanvas` via `next/dynamic(() => import("./hero-scene-canvas"), { ssr: false })` — no loading spinner (D-21).
    - Fade-in: optional opacity transition on wrapper max 600ms.

    Create `hero-scene-canvas.tsx` as the dynamic import target:
    - `<Canvas frameloop="demand" dpr={[1, 1.5]} gl={{ powerPreference: "low-power", antialias: false }} className="h-full w-full" />`
  - Placeholder content: minimal `<mesh>` or empty scene with ambient light — topology arrives in 04-02.
    - Export `setHeroSceneInvalidate(fn)` or accept ref from bridge (stub for 04-02).

    **Bundle discipline:** do NOT import `three` or `@react-three/*` in `hero-scene-slot.tsx` — only inside `hero-scene-canvas.tsx` loaded dynamically.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && node -e "
const fs=require('fs');const {execSync}=require('child_process');
const slot=fs.readFileSync('src/components/motion/hero-scene-slot.tsx','utf8');
if(!slot.includes('useSyncExternalStore')){console.error('missing gate');process.exit(1)}
if(/from ['\"]three['\"]/.test(slot)||/@react-three/.test(slot)){console.error('three imported in slot');process.exit(1)}
console.log('gate ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `hero-scene-slot.tsx` uses `useSyncExternalStore`; no direct three imports in slot file
    - `hero-scene-canvas.tsx` is only reachable via dynamic import
    - `pnpm build` succeeds
    - Inspect `.next` build output: homepage first-load JS chunk does not include the string `three` (grep chunk files for the home route)
    - Gate-closed path renders empty div matching Phase 3 classes
  </acceptance_criteria>
  <done>Lazy gated Canvas shell mounts on capable desktop only; progress ref exists; initial bundle clean.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright gate contracts + regression guard</name>
  <files>evals/hero-3d.spec.ts, evals/immersive.spec.ts</files>
  <read_first>
    - evals/immersive.spec.ts — locale loop + emulateMedia patterns
    - 04-UI-SPEC.md §"Capability Gate UX"
  </read_first>
  <action>
    Add `evals/hero-3d.spec.ts`:
    - For `/de` and `/en`: under `prefers-reduced-motion: reduce`, assert `#hero canvas` count is 0.
    - Under `pointer: coarse` emulation (Playwright `hasTouch: true` + coarse media if available), assert no canvas OR gate closed (document chosen approach).
    - Default desktop: optionally assert canvas appears after short wait — may be flaky in CI mobile emulation; prefer testing gate function via `page.addInitScript` forcing `hardwareConcurrency=8` and fine pointer when feasible.
    - Assert hero H1 still visible with or without canvas (WOW-04).

    Run full eval suite to ensure no Phase 3 regressions.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && pnpm exec playwright test evals/hero-3d.spec.ts evals/immersive.spec.ts evals/home.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - hero-3d.spec.ts passes on both locales
    - immersive.spec.ts + home.spec.ts still green
    - LHCI script:size still PASS on production build (run `pnpm exec lhci autorun` if CI machine allows)
  </acceptance_criteria>
  <done>Automated proof that reduced-motion and touch paths never mount WebGL; hero content always present.</done>
</task>

</tasks>

## Verification summary (plan-level)

- [ ] R3F deps installed with human legitimacy approval
- [ ] Capability gate + lazy canvas shell wired
- [ ] three.js absent from initial homepage chunk
- [ ] Playwright gate tests green

Create `.planning/phases/04-signature-moment-launch-hardening/04-01-SUMMARY.md` when done.
