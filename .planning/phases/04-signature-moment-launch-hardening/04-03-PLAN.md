---
phase: 04-signature-moment-launch-hardening
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/components/motion/hero-scene-canvas.tsx
  - src/components/motion/hero-scene-topology.tsx
  - src/components/motion/hero-scene-slot.tsx
  - evals/hero-3d.spec.ts
  - .planning/STATE.md
autonomous: true
requirements: [WOW-01]

must_haves:
  truths:
    - "webglcontextlost triggers graceful degradation: canvas unmounts, empty aria-hidden layer returns, hero SSR content unaffected (D-25, ROADMAP SC2)"
    - "All BufferGeometry and Material instances dispose on unmount; no monotonic renderer.info.memory growth in a 2-minute desktop smoke test"
    - "Context-loss handler calls event.preventDefault() and does NOT loop forceContextRestore()"
    - "Capability gate still prevents canvas on reduced-motion, touch, low-core, and no-WebGL2 paths (D-23)"
  artifacts:
    - path: "src/components/motion/hero-scene-canvas.tsx"
      provides: "context-loss listeners + dispose on unmount"
      contains: "webglcontextlost"
    - path: "evals/hero-3d.spec.ts"
      provides: "fallback + disposal contracts where automatable"
  key_links:
    - from: "src/components/motion/hero-scene-canvas.tsx"
      to: "src/components/motion/hero-scene-slot.tsx"
      via: "dead state unmounts canvas, slot shows empty layer"
      pattern: "webglcontextlost"
---

<objective>
Harden the WebGL layer for production: context-loss fallback, disposal discipline, and expanded automated tests. Document any LHCI script-budget or LCP delta after 3D lands in STATE.md blockers if measurable.

Purpose: Close PITFALLS #4 (desktop WebGL on every device) and #5 (context loss) entry requirements before launch verification.
Output: resilient canvas lifecycle + eval coverage + STATE.md performance note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-signature-moment-launch-hardening/04-RESEARCH.md
@.planning/phases/04-signature-moment-launch-hardening/04-SECURITY.md
@.planning/STATE.md
@src/components/motion/hero-scene-canvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Context-loss handler + dispose discipline</name>
  <files>src/components/motion/hero-scene-canvas.tsx, src/components/motion/hero-scene-topology.tsx, src/components/motion/hero-scene-slot.tsx</files>
  <read_first>
    - 04-RESEARCH.md §"Pattern 4" and §"Pattern 5"
    - 04-CONTEXT.md D-25
  </read_first>
  <action>
    In `hero-scene-canvas.tsx` `onCreated`:
    - Attach `webglcontextlost` → `preventDefault()`, set parent-notified `dead` flag (callback prop or shared module setter) so `HeroSceneSlot` unmounts canvas and shows empty layer.
    - Attach `webglcontextrestored` only to log in dev — do not auto-remount in v1 (product: stay degraded).

    In `hero-scene-topology.tsx`:
    - `useEffect` cleanup disposes geometries/materials created locally.
    - Traverse on unmount if needed.

    Ensure `HeroSceneSlot` can reset to empty div when child signals dead.

    **No** `gl.forceContextRestore()` retry loops.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && rg -n "forceContextRestore" src/; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `webglcontextlost` listener present in hero-scene-canvas.tsx
    - `forceContextRestore` absent from src/
    - Manual dev test: `gl.forceContextLoss()` in console → canvas clears, hero text remains
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Context loss degrades to Phase-3-equivalent empty hero layer.</done>
</task>

<task type="auto">
  <name>Task 2: Expanded evals + production-build budget check</name>
  <files>evals/hero-3d.spec.ts, .planning/STATE.md</files>
  <read_first>
    - lighthouserc.json — script:size 184643, LCP warn 2500
    - 03-04-SUMMARY.md LHCI numbers for baseline comparison
  </read_first>
  <action>
    Extend hero-3d.spec.ts:
    - Simulate context loss if Playwright exposes WebGL (optional — document skip if not feasible).
    - Re-assert gate paths after 04-02 topology landed.

    Run `pnpm build && pnpm exec lhci autorun` (or single URL if timeboxed). Record in STATE.md:
    - script:size PASS/FAIL vs 184643
    - LCP values /de + /en (note: mobile emulation likely gate-closed — document)
    - Whether three appears only in async chunks (brief note)

    If script:size FAILS due to accidental eager import, fix before closing — do not relax budget in this plan.
  </action>
  <verify>
    <automated>pnpm exec playwright test evals/hero-3d.spec.ts evals/immersive.spec.ts evals/home.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Full eval suite green
    - STATE.md updated with post-3D LHCI snapshot under Blockers/Concerns or Performance Metrics
    - resource-summary:script:size PASS on production build OR explicit fix committed before plan close
  </acceptance_criteria>
  <done>Automated gates green; performance delta documented for launch plan.</done>
</task>

</tasks>

Create `.planning/phases/04-signature-moment-launch-hardening/04-03-SUMMARY.md` when done.
