---
phase: 04-signature-moment-launch-hardening
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/04-signature-moment-launch-hardening/04-UAT.md
  - .planning/phases/04-signature-moment-launch-hardening/04-VERIFICATION.md
autonomous: false
requirements: [WOW-01]

must_haves:
  truths:
    - "WOW-01 marked complete in REQUIREMENTS.md only after human verifies signature 3D on capable desktop production URL"
    - "Production https://lsiem.de passes launch verification: TECH-01 mobile CWV audit with 3D tier available on desktop audit path, MODE-01 30s stopwatch, MODE-02 reduced-motion walkthrough (D-26)"
    - "Mid-tier Android real-device check completed and recorded in 04-UAT.md (pass = stable or cleanly gated off)"
    - "Phase 4 success criteria in ROADMAP.md reflected as complete with date after all gates pass"
    - "No promotion while pre-existing blocking gates fail (CV 404, Impressum placeholder, GITHUB_TOKEN absent) — inherit 02-07 gates"
  artifacts:
    - path: ".planning/phases/04-signature-moment-launch-hardening/04-UAT.md"
      provides: "human launch walkthrough results (7 dimensions per UI-SPEC)"
    - path: ".planning/phases/04-signature-moment-launch-hardening/04-VERIFICATION.md"
      provides: "phase goal achievement report"
  key_links:
    - from: "04-UAT.md"
      to: "https://lsiem.de/de"
      via: "production verification"
      pattern: "lsiem.de"
---

<objective>
Close the v1 milestone: verify every launch promise on the production URL, promote the Phase-4 build if needed, and mark WOW-01 + phase complete in planning artifacts.

Purpose: The phase goal's second clause — "jede Zusage der Site ist vor dem Launch auf der Produktions-URL verifiziert."
Output: 04-UAT.md, 04-VERIFICATION.md, updated REQUIREMENTS/ROADMAP/STATE, production deployment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-signature-moment-launch-hardening/04-UI-SPEC.md
@.planning/phases/04-signature-moment-launch-hardening/04-CONTEXT.md
@.planning/phases/02-recruiter-overview-live/02-07-PLAN.md
@.planning/phases/03-design-direction-immersive-experience/03-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight — full CI suite + production-build LHCI</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - lighthouserc.json
    - 04-03-SUMMARY.md LHCI notes if present
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Run locally: `pnpm lint`, `pnpm build`, `pnpm test` (full Playwright), `pnpm exec lhci autorun`.
    Record results in STATE.md (script:size, LCP, TBT, CLS per /de and /en).

    If any assertion fails, fix in code and re-run — do not proceed to human cutover with red CI.

    **LCP interpretation:** If LCP > 2500ms matches pre-3D Bricolage font baseline (Phase 3), note explicitly whether 3D added delta vs baseline. Escalate only if 3D caused regression or production exceeds budget with new evidence.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && pnpm test && pnpm exec lhci autorun</automated>
  </verify>
  <acceptance_criteria>
    - Lint + build + full Playwright green
    - LHCI script:size and TBT/CLS error-level assertions pass
    - LCP documented (warn-level acceptable if pre-existing font cost confirmed unchanged by 3D)
  </acceptance_criteria>
  <done>Automated gates green; metrics recorded for human sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production launch walkthrough — UAT on lsiem.de</name>
  <action>
    Human-gated launch verification (D-26). Promote to production via PR to main only when Task 1 is green and pre-cutover gates from 02-07 still pass (CV PDFs, Impressum, GITHUB_TOKEN).

    Perform and record in `04-UAT.md` (mirror 03-UAT.md structure):

    | # | Dimension | Pass? |
    |---|-----------|-------|
    | 1 | 3D wow desktop (/de + /en) — topology visible, text readable | |
    | 2 | Skippable — facts at first paint, 3D never blocks | |
    | 3 | Quiet — reduced-motion, full content, no canvas | |
    | 4 | Mobile — no canvas, intentional layout, no scrolljacking | |
    | 5 | 30-second stopwatch external tester (MODE-01) | |
    | 6 | Mid-tier Android real device | |
    | 7 | Production CWV (Vercel Speed Insights or LHCI against live URL) | |

    Also verify: dark mode toggle, CV download, OG share card, GitHub heatmap on production.
  </action>
  <what-built>
    Complete v1 site with signature Infrastructure Topology hero 3D (capability-gated), full immersive Phase-3 layer, recruiter facts path, bilingual content, live on lsiem.de.
  </what-built>
  <how-to-verify>
    Desktop Chrome (pointer fine, motion on): visit https://lsiem.de/de and /en — confirm topology behind hero.
    Enable OS reduced motion — confirm no WebGL, all sections present.
    Phone or DevTools mobile — confirm no WebGL.
    Stopwatch test with external volunteer on production.
    Optional: run Lighthouse mobile against live production URLs.
  </how-to-verify>
  <acceptance_criteria>
    - 04-UAT.md committed with 7/7 dimensions PASS (or documented FAIL with fix-forward before marking phase complete)
    - Android item explicitly recorded (device model + outcome)
    - Human sign-off recorded in 04-04-SUMMARY.md — never auto-approve (`auto_advance: false`)
  </acceptance_criteria>
  <done>Production launch verified; UAT artifact on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Close phase — REQUIREMENTS, ROADMAP, VERIFICATION</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/04-signature-moment-launch-hardening/04-VERIFICATION.md</files>
  <read_first>
    - 03-VERIFICATION.md — report structure template
    - REQUIREMENTS.md WOW-01 row
  </read_first>
  <action>
    Only after Task 2 human PASS:
    - Mark WOW-01 complete in REQUIREMENTS.md
    - Update ROADMAP Phase 4: Plans 4/4, completed date, success criteria checked
    - Update STATE.md: Phase 4 complete, 100% milestone, clear Phase-4 blockers resolved
    - Write 04-VERIFICATION.md with goal achievement table and links to evals + UAT

    If Task 2 fails any blocking dimension, do NOT mark complete — file gaps in VERIFICATION.md and stop.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-signature-moment-launch-hardening/04-UAT.md && test -f .planning/phases/04-signature-moment-launch-hardening/04-VERIFICATION.md && rg "WOW-01.*Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - WOW-01 status Complete only with UAT PASS
    - ROADMAP Phase 4 shows complete
    - STATE.md progress 100% / 4/4 phases
    - 04-VERIFICATION.md status `passed` or `human_needed` honestly — not `passed` without UAT
  </acceptance_criteria>
  <done>v1 milestone planning artifacts reflect shipped state.</done>
</task>

</tasks>

Create `.planning/phases/04-signature-moment-launch-hardening/04-04-SUMMARY.md` when done.
