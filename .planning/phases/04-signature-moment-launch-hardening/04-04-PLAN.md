---
phase: 04-signature-moment-launch-hardening
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - src/components/scene/constellation.tsx
  - src/components/scene/constellation-data.ts
  - src/components/scene/scene-bridge.ts
  - src/components/scene/constellation-canvas.tsx
  - src/lib/theme-color-resolver.ts
  - src/components/motion/hero-intro.tsx
  - evals/scene.spec.ts
  - lighthouserc.webgl.json
autonomous: true
requirements: [WOW-01]
must_haves:
  truths:
    - "A visitor on a capable device sees the multi-agent constellation living behind the hero text: drifting nodes, hairline edges, and irregular orange message pulses traveling edges (D-01, D-03, D-04)"
    - "The headline and value-prop remain the unambiguous focal point: node density behind the central reading column is at most 30% of the outer-thirds density, and resting materials use only muted/border tokens at low opacity (D-08)"
    - "When the intro is still running the constellation assembles as the final beat of the existing hero-intro GSAP timeline; when the chunk arrives late it fades in over ~800ms instead — never a double intro (D-09)"
    - "Scrolling past the hero recedes and dissolves the scene mapped to exit progress, then rendering pauses (frameloop never) — GPU work is zero for the rest of the visit, on desktop AND capable phones (D-05, RESEARCH Pitfall 8)"
    - "On pointer:fine devices, nodes near the cursor gently attract and illuminate toward accent; on touch/keyboard the pointer feature is a no-op while the scene itself still runs (D-06, D-07)"
    - "Scene colors resolve from the CSS custom properties in both themes and re-resolve on theme change — zero hardcoded hex in scene code"
  artifacts:
    - path: "src/components/scene/constellation-data.ts"
      provides: "D-02 hidden structure: graph derived from ELIA's multi-agent topology (orchestrator hub + satellite agent clusters), density-masked placement, per-tier counts"
      contains: "density"
    - path: "src/components/scene/constellation.tsx"
      provides: "scene interior: useFrame drift/pulses/pointer, BufferGeometry mutation, no per-frame React state"
      contains: "useFrame"
    - path: "src/components/scene/scene-bridge.ts"
      provides: "one-way GSAP→scene bridge: scrollProgress, introBeatAt, pointer, paused"
      contains: "sceneBridge"
    - path: "src/lib/theme-color-resolver.ts"
      provides: "CSS token → THREE.Color resolution + data-theme MutationObserver + prefers-color-scheme listener"
      contains: "data-theme"
    - path: "src/components/motion/hero-intro.tsx"
      provides: "D-09 boot-beat handshake: timeline completion recorded into sceneBridge"
      contains: "introBeatAt"
    - path: "lighthouserc.webgl.json"
      provides: "documented second budget for the ?webgl=force 3D-active audit (RESEARCH Open Question 1a)"
      contains: "webgl=force"
  key_links:
    - from: "src/components/motion/hero-intro.tsx"
      to: "src/components/scene/scene-bridge.ts"
      via: "timeline onComplete writes sceneBridge.introBeatAt (GSAP-land writes, canvas-land reads)"
      pattern: "introBeatAt"
    - from: "src/components/scene/constellation.tsx"
      to: "src/components/scene/scene-bridge.ts"
      via: "useFrame reads scrollProgress/pointer/paused every frame — no React state per frame"
      pattern: "sceneBridge"
    - from: "src/components/scene/constellation.tsx"
      to: "src/lib/theme-color-resolver.ts"
      via: "material colors resolved from --muted/--border/--accent/--foreground tokens"
      pattern: "--accent"
---

## Phase Goal (user story)

**As a** visitor on a capable device, **I want to** encounter one living, identity-bound signature moment in the hero, **so that** the site feels like a system that thinks — while every visitor still gets the fast facts unharmed and every promise is proven on production before launch.

*(MVP slice: after this plan, the 04-03 proof-of-life stub becomes the real signature moment — the ELIA-bound multi-agent constellation with boot-beat entrance, message pulses, pointer influence, and scroll-linked exit. This IS WOW-01's user-facing payload.)*

<objective>
Replace the 04-03 stub with the full multi-agent constellation (D-01): the hidden-structure graph derived from ELIA's agent topology (D-02), precise points + hairlines with occasional orange pulses (D-03), slow drift + irregular message pulses (D-04), subtle pointer influence on pointer:fine (D-06), atmospheric density-masked composition (D-08), the boot-sequence entrance with graceful late-load fade-in (D-09), and the scroll-linked exit that pauses the GPU after the hero leaves the viewport (D-05) — all reading theme tokens, all inside the 04-03 gate, all deletable with `src/components/scene/`.

Purpose: this is the phase's creative anchor — "the constellation is ELIA's world made visible: agents passing messages" (CONTEXT specifics). Every visual value is prescribed by 04-UI-SPEC (geometry, densities, timings, color/opacity table); the executor implements, tunes within the stated ±20%, and never re-litigates the contract.

Output: `constellation-data.ts`, `constellation.tsx`, `scene-bridge.ts`, `theme-color-resolver.ts`, the hero-intro D-09 handshake, the extended canvas entry, extended scene spec assertions, and the documented 3D-active second audit (`lighthouserc.webgl.json`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-signature-moment-launch-hardening/04-CONTEXT.md
@.planning/phases/04-signature-moment-launch-hardening/04-RESEARCH.md
@.planning/phases/04-signature-moment-launch-hardening/04-PATTERNS.md
@.planning/phases/04-signature-moment-launch-hardening/04-UI-SPEC.md
@.planning/phases/04-signature-moment-launch-hardening/04-03-SUMMARY.md
@src/components/scene/constellation-canvas.tsx
@src/components/motion/hero-intro.tsx
@src/components/motion/career-spine.tsx
@src/components/motion/reveal.tsx
@src/app/globals.css
@src/lib/motion-tokens.ts
</context>

## Artifacts this phase produces (Plan 04)

New symbols/files/config this plan creates (exclude from source-drift acknowledgement):
- `src/components/scene/constellation-data.ts` — `buildConstellation(tier)` (or equivalent factory): node positions (Float32Array), edge index pairs, cluster metadata; per-tier counts; density mask
- `src/components/scene/constellation.tsx` — `Constellation` component: `useFrame` drift/pulse/pointer loop, BufferGeometry refs, entrance/exit state
- `src/components/scene/scene-bridge.ts` — `sceneBridge` module-scope singleton: `scrollProgress`, `introBeatAt`, `pointer {x,y,active}`, `paused`
- `src/lib/theme-color-resolver.ts` — `resolveSceneColors()` / `observeThemeColors(callback)`: token reads + `data-theme` MutationObserver + `prefers-color-scheme` listener
- `src/components/motion/hero-intro.tsx` — timeline `onComplete` handler writing `sceneBridge.introBeatAt` (D-09 handshake)
- `src/components/scene/constellation-canvas.tsx` — extended: renders `Constellation`, scroll-progress sources (ScrollTrigger + IntersectionObserver fallback), visibilitychange pause
- `lighthouserc.webgl.json` — second LHCI config for the `?webgl=force` 3D-active audit with its own documented script budget
- `evals/scene.spec.ts` — extended assertions (pulse/drift presence via canvas snapshot deltas or scene-exposed test hooks, exit pause)

<tasks>

<task type="auto">
  <name>Task 1: Constellation data, scene interior, and theme-token colors (D-01…D-04, D-08)</name>
  <files>src/components/scene/constellation-data.ts, src/components/scene/constellation.tsx, src/lib/theme-color-resolver.ts, src/components/scene/constellation-canvas.tsx</files>
  <read_first>
    - .planning/phases/04-signature-moment-launch-hardening/04-UI-SPEC.md (Constellation Visual Spec — the ENTIRE section is the binding contract: geometry table with per-tier node/edge counts and DPR clamps, composition/density-mask rule, temporal-behavior table with drift/pulse/pointer values, color-token mapping table with exact opacities; the Color section's exhaustive accent list)
    - .planning/phases/04-signature-moment-launch-hardening/04-RESEARCH.md (Code Example 4 — useFrame delta mutation; Pattern 3 — the bridge; Pattern 5 — theme color resolution; Anti-Patterns list — no setState per frame, no GSAP tweens on three objects, no uncapped DPR)
    - .planning/phases/04-signature-moment-launch-hardening/04-PATTERNS.md (constellation-canvas/constellation section — repo conventions that still apply: decision-referencing doc comments, "use client", import type; theme-color-resolver section — data-theme mechanics from theme-toggle.tsx lines 63-73; constellation-data.ts analog: src/lib/content.ts typed static data module)
    - src/components/scene/constellation-canvas.tsx (the 04-03 stub this task replaces — keep the Canvas config, context-loss listener, and tier prop contract intact)
    - src/app/globals.css (token values: --muted, --border, --accent, --foreground, --background in both :root and [data-theme] blocks — plain hex, resolvable via getComputedStyle)
    - src/components/theme-toggle.tsx (data-theme attribute mechanics: "system" removes the attribute, so prefers-color-scheme must also be observed)
  </read_first>
  <action>
    Create `src/components/scene/constellation-data.ts` (typed static data module, content.ts convention): a factory that, given the tier, produces node positions, edge index pairs, and cluster metadata for the D-02 hidden structure — an orchestrator hub cluster with satellite agent clusters (ELIA's multi-agent shape, UI-SPEC's recommended derivation; no labels, no legend, no i18n surface). Prescriptive counts from UI-SPEC: desktop 72 nodes / ~110 edges (sparse, avg degree ~3, never fully connected), mobile 36 / ~54; executor may tune ±20% on profiling evidence, never upward past the D-08 "atmospheric, not busy" judgment. Enforce the density mask AT PLACEMENT (not post-hoc): node density inside the central reading column (the H1/value-prop zone) at most 30% of the density in the hero's outer thirds.

    Create `src/lib/theme-color-resolver.ts` (BROWSER-ONLY header discipline like motion-tokens.ts): resolve the scene's four tokens (--muted, --border, --accent, --foreground) from `getComputedStyle(document.documentElement)` into THREE.Color-compatible hex strings; export an observer that re-resolves on `data-theme` attribute mutation (MutationObserver on documentElement) AND on `matchMedia("(prefers-color-scheme: dark)")` change (the "system" state removes the attribute — theme-toggle.tsx mechanics); callback updates material colors imperatively, no remount (RESEARCH Pattern 5). No hardcoded hex anywhere in scene code (UI-SPEC hard rule).

    Create `src/components/scene/constellation.tsx` (the scene interior, replacing the 04-03 stub's content): points for nodes, line segments for edges, with materials per the UI-SPEC color table — resting nodes muted @ 0.55 opacity, resting edges border @ 0.35, transparent materials, node dot size 2-3 device px depth-attenuated (2 on mobile), 1-device-px hairline edges. Temporal behavior in a single `useFrame((state, delta) => ...)` loop, all delta-based (refresh-rate independent), mutating BufferAttribute arrays with `needsUpdate` — NEVER React state per frame (RESEARCH Anti-Patterns): (a) drift — slow 3D oscillation around home positions, max screen-space velocity <= 8 px/s, randomized period 20-40s per node (D-04); (b) message pulses — one accent-colored dot traveling an edge over ~800ms with a trailing glow fading to 0, spawned every 2-5s randomized (irregular, never metronomic), at most 2 in flight (D-04, D-03); accent is transient event signal only — no permanently accent-colored node or edge (UI-SPEC accent rule 7). Early-return the whole loop when `sceneBridge.paused` is true.

    Update `src/components/scene/constellation-canvas.tsx` to render `<Constellation tier={tier} />` and delete the stub interior; keep the 04-03 Canvas config (dpr clamps, alpha, low-power, context-loss listener) byte-compatible. Decision-referencing doc comments on all new files (D-01…D-04, D-08, RESEARCH pitfall numbers) — the "streichbar" requirement makes these load-bearing (PATTERNS Shared Pattern 3).
  </action>
  <verify>
    <automated>pnpm build && pnpm lint && pnpm exec playwright test evals/scene.spec.ts --reporter=line</automated>
    <human-check>End-of-phase: on /de light AND dark theme, the constellation reads as quiet engineered structure (muted dots, hairlines) with occasional single orange pulses; the H1/value-prop zone is visibly sparser than the hero edges; H1 contrast is unimpaired in both themes.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm build` and `pnpm lint` exit 0; the existing scene spec stays green
    - `src/components/scene/constellation-data.ts` exports a tier-parameterized factory; node counts are 72/36 (±20% max, deviation justified in a comment); the density mask is implemented in placement code
    - `src/components/scene/constellation.tsx` contains exactly one `useFrame` loop; a grep across `src/components/scene/` and `src/lib/theme-color-resolver.ts` for 6-digit hex color literals returns only matches inside comments or none (all runtime colors flow from the resolver)
    - `src/lib/theme-color-resolver.ts` contains `MutationObserver` and `prefers-color-scheme` (grep each)
    - No `useState`/`setState` call inside the `useFrame` body (source assertion)
  </acceptance_criteria>
  <done>The living constellation renders per the UI-SPEC contract — identity-bound structure, drift, pulses, token-driven colors in both themes — inside the 04-03 gate.</done>
</task>

<task type="auto">
  <name>Task 2: Boot-beat entrance, scroll-linked exit, and pointer influence (D-05, D-06, D-09)</name>
  <files>src/components/scene/scene-bridge.ts, src/components/motion/hero-intro.tsx, src/components/scene/constellation-canvas.tsx, src/components/scene/constellation.tsx</files>
  <read_first>
    - .planning/phases/04-signature-moment-launch-hardening/04-UI-SPEC.md (Entrance & exit choreography table — boot-beat ~1200ms --motion-duration-chapter with --motion-ease-out appended to the existing timeline; late-load 800ms opacity ramp inside useFrame; exit recede ~10% scale + dissolve mapped to progress; touch scroll-progress fallback; Pointer influence row — 120px radius, 10px max displacement, muted→accent max 60% blend)
    - src/components/motion/hero-intro.tsx (lines 90-142: the gsap.context timeline — grid draw 0-400ms, H1 words 200-900ms, value-prop 500-1000ms; the invariants at lines 40-44: text opacity stays 1, reduced-motion/touch never runs the timeline; the `await document.fonts.ready` at line 96)
    - .planning/phases/04-signature-moment-launch-hardening/04-PATTERNS.md (hero-intro section — where the handshake lands: `sceneBridge.introBeatAt = performance.now()` in the timeline's onComplete; scroll-exit section — career-spine.tsx lines 81-105 as the ScrollTrigger analog with the cancelled flag, reveal.tsx as the IntersectionObserver touch fallback)
    - src/components/motion/career-spine.tsx (lines 81-105: the lazy ScrollTrigger.create pattern — onUpdate writes progress; cleanup kills the trigger)
    - src/components/motion/reveal.tsx (the IntersectionObserver gate — gates on reduced-motion ONLY, not pointer: the exact gate split the scene's touch fallback needs)
    - .planning/phases/04-signature-moment-launch-hardening/04-RESEARCH.md (Pattern 3 — the bridge shape; Pattern 4 — the two D-09 cases; Pattern 7 — frameloop lifecycle, pause-first recommendation; Pitfall 8 — ScrollTrigger only exists on pointer:fine, the exit must also work on capable phones)
    - src/lib/motion-tokens.ts (getMotionToken for --motion-duration-chapter / --motion-ease-out reads on the DOM side)
  </read_first>
  <action>
    Create `src/components/scene/scene-bridge.ts` — the module-scope mutable singleton per RESEARCH Pattern 3 (theme-toggle.tsx module-scope-state convention, PATTERNS): `scrollProgress: 0..1`, `introBeatAt: 0`, `pointer: {x, y, active}`, `paused: false`. This is the ONLY shared surface between GSAP-land and canvas-land; data flows one way (GSAP writes, useFrame reads) — single-engine discipline (Phase 3 D-08).

    Wire the D-09 handshake in `src/components/motion/hero-intro.tsx`: add `onComplete` to the existing `gsap.timeline({...})` options that sets `sceneBridge.introBeatAt = performance.now()`. Import the bridge statically — it is a few bytes of plain object, NOT a three/fiber import; verify it drags no heavy code into the eager bundle. Preserve every documented invariant: text opacity stays 1, reduced-motion/touch never constructs the timeline (in that regime introBeatAt simply stays 0 — and the gate is closed under reduced-motion anyway), `document.fonts.ready` timing untouched.

    Implement the two D-09 entrance cases in the scene: on mount, if the intro timeline is still running (introBeatAt === 0 AND the motion stack is active), the constellation assembles as the final boot beat — clusters wake sequentially (nodes fade/scale in, then edges draw), total ~1200ms using the --motion-duration-chapter reference and --motion-ease-out character, appended/synchronized so it reads as the timeline's final act; nodes may flash foreground @ 0.8 for <= 300ms before settling to resting muted (UI-SPEC boot-flash row). If the chunk arrives after the intro finished (introBeatAt > 0) OR the motion stack never ran (capable phone), run the graceful fallback instead: a simple opacity ramp from 0 to resting values over ~800ms inside useFrame — no second GSAP timeline, no replayed intro, no waiting state (D-09).

    Implement the D-05 scroll-linked exit with BOTH progress sources writing the same `sceneBridge.scrollProgress` (RESEARCH Pitfall 8): (a) when the motion stack is up (pointer:fine desktop), a lazy ScrollTrigger over the hero section (career-spine.tsx pattern: dynamic gsap/ScrollTrigger import behind the cancelled flag, `onUpdate: (self) => sceneBridge.scrollProgress = self.progress`, cleanup kills the trigger); (b) on capable phones (no ScrollTrigger), a passive scroll listener / IntersectionObserver computing hero-exit progress (reveal.tsx precedent). In useFrame, map progress to the exit: recede (translate -z / scale down ~10%) and dissolve (opacity → 0) over progress 0→1; at progress >= 1 set paused and flip the Canvas `frameloop` to "never" (pause-first per RESEARCH Pattern 7 — profile before ever switching to unmount; document the profiling observation in the SUMMARY). Also pause on `document.visibilitychange` hidden and resume on visible (UI-SPEC state contract). Scrolling back up resumes ("always") and reverses the recede.

    Implement D-06 pointer influence, pointer:fine ONLY (matchMedia check — a no-op on touch/keyboard; the mount itself is NOT pointer-gated, RESEARCH Pitfall 7): a document-level pointermove listener (the canvas layer is pointer-events-none and must stay so — WOW-04) writes unprojected coordinates into `sceneBridge.pointer`; in useFrame, nodes within a 120-screen-px radius get gentle attraction (max displacement 10px) and illumination lerping muted→accent capped at 60% blend at radius center, with the magnetic-button easing character (UI-SPEC pointer row). Add scene-spec assertions for the exit-pause: after scrolling past the hero in a `?webgl=force` run, the canvas element still exists but rendering is paused (assert via an exposed data attribute or the frameloop state reflected on the canvas wrapper — pick one deterministic hook and document it).
  </action>
  <verify>
    <automated>pnpm build && pnpm lint && pnpm exec playwright test evals/scene.spec.ts --reporter=line</automated>
    <human-check>End-of-phase: hard-reload /de on desktop — the constellation wakes as the final beat of the boot intro (no double intro); throttle network to Slow 3G and reload — it fades in quietly after the intro instead; scroll past the hero — it recedes/dissolves and GPU usage drops to idle (devtools performance monitor); moving the cursor near nodes attracts/illuminates them subtly.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/motion/hero-intro.tsx` sets `sceneBridge.introBeatAt` in the timeline's `onComplete` (grep: `introBeatAt`); the file's documented invariants (opacity-1 reveals, reduced-motion guard, fonts.ready) are textually intact
    - `src/components/scene/scene-bridge.ts` exports `sceneBridge` with the four fields; no scene file imports React state into the per-frame path (no setState inside useFrame)
    - Both scroll-progress sources exist and write `sceneBridge.scrollProgress` (grep in constellation-canvas.tsx/constellation.tsx: `ScrollTrigger.create` behind a dynamic import AND an IntersectionObserver/scroll-listener fallback)
    - The exit-pause spec assertion passes: post-hero scroll in a forced run reaches the paused state
    - Pointer influence code is guarded by a pointer:fine matchMedia check; the slot's `pointer-events-none` is unchanged (grep hero-scene-slot.tsx)
    - `pnpm build`, `pnpm lint`, and the scene spec all green
  </acceptance_criteria>
  <done>The constellation enters as the boot sequence's final act (or fades in late), recedes and pauses on scroll exit in both input regimes, and answers the cursor on pointer devices — the complete WOW-01 moment.</done>
</task>

<task type="auto">
  <name>Task 3: 3D-active performance audit — second budget, TBT check, CI neutrality (RESEARCH Open Question 1, Pitfalls 1+4)</name>
  <files>lighthouserc.webgl.json, evals/scene.spec.ts</files>
  <read_first>
    - .planning/phases/04-signature-moment-launch-hardening/04-RESEARCH.md (Pitfall 1 — the lazy chunk exceeds the entire 184,643-byte script budget by itself, so the 3D-active scenario needs its own documented budget via a second config; Open Question 1 recommendation (a)+(b); Pitfall 4 — TBT from parsing three.js on 4x throttle; Assumption A5 — LHCI assertions are global per config)
    - .planning/phases/04-signature-moment-launch-hardening/04-03-SUMMARY.md (the measured three/fiber chunk sizes from 04-03 Task 3 — the input to the second budget's math)
    - lighthouserc.json (the five baseline assertions — the template the second config derives from; its collect config shape)
    - .planning/phases/04-signature-moment-launch-hardening/04-02-PLAN.md (how the representative CI gate collects URLs — the second config must not disturb it)
  </read_first>
  <action>
    Create `lighthouserc.webgl.json` — a second, clearly-commented LHCI config auditing `/de?webgl=force` and `/en?webgl=force` on the local production build: same LCP 2500 / TBT 200 / CLS 0.1 / performance 0.9 error assertions as the baseline, but with `resource-summary:script:size` set to a DOCUMENTED 3D-active budget computed as: baseline budget (184,643) + the 04-03-measured three/fiber+scene chunk transfer size + 10% margin — write the computation as a comment-equivalent (JSON has no comments: put the derivation in a `_budget_derivation` string field) so the number is auditable, never a magic value. This implements RESEARCH Open Question 1 option (a); option (b) — production PSI/WebPageTest judgment with the carve-out documented — is 04-05's job.

    Run it: `pnpm build`, start the production server, `npx lhci autorun --config=lighthouserc.webgl.json`. LCP/CLS must hold (the canvas mounts after paint into a zero-layout slot); TBT must stay <= 200ms even with the chunk parse on throttled CPU (RESEARCH Pitfall 4 — if TBT breaches, the mount is landing too early: verify the load+idle scheduling before touching anything else). Record all five numbers for both URLs in the SUMMARY. Note the SwiftShader caveat: local machines render real GL; this audit is the lab check, production real-device verification is 04-05.

    Confirm CI neutrality: the default `lighthouserc.json` and the 04-02 representative gate are untouched by this plan (no ?webgl param in their URLs → gate closed → baseline budgets still apply). Add a package.json script `lhci:webgl` invoking the second config so the audit is one command, and reference it from the CI workflow only as a non-blocking/manual job if trivially wireable — the blocking gate stays the baseline one (the 3D-active lab audit is evidence, not a merge gate; production is the source of truth per D-11).
  </action>
  <verify>
    <automated>pnpm build && npx lhci autorun --config=lighthouserc.webgl.json</automated>
  </verify>
  <acceptance_criteria>
    - `lighthouserc.webgl.json` exists, collects `?webgl=force` URLs for both locales, keeps LCP 2500/TBT 200/CLS 0.1/perf 0.9 as errors, and contains a `_budget_derivation` field showing the script-size math from the measured chunk
    - The 3D-active audit passes all assertions locally; the five metric values for /de and /en are recorded in the SUMMARY
    - `lighthouserc.json` is byte-identical to its 04-02 state (the baseline gate is undisturbed)
    - `package.json` has an `lhci:webgl` script
  </acceptance_criteria>
  <done>The 3D-active scenario has its own honest, documented lab budget and passes it; the baseline CI gate is untouched; the numbers are recorded for 04-05's production verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer input → scene | Global pointermove coordinates are consumed by canvas code |
| GSAP-land → canvas-land | The scene-bridge singleton is the only crossing; one-way writes |
| CI budgets → second config | A second LHCI config could be abused to launder budget relaxation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04-01 | Tampering | lighthouserc.webgl.json vs baseline budgets | high | mitigate | The baseline lighthouserc.json is asserted byte-identical to its 04-02 state; the second config's only widened assertion is script:size with an auditable `_budget_derivation`; LCP/TBT/CLS/perf thresholds identical to baseline |
| T-04-04-02 | Denial of Service | per-frame work (drift/pulse/pointer) on weak GPUs | medium | mitigate | Gate excludes weak devices (04-03); delta-based single useFrame loop; paused early-return; frameloop "never" on exit/hidden; DPR clamps; counts capped per tier (UI-SPEC) |
| T-04-04-03 | Information Disclosure | pointer coordinates consumed by scene | low | accept | Coordinates never leave the client, never serialized, never sent anywhere (static site, zero runtime third-party calls per AGENTS.md) |
| T-04-04-04 | Elevation of Privilege | canvas intercepting clicks/focus over the fast path | medium | mitigate | Slot stays `pointer-events-none` + `aria-hidden`; pointer influence reads a document-level listener instead of canvas events; asserted in acceptance criteria (WOW-04) |
| T-04-04-SC | Tampering | npm installs | low | accept | No new packages in this plan (all deps landed in 04-03 with OK audit verdicts); nothing to slopcheck |
</threat_model>

<verification>
- `pnpm build`, `pnpm lint`, and `evals/scene.spec.ts` green (including exit-pause assertion).
- 3D-active lab audit (`lighthouserc.webgl.json`) passes on both locales; numbers recorded.
- Baseline `lighthouserc.json` unchanged; representative CI gate unaffected.
- End-of-phase human walkthrough items staged (boot-beat, late-load fade, exit pause, pointer influence, both themes).
</verification>

<success_criteria>
- WOW-01's experience is complete per the UI-SPEC contract: identity-bound constellation, atmospheric composition, boot-beat entrance, message pulses, pointer influence, scroll-linked exit with GPU zeroed after the hero.
- Single-engine discipline holds: GSAP owns DOM/choreography timing; the canvas owns its interior; the bridge is the only crossing, one-way.
- All scene code remains deletable as `src/components/scene/` + `src/lib/theme-color-resolver.ts`/`capability.ts` (D-12 removability).
- The 3D-active scenario is lab-audited under a documented budget without touching the baseline gate.
</success_criteria>

<output>
Create `.planning/phases/04-signature-moment-launch-hardening/04-04-SUMMARY.md` when done. Record: the 3D-active audit numbers (both locales, all five metrics), the pause-vs-unmount profiling observation (D-05 discretion), the exit-pause test hook chosen, and any ±20% geometry tuning applied with its justification.
</output>
