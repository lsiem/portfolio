---
phase: 01-bilingual-content-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-bilingual-content-foundation/extracted/
  - .gitignore
  - README.md
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - tsconfig.json
  - content-collections.ts
  - content/shared/types.ts
  - content/de/case-studies/vidama-mediathek.mdx
  - content/en/case-studies/vidama-mediathek.mdx
  - messages/de.json
  - messages/en.json
  - src/i18n/routing.ts
  - src/i18n/navigation.ts
  - src/i18n/request.ts
  - src/proxy.ts
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/app/[locale]/case-studies/[slug]/page.tsx
  - src/components/locale-switcher.tsx
  - src/lib/content.ts
  - src/lib/seo.ts
  - (deleted) vercel.json
  - (deleted) src/* (old Vite tree), api/, e2e/, public/ (old), index.html, vite.config.ts, vitest.config.ts, playwright.config.ts, tailwind.config.js, eslint.config.js, tsconfig.app.json, tsconfig.node.json, package-lock.json, .github/workflows/ci.yml, DEPLOYMENT.md, QUICK_REFERENCE.md, WARP.md
autonomous: true
requirements: [CONT-01, I18N-01]
must_haves:
  truths:
    - "Visiting / redirects (307) to /de — German is the default locale (D-09)"
    - "A visitor can read the Vidama-Mediathek case study in German at /de/case-studies/vidama-mediathek and switch to the same page in English via a real link"
    - "Every [locale] route is prerendered (SSG) at build time — no dynamic rendering"
    - "hreflang alternates for de, en, and x-default are present in the HTML head and Link response header"
    - "The old German raw content is preserved in a staging directory before the Vite tree is deleted (D-04)"
  artifacts:
    - path: "content-collections.ts"
      provides: "caseStudies collection with Zod frontmatter schema (D-06)"
      contains: "defineCollection"
    - path: "content/shared/types.ts"
      provides: "Shared Zod schemas + TS types for career, projects, skills, contact — the contract plans 03/04 implement against"
      exports: ["careerEntrySchema", "projectSchema", "skillDomainSchema", "contactInfoSchema", "CareerEntry", "Project", "SkillDomain", "ContactInfo"]
    - path: "src/i18n/routing.ts"
      provides: "defineRouting with locales de/en, defaultLocale de, localePrefix always, localeDetection false"
    - path: "src/proxy.ts"
      provides: "next-intl middleware (Next 16 filename)"
    - path: "src/app/[locale]/layout.tsx"
      provides: "generateStaticParams + hasLocale guard + setRequestLocale + NextIntlClientProvider"
    - path: "content/de/case-studies/vidama-mediathek.mdx"
      provides: "Thin real case study, German"
      min_lines: 20
    - path: "content/en/case-studies/vidama-mediathek.mdx"
      provides: "Thin real case study, English"
      min_lines: 20
    - path: ".planning/phases/01-bilingual-content-foundation/extracted/"
      provides: "Staged copy of old src/content/*.ts raw material (D-04)"
  key_links:
    - from: "src/app/[locale]/case-studies/[slug]/page.tsx"
      to: "content-collections.ts"
      via: "typed accessor src/lib/content.ts importing from content-collections generated output"
      pattern: "content-collections"
    - from: "src/proxy.ts"
      to: "src/i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware"
    - from: "next.config.ts"
      to: "content-collections.ts"
      via: "withContentCollections as outermost plugin"
      pattern: "withContentCollections"
---

# Plan 01-01: Walking Skeleton — Next.js 16 bilingual foundation with one real case study end-to-end

## Phase Goal

**As a** Besucher (Recruiter, Tech-Lead oder Reviewer), **I want to** sämtliche Portfolio-Inhalte vollständig auf Deutsch und Englisch unter `/de` und `/en` einer deployten, performance-überwachten Site lesen und die Sprache umschalten, **so that** ich Lasses Werdegang, Projekte und Kontaktmöglichkeiten in meiner Sprache beurteilen kann.

*(Derived verbatim from the ROADMAP Phase-1 goal and success criteria 1–2; roles named there: Besucher, Reviewer.)*

<objective>
Replace the discarded Vite SPA with a Next.js 16 walking skeleton: extract the old German raw material (D-04), delete the Vite tree (D-07), scaffold Next.js 16 with pnpm, wire route-based DE/EN i18n with hreflang (I18N-01, D-09), and stand up the typed content model (CONT-01, D-06) proven by ONE real case study rendering fully static at /de/case-studies/vidama-mediathek and /en/case-studies/vidama-mediathek with a working language switcher.

Purpose: This is the architectural backbone (see SKELETON.md) every later slice builds on. After this plan, a visitor can already read one real, bilingual case study locally.
Output: Runnable Next.js 16 app, i18n routing, Content Collections pipeline, shared content type contract, thin Vidama case study in both locales.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bilingual-content-foundation/01-CONTEXT.md
@.planning/phases/01-bilingual-content-foundation/01-RESEARCH.md
@.planning/phases/01-bilingual-content-foundation/01-PATTERNS.md
@.planning/phases/01-bilingual-content-foundation/SKELETON.md
</context>

## Artifacts this phase produces (plan 01 portion)

- Directory `.planning/phases/01-bilingual-content-foundation/extracted/` — staged copies of old `src/content/{about,experience,index,personal,projects,skills,types,ui,validate}.ts`
- Next.js 16 scaffold: `next.config.ts`, `tsconfig.json`, `src/app/`, Tailwind 4 config-in-CSS, ESLint 9 flat config, `pnpm-lock.yaml`
- `content-collections.ts` — exports default `defineConfig`; collection `caseStudies` (Zod frontmatter: `title`, `summary`, `role`, `period`, `stack`, `depth`, `order`; computed fields `locale`, `slug`, `mdx`, raw `content` retained)
- `content/shared/types.ts` — exports Zod schemas `careerEntrySchema`, `careerRoleSchema`, `projectSchema`, `skillDomainSchema`, `skillSchema`, `contactInfoSchema` and inferred types `CareerEntry`, `CareerRole`, `Project`, `SkillDomain`, `Skill`, `ContactInfo`
- `content/de/case-studies/vidama-mediathek.mdx`, `content/en/case-studies/vidama-mediathek.mdx`
- `messages/de.json`, `messages/en.json` (seed keys: `nav.*`, `common.languageSwitcher`, `home.*`)
- `src/i18n/routing.ts` (exports `routing`), `src/i18n/navigation.ts` (exports `Link`, `usePathname`, `useRouter`, `getPathname`), `src/i18n/request.ts` (default export `getRequestConfig`)
- `src/proxy.ts` (default export next-intl middleware + `config.matcher`)
- `src/app/[locale]/layout.tsx` (exports `generateStaticParams`, default `LocaleLayout`), `src/app/[locale]/page.tsx`, `src/app/[locale]/case-studies/[slug]/page.tsx`
- `src/components/locale-switcher.tsx` (exports `LocaleSwitcher`)
- `src/lib/content.ts` (exports `getCaseStudies(locale)`, `getCaseStudy(locale, slug)`)
- `src/lib/seo.ts` (exports `localeAlternates(pathname)`, `siteMetadataBase`)

<tasks>

<task type="auto">
  <name>Task 1: Extract raw material, tear down the Vite tree, scaffold Next.js 16 (D-04, D-07)</name>
  <files>.planning/phases/01-bilingual-content-foundation/extracted/, .gitignore, README.md, package.json, pnpm-lock.yaml, next.config.ts, tsconfig.json, src/app/, plus deletions listed in frontmatter</files>
  <read_first>
    - .planning/phases/01-bilingual-content-foundation/01-PATTERNS.md (extraction urgency section + anti-patterns table)
    - .planning/phases/01-bilingual-content-foundation/01-RESEARCH.md (Pattern 1 scaffold flags, Runtime State Inventory)
    - .gitignore (current — lines 83–84 carry the internal-reference ignore rule that MUST survive)
    - vercel.json (confirm the SPA rewrite you are deleting)
    - src/content/ directory listing (the 10 files to stage)
  </read_first>
  <action>
    Sequence is load-bearing: extract → delete → scaffold → merge → pin → install.

    1. EXTRACT (D-04): copy all of src/content/*.ts (about, content.test, experience, index, personal, projects, skills, types, ui, validate) to .planning/phases/01-bilingual-content-foundation/extracted/. Git history also preserves them (git show HEAD:src/content/experience.ts), but the staging copy is the canonical read source for plans 03/04.
    2. DELETE the old Vite rewrite (D-07): src/, api/, e2e/, public/, index.html, vite.config.ts, vitest.config.ts, playwright.config.ts, tailwind.config.js, eslint.config.js, tsconfig.json, tsconfig.app.json, tsconfig.node.json, package.json, package-lock.json, node_modules/, vercel.json (its SPA rewrite to /index.html would 404 every Next route — RESEARCH Pitfall 4), .github/workflows/ci.yml (npm+Vite+vitest — plan 01-02 writes the replacement), and the stale build docs DEPLOYMENT.md, QUICK_REFERENCE.md, WARP.md. KEEP: .git/, .planning/ (including the gitignored reference/ dir with the ELIA PDF/txt), .gitignore, .claude/, .cursor/, .serena/, LICENSE, CODE_OF_CONDUCT.md, CONTRIBUTING.md. Replace README.md with a 5-line stub (project name, "rebuild in progress", pnpm dev/build commands) — full rewrite happens in plan 01-04.
    3. SCAFFOLD: create-next-app refuses non-empty directories, so scaffold into the session scratchpad temp dir and move files in: run pnpm create next-app@16.2.10 with flags --ts --tailwind --app --src-dir --eslint --import-alias "@/*" --use-pnpm --yes (verified flag set from RESEARCH Pattern 1; no --turbopack flag exists — Turbopack is the default). Copy the scaffold's contents (excluding its .git) into the repo root.
    4. MERGE .gitignore: start from the scaffold's .gitignore, then append the preserved repo-specific block — the comment line about internal reference material and the rule .planning/phases/*/reference/ (verbatim from the old file, lines 83–84) — plus a new line for .content-collections (used in Task 3).
    5. PIN React per STACK.md: in package.json set react and react-dom to ~19.2.0 (tilde, not caret — R3F 9 peer range is >=19 <19.3). If the scaffold's TypeScript is 6.0.x and any tool in the chain rejects it, fall back to 5.9.x per STACK.md (no code changes).
    6. INSTALL the phase dependency set NOW so later plans never touch package.json deps (supply-chain rule: ONLY packages from the RESEARCH.md Package Legitimacy Audit — all approved there, [SUS] entries are documented false positives, no human checkpoint required, cite the audit): pnpm add next-intl @vercel/analytics @vercel/speed-insights and pnpm add -D @content-collections/core @content-collections/mdx @content-collections/next zod @lhci/cli. Commit pnpm-lock.yaml.
    7. Run pnpm build to confirm the pristine scaffold builds.
  </action>
  <verify>
    <automated>ls .planning/phases/01-bilingual-content-foundation/extracted/ | wc -l && test ! -f vercel.json && test ! -f vite.config.ts && test ! -f .github/workflows/ci.yml && grep -q '.planning/phases/\*/reference/' .gitignore && grep -q '"react": "~19.2.0"' package.json && git check-ignore .planning/phases/01-bilingual-content-foundation/reference/ELIA-produktvision.pdf && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-bilingual-content-foundation/extracted/` contains all 10 old `src/content/*.ts` files
    - `test ! -f vercel.json`, `test ! -f vite.config.ts`, `test ! -f index.html`, `test ! -f package-lock.json` all succeed
    - `.gitignore` contains the line `.planning/phases/*/reference/` and a line for `.content-collections`
    - `git check-ignore .planning/phases/01-bilingual-content-foundation/reference/ELIA-produktvision.pdf` exits 0 (reference material still ignored)
    - `package.json` contains `"react": "~19.2.0"` and `"react-dom": "~19.2.0"`, plus deps `next-intl`, `@vercel/analytics`, `@vercel/speed-insights` and devDeps `@content-collections/core`, `@content-collections/mdx`, `@content-collections/next`, `zod`, `@lhci/cli`
    - `pnpm-lock.yaml` exists and is staged for commit; `pnpm build` exits 0
    - `git status --porcelain` shows NO file under `.planning/phases/01-bilingual-content-foundation/reference/`
  </acceptance_criteria>
  <done>Old Vite tree gone, raw material staged, Next.js 16 scaffold builds with pinned React and the full audited dependency set installed.</done>
</task>

<task type="auto">
  <name>Task 2: Route-based DE/EN i18n with hreflang and language switcher (I18N-01, D-09)</name>
  <files>src/i18n/routing.ts, src/i18n/navigation.ts, src/i18n/request.ts, src/proxy.ts, messages/de.json, messages/en.json, src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx, src/components/locale-switcher.tsx, src/lib/seo.ts</files>
  <read_first>
    - .planning/phases/01-bilingual-content-foundation/01-RESEARCH.md (Pattern 2 — verbatim, docs-cited code for routing.ts, proxy.ts, [locale]/layout.tsx; Code Examples section for request.ts; Pitfalls 2 and 3)
    - .planning/phases/01-bilingual-content-foundation/extracted/ui.ts (seed material for messages/de.json nav keys)
    - src/app/layout.tsx and src/app/page.tsx (scaffold files you will delete/replace)
  </read_first>
  <action>
    Implement RESEARCH.md Pattern 2 exactly — the code there is cited from next-intl docs and is the source of truth:

    - src/i18n/routing.ts: defineRouting with locales ['de','en'], defaultLocale 'de', localePrefix 'always', localeDetection false (D-09: root always redirects to /de, no accept-language negotiation).
    - src/i18n/navigation.ts: createNavigation(routing) exporting Link, usePathname, useRouter, getPathname.
    - src/i18n/request.ts: getRequestConfig resolving requestLocale via hasLocale, loading messages from ../../messages/{locale}.json (verbatim from RESEARCH Code Examples).
    - src/proxy.ts (Next 16 renamed middleware.ts to proxy.ts — use the new name): default export createMiddleware(routing), config.matcher exactly as in Pattern 2. The middleware auto-emits the hreflang Link response header including x-default.
    - next.config.ts: wrap with createNextIntlPlugin() from next-intl/plugin (Task 3 adds withContentCollections as the OUTERMOST wrapper).
    - Delete the scaffold's src/app/layout.tsx and src/app/page.tsx. src/app/[locale]/layout.tsx becomes the root layout: exports generateStaticParams mapping routing.locales, awaits params, guards with hasLocale + notFound, calls setRequestLocale(locale) (REQUIRED in EVERY layout and page under [locale] — a single miss silently flips routes to dynamic rendering, RESEARCH Pitfall 2), renders html lang={locale} with NextIntlClientProvider, a minimal header containing LocaleSwitcher, and children.
    - src/lib/seo.ts: export siteMetadataBase — a URL built from process.env.VERCEL_PROJECT_PRODUCTION_URL (prefix https://) falling back to http://localhost:3000 — and localeAlternates(pathname) returning a Metadata alternates object whose languages map contains 'de', 'en' (self-referencing per-locale URLs built with getPathname from src/i18n/navigation.ts) and 'x-default' pointing at the German default variant (D-09). RESEARCH Pitfall 3: missing self-reference or x-default invalidates the whole hreflang set.
    - src/app/[locale]/page.tsx: minimal semantic home page — h1 with name "Lasse Siemoneit", a role line, nav links from messages — using getTranslations/setRequestLocale; generateMetadata uses siteMetadataBase + localeAlternates('/').
    - src/components/locale-switcher.tsx: renders Link from src/i18n/navigation.ts with href={pathname} and locale={otherLocale} — a REAL anchor causing full navigation (never a client-side string swap — RESEARCH anti-pattern, PITFALLS.md Pitfall 7). Label from messages key common.languageSwitcher.
    - messages/de.json + messages/en.json: seed with nav.* (from extracted/ui.ts: Start/Home, Projekte/Projects, Über mich/About, Kontakt/Contact), common.languageSwitcher, home.role. Plan 01-03 completes the full message set.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tee /tmp/next-build.log && grep -E "[●○].*/\[locale\]" /tmp/next-build.log && (pnpm start & SERVER=$!; sleep 4; curl -sI http://localhost:3000/ | grep -i "location: */de"; curl -s http://localhost:3000/de | grep -c 'hreflang="x-default"'; curl -sI http://localhost:3000/de | grep -i '^link:.*x-default'; curl -s http://localhost:3000/en | grep -c 'lang="en"'; kill $SERVER)</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` output marks the `/[locale]` route as prerendered/static (filled or hollow circle marker), NOT as dynamic
    - `curl -sI http://localhost:3000/` returns a 307 redirect with `location: /de`
    - `curl -s http://localhost:3000/de` HTML contains `lang="de"`, at least one `hreflang="de"`, one `hreflang="en"`, and one `hreflang="x-default"` link tag with ABSOLUTE URLs
    - `curl -sI http://localhost:3000/de` includes a `link:` response header containing `x-default`
    - `/en` serves the English page (`lang="en"`) and its switcher anchor href points to `/de` (and vice versa)
    - File `src/proxy.ts` exists; file `src/middleware.ts` does NOT exist
    - Every file under `src/app/[locale]/` calls `setRequestLocale`
  </acceptance_criteria>
  <done>Both locales render statically with complete hreflang sets, / redirects to /de, and the language switcher navigates between locale variants of the same page.</done>
</task>

<task type="auto">
  <name>Task 3: Typed content model + thin Vidama case study end-to-end (CONT-01, D-06, D-01)</name>
  <files>content-collections.ts, next.config.ts, tsconfig.json, .gitignore, content/shared/types.ts, content/de/case-studies/vidama-mediathek.mdx, content/en/case-studies/vidama-mediathek.mdx, src/lib/content.ts, src/app/[locale]/case-studies/[slug]/page.tsx, src/app/[locale]/page.tsx</files>
  <read_first>
    - .planning/phases/01-bilingual-content-foundation/01-RESEARCH.md (Pattern 3 — verbatim collection definition, next.config wrapping order, tsconfig paths; Pitfall 6 on presentation-free schemas)
    - .planning/phases/01-bilingual-content-foundation/01-PATTERNS.md (type-shape mapping old→new, satisfies idiom, case-study frontmatter shape)
    - .planning/phases/01-bilingual-content-foundation/extracted/projects.ts (ewe-mediathek entry — raw material for the thin case study)
    - .planning/phases/01-bilingual-content-foundation/extracted/experience.ts (Vidama CTO entry — role/period facts)
    - .planning/phases/01-bilingual-content-foundation/extracted/types.ts (old shapes — refine per PATTERNS mapping, drop presentation fields like color/logoPath)
  </read_first>
  <action>
    Wire Content Collections per RESEARCH Pattern 3 and prove it with one real bilingual case study:

    - content-collections.ts: defineCollection name 'caseStudies', directory 'content', include '*/case-studies/*.mdx' (matches de/ and en/), Zod schema with fields title (string), summary (string), role (string), period (string), stack (string array), depth (enum 'flagship' | 'deep' — D-01 weighting), order (number). Transform: compileMDX from @content-collections/mdx, derive locale as first path segment and slug from fileName, and KEEP the raw doc.content field in the output — Phase 2's CV-PDF and v2's AI chat need prose text, not just compiled MDX (CONT-01 downstream seam). Export default defineConfig.
    - next.config.ts: withContentCollections(withNextIntl(nextConfig)) — withContentCollections MUST be outermost (RESEARCH Pattern 3). The adapter is Turbopack-safe by construction (verified from source, not a webpack plugin).
    - tsconfig.json: add paths entry mapping "content-collections" to ["./.content-collections/generated"]. Confirm .content-collections is in .gitignore (added in Task 1).
    - content/shared/types.ts: the INTERFACE CONTRACT for plans 01-03/01-04 — export Zod schemas and z.infer types, presentation-free (RESEARCH Pitfall 6):
      careerRoleSchema { title: string; from: string nullable (YYYY-MM); to: string nullable (null = present); description: string } — nullable from supports the ITSC mid-progression phase whose exact start date is unconfirmed;
      careerEntrySchema { slug; org; orgUrl optional; location optional; from (YYYY-MM); to (YYYY-MM or null = present); roles: careerRoleSchema array (min 1) — models the D-02 ITSC arc Systemadministrator → Software Engineering → Product Owner; intro optional; techStack: string array; highlights: string array optional };
      projectSchema { slug; title; summary; tags: string array (min 1); url optional; period optional; depth: enum 'flagship' | 'deep' | 'card' (D-01); order: number };
      skillSchema { name; years optional number; projectSlugs optional string array; note optional } and skillDomainSchema { domain; skills: skillSchema array } — NO percent/level fields (CONT-05 forbids skill bars);
      contactInfoSchema { name; role; email; github; linkedin } — deliberately NO phone/birthdate/address fields (RESEARCH Security Domain V14: role-relevant facts only).
    - content/{de,en}/case-studies/vidama-mediathek.mdx: THIN but real (D-01: Vidama = deep case study; full depth arrives in plan 01-04). Frontmatter: title, summary, role (CTO / sole developer at Vidama GmbH), period (12/2021 – 01/2024), stack from the extracted ewe-mediathek tags (Java, Spring Boot, Vaadin, PostgreSQL, Docker, CI/CD), depth 'deep', order 2. Body: 3–4 short German paragraphs on the Mediathek development (Vertriebspartner-Mediathek: campaign film management, role model for admins/partners, template-based content creation) sourced ONLY from extracted/projects.ts + extracted/experience.ts. English version is Claude's adaptation of the final German text (D-05), written in the SAME commit (RESEARCH Pitfall 7).
    - src/lib/content.ts: export getCaseStudies(locale) returning the collection filtered by locale and sorted by order, and getCaseStudy(locale, slug). Import from 'content-collections' (the tsconfig path).
    - src/app/[locale]/case-studies/[slug]/page.tsx: generateStaticParams over routing.locales × case-study slugs; await params; setRequestLocale; notFound for unknown slugs; render frontmatter title/role/period/stack as semantic HTML plus MDXContent (from @content-collections/mdx/react) with the compiled code; generateMetadata with localeAlternates('/case-studies/' + slug).
    - src/app/[locale]/page.tsx: add a case-studies section listing getCaseStudies(locale) as Links to their detail pages.
    - Styling: none beyond scaffold defaults — presentation is Phase 2 (deferred idea in CONTEXT.md). Semantic HTML only.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tee /tmp/next-build.log && grep -E "case-studies" /tmp/next-build.log && (pnpm start & SERVER=$!; sleep 4; curl -s http://localhost:3000/de/case-studies/vidama-mediathek | grep -c "Mediathek"; curl -s http://localhost:3000/en/case-studies/vidama-mediathek | grep -ci "media"; curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/de/case-studies/does-not-exist; kill $SERVER)</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 and the content build validates frontmatter via Zod (introduce a deliberate temporary frontmatter typo in a scratch copy is NOT required — Zod validation is proven by the build passing with the schema in place)
    - `/de/case-studies/vidama-mediathek` returns 200 with German prose; `/en/case-studies/vidama-mediathek` returns 200 with English prose; both are listed in the build output as prerendered routes
    - An unknown slug under `/de/case-studies/` returns 404
    - `content/shared/types.ts` exports all six schemas and six inferred types named exactly as listed in the Artifacts section; `contactInfoSchema` has no phone/birthdate/address field; `skillSchema` has no percent/level field; `projectSchema.depth` accepts exactly 'flagship', 'deep', 'card'
    - The caseStudies transform output retains the raw MDX text (a `content` field exists on generated documents)
    - `.content-collections/` is not tracked by git (`git check-ignore .content-collections` exits 0 after first build)
    - Home page at `/de` and `/en` lists the case study with a working link
  </acceptance_criteria>
  <done>The full content pipeline works end-to-end: MDX + Zod frontmatter → generated typed output → SSG detail pages in both locales, with the shared type contract in place for plans 01-03 and 01-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GitHub repo | Everything committed is world-readable — confidential internal material must never cross this boundary (D-03) |
| npm registry → local install | Third-party code enters the build |
| Content files → build | Repo-owned input validated at build time (no runtime user input exists) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .gitignore merge during scaffold | high | mitigate | Task 1 step 4 preserves the `.planning/phases/*/reference/` ignore rule verbatim; acceptance gate `git check-ignore` on the ELIA PDF + `git status --porcelain` shows no reference files |
| T-01-02 | Information Disclosure | vidama-mediathek.mdx content | medium | mitigate | Task 3 sources content ONLY from extracted public material (old repo, already public); no ELIA/reference material touched in this plan |
| T-01-03 | Tampering | Zod content validation (ASVS V5) | low | mitigate | All frontmatter validated at content-build time; build fails on schema violation; no runtime input surface |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Installs restricted to the RESEARCH.md Package Legitimacy Audit set (all Approved; [SUS] entries documented as false positives with per-package rationale — audit states no human checkpoint required); pnpm strict resolution; pnpm-lock.yaml committed |
</threat_model>

<verification>
- `pnpm build` fully static: `/[locale]`, `/[locale]/case-studies/[slug]` prerendered for de and en
- `/` → 307 `/de`; hreflang de/en/x-default in HTML head (absolute URLs) and Link header
- One real case study readable in both languages with working switcher
- No reference-directory file tracked by git; React pinned `~19.2.0`
</verification>

<success_criteria>
- Walking skeleton proven: scaffold → routing → content read → rendered bilingual page, all locally verifiable with `pnpm build && pnpm start`
- Interface contract (`content/shared/types.ts`, `content-collections.ts` schema, `src/lib/content.ts` accessors) ready for plans 01-03/01-04 to implement against without renegotiation
</success_criteria>

<output>
Create `.planning/phases/01-bilingual-content-foundation/01-01-SUMMARY.md` when done. Record: scaffold versions actually installed (next, react, typescript — note if TS fallback 5.9 was needed per RESEARCH A4), any deviation from the verified create-next-app flag set, and confirmation that the reference/ dir survived untracked.
</output>
