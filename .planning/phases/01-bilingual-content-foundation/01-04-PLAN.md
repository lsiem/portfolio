---
phase: 01-bilingual-content-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - content/de/case-studies/elia.mdx
  - content/en/case-studies/elia.mdx
  - content/de/case-studies/vidama-mediathek.mdx
  - content/en/case-studies/vidama-mediathek.mdx
  - content/de/pages/about.mdx
  - content/en/pages/about.mdx
  - content/de/pages/impressum.mdx
  - content/en/pages/impressum.mdx
  - content/de/pages/datenschutz.mdx
  - content/en/pages/datenschutz.mdx
  - content-collections.ts
  - src/lib/content.ts
  - src/app/[locale]/[slug]/page.tsx
  - src/app/[locale]/page.tsx
  - messages/de.json
  - messages/en.json
  - README.md
  - .planning/phases/01-bilingual-content-foundation/reference/blocklist.txt (LOCAL ONLY — gitignored, never committed)
autonomous: true
requirements: [CONT-01, I18N-02]
must_haves:
  truths:
    - "A reviewer can read the ELIA flagship case study fully in German AND English — Problem, Architektur-Entscheidungen, Tradeoffs, Stand — told from the Product-Owner perspective (D-01, D-02)"
    - "The ELIA case study and every other committed file contain ONLY abstracted vocabulary — no internal system names, roadmap dates, or model names; the automated blocklist scan passes (D-03)"
    - "A reviewer can read the Vidama-Mediathek case study as a full deep case study in both languages (D-01)"
    - "About, Impressum, and Datenschutzerklärung exist in both locales and render at their own routes"
    - "All content renders on the Vercel preview URL in both locales — roadmap success criteria 1 and 2 verifiable end-to-end"
    - "lsiem.de still serves the old site (D-08)"
  artifacts:
    - path: "content/de/case-studies/elia.mdx"
      provides: "Flagship case study, German, PO perspective"
      min_lines: 60
    - path: "content/en/case-studies/elia.mdx"
      provides: "Flagship case study, English adaptation"
      min_lines: 60
    - path: "content/de/case-studies/vidama-mediathek.mdx"
      provides: "Deep case study (Mediathek-Entwicklung), German"
      min_lines: 45
    - path: "content/de/pages/about.mdx"
      provides: "Über-mich prose with D-02 arc"
      min_lines: 15
    - path: "content/de/pages/datenschutz.mdx"
      provides: "Privacy statement covering Vercel hosting + cookieless analytics"
    - path: "src/app/[locale]/[slug]/page.tsx"
      provides: "SSG route for prose pages (about, impressum, datenschutz)"
  key_links:
    - from: "src/app/[locale]/[slug]/page.tsx"
      to: "content-collections.ts"
      via: "pages collection accessor in src/lib/content.ts"
      pattern: "getPage"
    - from: "scripts/check-content-parity.ts"
      to: ".planning/phases/01-bilingual-content-foundation/reference/blocklist.txt"
      via: "local-only confidentiality scan (skips in CI)"
      pattern: "blocklist"
---

# Plan 01-04: Case studies, about & legal prose — full bilingual content reviewable on the preview URL

## Phase Goal

**As a** Besucher (Recruiter, Tech-Lead oder Reviewer), **I want to** sämtliche Portfolio-Inhalte vollständig auf Deutsch und Englisch unter `/de` und `/en` einer deployten, performance-überwachten Site lesen und die Sprache umschalten, **so that** ich Lasses Werdegang, Projekte und Kontaktmöglichkeiten in meiner Sprache beurteilen kann.

<objective>
Write the prose half of the content model: the ELIA flagship case study from the Product-Owner perspective (D-01, D-02) using ONLY abstracted vocabulary and gated by a local confidentiality blocklist (D-03), the deep Vidama-Mediathek case study, the Über-mich page, and the Impressum/Datenschutz documents (RESEARCH Open Question 2 — cheap now, needed for the Phase-2 launch) — all in German and English (D-05, I18N-02) — then deploy a fresh preview and verify every roadmap success criterion end-to-end.

Purpose: Completes roadmap success criterion 1 — every case study readable in both languages in the typed content model — and proves criterion 2 on the deployed URL.
Output: 10 bilingual MDX documents, pages collection + route, local blocklist gate active, fresh verified preview deployment, rewritten README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-bilingual-content-foundation/01-CONTEXT.md
@.planning/phases/01-bilingual-content-foundation/01-RESEARCH.md
@.planning/phases/01-bilingual-content-foundation/01-PATTERNS.md
@.planning/phases/01-bilingual-content-foundation/01-01-SUMMARY.md
@.planning/phases/01-bilingual-content-foundation/01-02-SUMMARY.md
@.planning/phases/01-bilingual-content-foundation/01-03-SUMMARY.md
</context>

## Artifacts this phase produces (plan 04 portion)

- `content/{de,en}/case-studies/elia.mdx` — flagship case study (frontmatter depth 'flagship', order 1)
- `content/{de,en}/case-studies/vidama-mediathek.mdx` — expanded to full deep case study
- `content/{de,en}/pages/about.mdx`, `impressum.mdx`, `datenschutz.mdx`
- `content-collections.ts` — new collection `pages` (name 'pages', include '*/pages/*.mdx', Zod schema: `title`, `description` optional, `order` optional; transform derives `locale` + `slug`, keeps raw `content`)
- `src/lib/content.ts` — new exports `getPages(locale)`, `getPage(locale, slug)`
- `src/app/[locale]/[slug]/page.tsx` — SSG prose-page route (generateStaticParams over locales × page slugs, notFound for unknown slugs, hreflang alternates)
- `messages/{de,en}.json` — added `footer.legal` keys (about/impressum/datenschutz labels)
- `README.md` — full rewrite (stack, content model, commands)
- LOCAL ONLY (gitignored, never committed): `.planning/phases/01-bilingual-content-foundation/reference/blocklist.txt`

<tasks>

<task type="auto">
  <name>Task 1: ELIA flagship case study (DE + EN) + local confidentiality blocklist (D-01, D-02, D-03, D-05)</name>
  <files>content/de/case-studies/elia.mdx, content/en/case-studies/elia.mdx, .planning/phases/01-bilingual-content-foundation/reference/blocklist.txt</files>
  <read_first>
    - .planning/phases/01-bilingual-content-foundation/reference/ELIA-produktvision.txt (LOCAL, gitignored — the ONLY source for facts; its contents must never appear verbatim in committed files, commit messages, or the SUMMARY)
    - .planning/phases/01-bilingual-content-foundation/01-CONTEXT.md (D-02 narrative arc, D-03 abstraction whitelist, Specific Ideas section — the three tellable architecture moments)
    - content/de/case-studies/vidama-mediathek.mdx (frontmatter shape + voice established in plan 01-01)
    - scripts/check-content-parity.ts (how the blocklist file is discovered and parsed)
  </read_first>
  <action>
    FIRST create the blocklist (D-03 safety net BEFORE authoring): read the local reference text and write .planning/phases/01-bilingual-content-foundation/reference/blocklist.txt with one term per line — every internal system/product name, every concrete model name, every internal codename, and distinctive roadmap-date strings found there. Lines starting with the hash character are comments. This file lives inside the gitignored reference/ directory and MUST NEVER be committed — verify with git check-ignore before proceeding.

    THEN author content/de/case-studies/elia.mdx. Frontmatter: title and summary (abstracted), role Product Owner, period (seit 04/2026 — role start only, NO internal roadmap dates), stack limited to whitelist-safe technology categories, depth 'flagship', order 1. Body structure per CONT-04/D-02, told from the Product-Owner perspective with the OpenShift/engineering background as the entry point (the career arc IS part of the story — CONTEXT Specific Ideas):
    - Problem/Kontext: internal AI assistant for an IT service provider serving public health insurers; why it exists (abstract, no internal names)
    - Architektur-Entscheidungen: the three tellable, explicitly permitted moments — (1) replacing the classic RAG path with MCP-based tool-calling (live data instead of a vector index, no permission duplication), (2) feature-flagged orchestrator migration to a current agent framework running in parallel operation, (3) EU data residency / DSGVO as a deliberate platform decision; plus Multi-Agenten-Architektur and Teams-Integration; refer to connected systems only by category (ITSM-System, Wiki)
    - Tradeoffs: honest engineering tradeoffs of those decisions, still abstract
    - Ergebnis/Stand: current state and the PO role's scope — numbers only if they are safe and non-internal; otherwise qualitative
    Then write content/en/case-studies/elia.mdx as Claude's English adaptation of the final German text (D-05), same commit (Pitfall 7).

    HARD RULE (public repo): no internal system names, no roadmap dates, no model names — in the MDX, the frontmatter, the commit message, or the SUMMARY. When in doubt, use the category term.

    Gate before committing: run pnpm check:content — the blocklist scan now runs for real over ALL content (including plan 01-03's projects.ts ELIA entry, retroactively) and must exit 0. If it flags a term, rewrite with the category word; never edit the blocklist to make content pass.
  </action>
  <verify>
    <automated>git check-ignore .planning/phases/01-bilingual-content-foundation/reference/blocklist.txt && pnpm check:content && pnpm build && (pnpm start & SERVER=$!; sleep 4; curl -s http://localhost:3000/de/case-studies/elia | grep -ci "product owner"; curl -s http://localhost:3000/en/case-studies/elia | grep -ci "tool-calling\|tool calling"; kill $SERVER) && test -z "$(git status --porcelain .planning/phases/01-bilingual-content-foundation/reference/)"</automated>
    <human-check>End-of-phase: read both ELIA case-study versions in full and confirm (a) nothing identifies internal systems, roadmap timing, or models beyond the D-03 whitelist, (b) the PO-perspective narrative and career arc read as intended, (c) the depth is flagship-worthy relative to Vidama.</human-check>
  </verify>
  <acceptance_criteria>
    - `git check-ignore` exits 0 for blocklist.txt; `git status --porcelain` shows nothing under reference/ (blocklist and reference material untracked)
    - blocklist.txt exists locally and contains at least the internal names present in the reference document (executor judgment; count > 0 non-comment lines)
    - `pnpm check:content` exits 0 with the blocklist ACTIVE (output no longer shows the skip notice)
    - Both elia.mdx files have frontmatter depth 'flagship' and order 1; both bodies contain the four section structure (Problem/Kontext, Architektur-Entscheidungen, Tradeoffs, Ergebnis/Stand — English equivalents in the EN file)
    - `/de/case-studies/elia` and `/en/case-studies/elia` return 200 and render the full prose
    - The commit message for this task contains no term from blocklist.txt
  </acceptance_criteria>
  <done>The flagship case study is readable in both languages, provably free of confidential terms, with the blocklist gate active for all future content edits.</done>
</task>

<task type="auto">
  <name>Task 2: Vidama deep case study + about + Impressum/Datenschutz, DE and EN (D-01, D-04, D-05)</name>
  <files>content/de/case-studies/vidama-mediathek.mdx, content/en/case-studies/vidama-mediathek.mdx, content/de/pages/about.mdx, content/en/pages/about.mdx, content/de/pages/impressum.mdx, content/en/pages/impressum.mdx, content/de/pages/datenschutz.mdx, content/en/pages/datenschutz.mdx, content-collections.ts, src/lib/content.ts, src/app/[locale]/[slug]/page.tsx, src/app/[locale]/page.tsx, messages/de.json, messages/en.json</files>
  <read_first>
    - content/de/case-studies/vidama-mediathek.mdx (the thin version to expand)
    - .planning/phases/01-bilingual-content-foundation/extracted/projects.ts (ewe-mediathek + disy-one entries — raw material for the deep Vidama story)
    - .planning/phases/01-bilingual-content-foundation/extracted/experience.ts (Vidama CTO entry) and extracted/about.ts (bio/stats seed — stats are stale)
    - content-collections.ts and src/app/[locale]/case-studies/[slug]/page.tsx (collection + route patterns to mirror for pages)
    - .planning/phases/01-bilingual-content-foundation/01-RESEARCH.md (Pattern 5 analytics facts for the Datenschutz text; Open Question 2 rationale)
  </read_first>
  <action>
    Expand content/{de,en}/case-studies/vidama-mediathek.mdx into a full deep case study focused on the Mediathek-Entwicklung (user-specified in CONTEXT Specific Ideas): Problem (sales partners needed centrally managed marketing media with regional control) → Architektur-Entscheidungen (enterprise web app: Java/Spring Boot/Vaadin/PostgreSQL/Docker; role model for admins, partners, delegates; template-based content creation; shop integration) → Tradeoffs (sole developer as CTO: pragmatic choices, operational simplicity vs. feature breadth) → Ergebnis (delivered platform in production use; numbers where the extracted material supports them). Keep frontmatter depth 'deep', order 2.

    content/{de,en}/pages/about.mdx: rewrite the bio around the D-02 arc — self-taught engineer, consultant start at 16, full-stack/DevOps years, sysadmin (OpenShift) → software engineering → Product Owner of an internal AI assistant. Refine the extracted headline/tagline; drop or update the stale stats (Claude discretion — prefer prose over stat blocks; presentation is Phase 2).

    content/{de,en}/pages/impressum.mdx and datenschutz.mdx (RESEARCH Open Question 2 — authored now as ordinary bilingual content, feeds the Phase-2 live launch): Impressum with the standard German TMG-style provider identification (name, contact email — reuse contact.ts facts; no street address unless the user supplies one at the end-of-phase check: note the placeholder need in the SUMMARY). Datenschutzerklärung covering: static site on Vercel (hosting provider, server logs), cookieless Vercel Web Analytics + Speed Insights (no cookies, no consent banner required, visitor hash discarded after 24h per vendor privacy docs), contact via email, rights under DSGVO. English versions as adaptations labeled as convenience translations.

    Wiring: add the pages collection to content-collections.ts (name 'pages', include '*/pages/*.mdx', schema title + optional description/order, transform mirrors caseStudies incl. raw content retention); add getPages/getPage accessors to src/lib/content.ts; create src/app/[locale]/[slug]/page.tsx mirroring the case-study route (generateStaticParams over locales × page slugs from the generated collection, setRequestLocale, notFound fallback, MDXContent render, localeAlternates for the slug path); add footer links on src/app/[locale]/page.tsx to about/impressum/datenschutz with labels from new messages footer keys (same keys in de.json and en.json).
  </action>
  <verify>
    <automated>pnpm check:content && pnpm build && (pnpm start & SERVER=$!; sleep 4; for p in about impressum datenschutz; do curl -s -o /dev/null -w "%{http_code} " "http://localhost:3000/de/$p"; curl -s -o /dev/null -w "%{http_code} " "http://localhost:3000/en/$p"; done; curl -s http://localhost:3000/de/case-studies/vidama-mediathek | grep -c "Tradeoff\|Abwägung"; kill $SERVER)</automated>
  </verify>
  <acceptance_criteria>
    - All six prose-page URLs (/de|/en × about|impressum|datenschutz) return 200 as prerendered routes; unknown slugs 404
    - The expanded Vidama case study contains all four deep-case-study sections in both locales and exceeds the thin version substantially (min_lines 45 per must_haves)
    - datenschutz.mdx names Vercel hosting and cookieless analytics; impressum.mdx contains name and contact email
    - `pnpm check:content` exits 0 (parity across the grown tree + blocklist scan)
    - messages key parity still holds between de.json and en.json (same flattened key set)
    - DE and EN of every document land in the same commit
  </acceptance_criteria>
  <done>Every v1 prose document exists complete in both languages and renders at its own static route.</done>
</task>

<task type="auto">
  <name>Task 3: Full-content preview deployment + end-to-end verification + README (D-08, success criteria 1+2)</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-bilingual-content-foundation/01-02-SUMMARY.md (preview URL mechanics, production branch name, analytics enablement state)
    - .planning/ROADMAP.md (the four Phase-1 success criteria being verified)
  </read_first>
  <action>
    README.md: full rewrite for the new stack — project intro (bilingual portfolio, Next.js 16 + next-intl + Content Collections), commands (pnpm dev / build / start / check:content), content-model overview (content/{de,en} parallel trees, MDX + typed TS modules, parity gate), deployment note (Vercel previews; production switch is Phase 2). Do not document internal reference material beyond noting that reference/ dirs are local-only.

    Then run the full local gate (pnpm check:content, pnpm build — all routes must remain statically prerendered) and push the branch so CI (parity + build + LHCI budget) runs green on the final content volume. Deploy a fresh preview via vercel deploy (NEVER --prod, D-08). Capture THIS deploy's URL from the command's stdout and write it (overwrite) to /tmp/vercel-preview-url.txt BEFORE running the verification matrix — never reuse plan 01-02's URL: /tmp persistence across waves/sessions is not guaranteed, and this task's criteria require the fresh full-content deployment, not the 01-02 skeleton deploy. Then run the verification matrix against the preview URL with curl:
    - / redirects to /de
    - For each locale (de, en): home 200 with career/projects/skills/contact content; /case-studies/elia 200; /case-studies/vidama-mediathek 200; /about, /impressum, /datenschutz 200
    - hreflang: home and one case-study page carry link alternates for de, en, x-default with absolute preview-host URLs; Link response header present
    - Analytics client reference present in the HTML
    - https://lsiem.de still serves the old site (D-08) — positive marker: its HTML references vite.svg (old Vite favicon, verified live at planning time)
    Record the preview URL and the full matrix results in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm check:content && pnpm build && git push && PREVIEW_URL=$(cat /tmp/vercel-preview-url.txt) && test -n "$PREVIEW_URL" && for path in de en de/case-studies/elia en/case-studies/elia de/case-studies/vidama-mediathek en/case-studies/vidama-mediathek de/about en/about de/impressum en/impressum de/datenschutz en/datenschutz; do code=$(curl -s -o /dev/null -w "%{http_code}" "$PREVIEW_URL/$path"); echo "$path -> $code"; [ "$code" = "200" ] || exit 1; done && curl -s "$PREVIEW_URL/de" | grep -c 'hreflang="x-default"'</automated>
    <human-check>End-of-phase review on the preview URL: (1) read the ELIA and Vidama case studies fully in BOTH languages — confidentiality (D-03) and content quality; (2) confirm career timeline dates, especially the ITSC Software-Engineering transition left date-free by plan 01-03, and supply the Impressum street-address decision; (3) switch locales on several pages and confirm content equivalence (roadmap criteria 1+2); (4) check the Vercel dashboard for Analytics page views and Speed Insights data (criterion 4).</human-check>
  </verify>
  <acceptance_criteria>
    - The curl matrix passes: 12 content URLs × 200 on the fresh preview deployment, root redirect intact, x-default hreflang present
    - `/tmp/vercel-preview-url.txt` was overwritten with THIS task's own `vercel deploy` output URL before verification (not plan 01-02's stale URL)
    - CI run on the final push is green including the LHCI budget (criterion 3 holds at full content volume)
    - README.md documents stack, commands, content model, and the preview-only deployment policy
    - lsiem.de response still identifies the old site
    - SUMMARY records the preview URL and any items needing user input (ITSC transition date, Impressum address)
  </acceptance_criteria>
  <done>All Phase-1 roadmap success criteria are verifiable on the deployed preview URL; open user-input items are surfaced for the end-of-phase human check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local reference material → committed files | The ONLY place internal ELIA details exist is the gitignored reference/ dir; authoring crosses this boundary under the D-03 abstraction rules |
| Public GitHub repo | Case studies, commit messages, and SUMMARYs are world-readable |
| Repo → Vercel production | lsiem.de must remain untouched (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Information Disclosure | elia.mdx (DE/EN), commit messages, SUMMARY | high | mitigate | Whitelist-only authoring (D-03); blocklist created BEFORE authoring and scanned via pnpm check:content pre-commit; rule extends to commit messages and SUMMARY; end-of-phase human review of both language versions |
| T-01-12 | Information Disclosure | blocklist.txt itself (contains the forbidden names) | high | mitigate | Stored inside the gitignored reference/ directory; git check-ignore + git status --porcelain assertions in Task 1; never referenced verbatim in committed files |
| T-01-13 | Information Disclosure | Legal pages exposing private data (address) | medium | mitigate | No street address published without explicit user decision (surfaced in end-of-phase check); only existing public contact facts reused |
| T-01-14 | Tampering | Production deployment (lsiem.de) | high | mitigate | Preview-only deploys; production-branch prohibition re-asserted; post-deploy lsiem.de assertion |
| T-01-SC | Tampering | npm installs | high | mitigate | Zero new packages in this plan; audited set unchanged (lockfile diff must be empty) |
</threat_model>

<verification>
- Reviewer path (criterion 1): every case study + about readable in DE and EN in the typed model, deployed
- Visitor path (criterion 2): /de and /en serve equivalent content with switcher and complete hreflang on the preview URL
- Pipeline (criterion 3) green at full volume; analytics (criterion 4) receiving — human check
- Confidentiality: blocklist scan green across the whole repo content; nothing under reference/ tracked
</verification>

<success_criteria>
- All four ROADMAP Phase-1 success criteria demonstrably TRUE (criteria 1-3 automated, criterion 4 + content quality via end-of-phase human check)
- Phase 2 can start on presentation immediately — the content model is complete, bilingual, and machine-readable
</success_criteria>

<output>
Create `.planning/phases/01-bilingual-content-foundation/01-04-SUMMARY.md` when done. Record: preview URL, verification matrix results, open user-input items (ITSC transition date, Impressum address), and confirmation that no reference-derived term appears in any committed artifact. The SUMMARY itself falls under D-03 — abstracted vocabulary only.
</output>
