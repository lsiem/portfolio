---
phase: 01-bilingual-content-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - content/shared/types.ts
  - content/de/career.ts
  - content/en/career.ts
  - content/de/contact.ts
  - content/en/contact.ts
  - content/de/projects.ts
  - content/en/projects.ts
  - content/de/skills.ts
  - content/en/skills.ts
  - messages/de.json
  - messages/en.json
  - src/lib/content.ts
  - src/app/[locale]/page.tsx
autonomous: true
requirements: [CONT-01, I18N-02]
must_haves:
  truths:
    - "A visitor on /de and /en sees the full career timeline including the ITSC role progression Systemadministrator (OpenShift) -> Software Engineering -> Product Owner (D-02)"
    - "A visitor sees 5-7 projects with exactly one flagship (ELIA) and one deep entry (Vidama-Mediathek) plus an OpenShift platform card (D-01)"
    - "Skills are grouped by domain with years and project references — no percent bars (CONT-05 shape)"
    - "Contact (email, GitHub, LinkedIn) is present in both locales with role-relevant facts only"
    - "Every DE structured-content file has an EN counterpart written in the same commit (I18N-02, D-05, Pitfall 7)"
    - "The ELIA project entry contains only abstracted whitelist terms — no internal system names, roadmap dates, or model names (D-03)"
  artifacts:
    - path: "content/de/career.ts"
      provides: "5 career stations, ITSC entry with D-02 role progression"
      min_lines: 40
    - path: "content/en/career.ts"
      provides: "English adaptation, identical structure"
      min_lines: 40
    - path: "content/de/projects.ts"
      provides: "5-7 projects: elia (flagship), vidama-mediathek (deep), openshift-platform (card), plus selected old projects"
      contains: "flagship"
    - path: "content/de/skills.ts"
      provides: "Domain-grouped skills with years and projectSlugs"
    - path: "content/de/contact.ts"
      provides: "name, role, email, github, linkedin"
      contains: "info@lsiem.de"
    - path: "messages/de.json"
      provides: "Complete UI string set (nav, contact, projects, accessibility, footer)"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "content/de/career.ts"
      via: "locale-keyed accessors in src/lib/content.ts"
      pattern: "getCareer"
    - from: "content/de/career.ts"
      to: "content/shared/types.ts"
      via: "satisfies CareerEntry[] against the shared schema types"
      pattern: "satisfies"
---

# Plan 01-03: Structured bilingual content — career, projects, skills, contact, UI strings

## Phase Goal

**As a** Besucher (Recruiter, Tech-Lead oder Reviewer), **I want to** sämtliche Portfolio-Inhalte vollständig auf Deutsch und Englisch unter `/de` und `/en` einer deployten, performance-überwachten Site lesen und die Sprache umschalten, **so that** ich Lasses Werdegang, Projekte und Kontaktmöglichkeiten in meiner Sprache beurteilen kann.

<objective>
Author ALL structured v1 content in German and English as typed TS modules against the plan-01-01 contract (CONT-01, D-06): career timeline with the D-02 ITSC arc, the D-01-weighted project set (ELIA flagship / Vidama deep / OpenShift card / selected older projects), domain-grouped skills, contact data, and the complete UI message set — refined from the extracted raw material (D-04), with the English versions written by Claude as adaptations (D-05). Render everything minimally on the locale home pages so it is reviewable in both languages.

Purpose: Content is this phase's center of gravity — the "thin content" failure of rebuild #1 is fixed by writing content BEFORE effects (RESEARCH Summary). This plan delivers all structured data; prose (case studies, about) is plan 01-04.
Output: 8 bilingual content modules, 2 complete message files, home page rendering all sections per locale.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-bilingual-content-foundation/01-CONTEXT.md
@.planning/phases/01-bilingual-content-foundation/01-RESEARCH.md
@.planning/phases/01-bilingual-content-foundation/01-PATTERNS.md
@.planning/phases/01-bilingual-content-foundation/01-01-SUMMARY.md
</context>

## Artifacts this phase produces (plan 03 portion)

- `content/de/career.ts` + `content/en/career.ts` — export `career: CareerEntry[]` (via `satisfies`), plus exported `careerIntro: string`
- `content/de/contact.ts` + `content/en/contact.ts` — export `contact: ContactInfo`
- `content/de/projects.ts` + `content/en/projects.ts` — export `projects: Project[]`, incl. new slugs `elia`, `openshift-platform`, `vidama-mediathek`
- `content/de/skills.ts` + `content/en/skills.ts` — export `skillDomains: SkillDomain[]`
- `messages/de.json` + `messages/en.json` — complete namespaces `nav`, `common`, `home`, `career`, `projects`, `skills`, `contact`, `footer`, `accessibility`
- Extended `src/lib/content.ts` — new exports `getCareer(locale)`, `getProjects(locale)`, `getSkillDomains(locale)`, `getContact(locale)`
- Extended `src/app/[locale]/page.tsx` — semantic sections for career, projects, skills, contact

<tasks>

<task type="auto">
  <name>Task 1: Career timeline + contact, DE and EN (D-02, D-04, D-05)</name>
  <files>content/de/career.ts, content/en/career.ts, content/de/contact.ts, content/en/contact.ts, content/shared/types.ts</files>
  <read_first>
    - .planning/phases/01-bilingual-content-foundation/extracted/experience.ts (the 5 stations — full German raw material)
    - .planning/phases/01-bilingual-content-foundation/extracted/personal.ts (contact facts)
    - content/shared/types.ts (the contract from plan 01-01 — extend only if a field is genuinely missing, never weaken)
    - .planning/phases/01-bilingual-content-foundation/01-PATTERNS.md (career table with the stale-ITSC warning, satisfies idiom, contact security note)
    - .planning/phases/01-bilingual-content-foundation/01-CONTEXT.md (D-02 wording of the ITSC arc)
  </read_first>
  <action>
    content/{de,en}/career.ts: transfer the 5 stations from extracted/experience.ts into CareerEntry[] (ISO-ish from/to as YYYY-MM; to null = present), typed with the satisfies idiom against content/shared/types.ts. CRITICAL per D-02 and PATTERNS: the old flat ITSC entry ("Systemadministrator, Juni 2024 – Heute") is STALE — rebuild it as ONE ITSC entry (from 2024-06, to null) whose roles array tells the progression: (1) Systemadministrator with OpenShift/Kubernetes cluster operations for public health insurers, from 2024-06; (2) Software Engineering phase — exact transition date is NOT in any source artifact, so set its from to null and keep the description date-free (the end-of-phase human check asks the user to confirm/correct dates); (3) Product Owner of an internal AI assistant (abstracted per D-03 — no internal product name beyond what the public whitelist allows; the role can say Product Owner eines internen KI-Assistenten), from 2026-04. The other 4 stations (Just Relate seit 2024-01, Vidama CTO 2021-12 bis 2024-01, Freelance seit 2021-04, Heinsohn 2021-05 bis 2021-12) carry a single role each; refine the old German descriptions, do not rewrite from scratch (D-04). Keep and refine the intro prose from extracted lines 4-7 as careerIntro.

    content/{de,en}/contact.ts: from extracted/personal.ts — name Lasse Siemoneit, email info@lsiem.de, github https://github.com/lsiem, linkedin https://www.linkedin.com/in/lasse-siemoneit/. Update the stale role string to reflect the broader D-02 narrative (e.g., Software Engineer & Product Owner — DE/EN variants). Deliberately publish NO phone, birthdate, or address (RESEARCH Security Domain: role-relevant facts only).

    English files are Claude's adaptation of the FINAL German text (D-05) — natural English for international tech leads, not literal translation — written in the SAME commit as their German counterparts (RESEARCH Pitfall 7). Only widen content/shared/types.ts if a needed field is missing (e.g., making careerRoleSchema.from nullable if plan 01-01 made it required); never remove fields other plans rely on.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && diff <(cd content/de && find . -type f | sort) <(cd content/en && find . -type f | sort) && grep -c "2026-04" content/de/career.ts && grep -c "Product Owner" content/en/career.ts && grep -c "info@lsiem.de" content/de/contact.ts content/en/contact.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit` exits 0 — both locale files satisfy the shared types (the compiler IS the structured parity check, D-06)
    - `content/de` and `content/en` file trees are identical
    - The ITSC entry contains a roles array with three entries; the Product Owner role has from `2026-04`; the middle Software-Engineering role carries no invented date
    - Career totals 5 entries in both locales; Vidama entry period is 2021-12 to 2024-01
    - Both contact files export exactly name/role/email/github/linkedin; a case-insensitive search for phone, birth, and street/address fields in contact.ts files returns nothing
    - DE and EN of each file are added in the same commit
  </acceptance_criteria>
  <done>Career and contact exist complete and typed in both languages with the D-02 arc modeled.</done>
</task>

<task type="auto">
  <name>Task 2: Projects + skills + complete UI messages, rendered on the home page (D-01, D-03, D-05, CONT-05)</name>
  <files>content/de/projects.ts, content/en/projects.ts, content/de/skills.ts, content/en/skills.ts, messages/de.json, messages/en.json, src/lib/content.ts, src/app/[locale]/page.tsx</files>
  <read_first>
    - .planning/phases/01-bilingual-content-foundation/extracted/projects.ts (7 old projects with problem/solution copy)
    - .planning/phases/01-bilingual-content-foundation/extracted/skills.ts (skill inventory to regroup)
    - .planning/phases/01-bilingual-content-foundation/extracted/ui.ts (message raw material; note the dead keys to drop)
    - .planning/phases/01-bilingual-content-foundation/01-CONTEXT.md (D-01 weighting, D-03 whitelist — the ONLY permitted ELIA vocabulary)
    - content/shared/types.ts and src/lib/content.ts (contract + accessor style from plan 01-01)
  </read_first>
  <action>
    content/{de,en}/projects.ts — export projects: Project[] with the D-01 weighting:
    1. Slug elia, depth 'flagship', order 1. Summary/tags STRICTLY from the D-03 abstraction whitelist: Multi-Agenten-Architektur, MCP-basiertes Tool-Calling statt klassischem RAG, Orchestrator-Migration auf ein aktuelles Agent-Framework, EU-Datenresidenz/DSGVO, Teams-Integration, category terms like ITSM-System and Wiki. Role: Product Owner eines internen KI-Assistenten einer IT-Dienstleisterin für Krankenkassen. NO internal system names, NO roadmap dates, NO model names — this file is committed to a PUBLIC repo. Do NOT read the local reference PDF/txt for this task; the whitelist in CONTEXT.md is sufficient and safer.
    2. Slug vidama-mediathek, depth 'deep', order 2 — metadata aligned with the plan-01-01 case-study frontmatter (title, summary from the extracted ewe-mediathek entry, period 2021-12 to 2024-01).
    3. Slug openshift-platform, depth 'card', order 3 — net-new short card: OpenShift/Kubernetes platform operations for public health insurers at an IT service provider (from the D-02 background; abstracted, no internal names).
    4. Plus 2-4 cards selected from the extracted set (Claude's discretion per CONTEXT.md — prefer enterprise relevance and tech breadth, e.g. disy-one, sport-event-controller, immobilienbaukasten), total 5-7 projects, refined German copy then English adaptation (D-04/D-05).

    content/{de,en}/skills.ts — export skillDomains: SkillDomain[]: regroup the extracted inventory into 4-6 domains (e.g., Web & Application Engineering, Cloud & Container Platforms, Databases, DevOps & CI/CD, AI & Integration) where each skill carries name, years (estimate from career dates), projectSlugs referencing real project slugs from projects.ts. NO percent values, NO level bars (CONT-05 anti-feature). Drop icon coupling; it is presentation (Pitfall 6).

    messages/{de,en}.json — complete the UI namespaces from extracted/ui.ts: keep and refine nav.*, contact.* (labels, copyEmail/copied), projects.* (problem/solution/technologies labels), accessibility.*; add footer.* and skills/career section headings; DROP the dead keys tied to the discarded UI: terminal, loading, hero.typingRoles, experience.flipHint (PATTERNS message map).

    src/lib/content.ts — add locale-keyed accessors getCareer, getProjects, getSkillDomains, getContact (static imports of both locale modules keyed by locale — build-time only, no dynamic import needed for SSG).

    src/app/[locale]/page.tsx — add semantic, unstyled sections: career timeline (ol with entries + roles), projects (ul with title/summary/tags, deep+flagship entries linking to their case-study routes where they exist), skills by domain, contact block (mailto + GitHub + LinkedIn anchors). Headings from messages. Keep setRequestLocale. Presentation stays Phase 2 — semantic HTML only.
  </action>
  <verify>
    <automated>pnpm build && (pnpm start & SERVER=$!; sleep 4; curl -s http://localhost:3000/de | grep -c "ITSC"; curl -s http://localhost:3000/en | grep -c "Product Owner"; curl -s http://localhost:3000/de | grep -ci "openshift"; curl -s http://localhost:3000/de | grep -c "info@lsiem.de"; kill $SERVER) && node scripts/check-content-parity.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0; home pages at /de and /en render career, projects, skills, and contact sections with real content
    - projects.ts has 5-7 entries; exactly one has depth 'flagship' (slug elia) and exactly one 'deep' (slug vidama-mediathek); openshift-platform exists with depth 'card'
    - Every projectSlugs reference in skills.ts matches an existing project slug (assert via a quick node -e script or tsc-level check)
    - A case-insensitive grep over content/ and messages/ for percent-style skill levels (pattern like level or a number followed by the percent sign in skills files) returns nothing
    - messages/de.json and messages/en.json have identical key structure (verify with node -e comparing sorted flattened keys) and contain no terminal/loading/typingRoles/flipHint keys
    - The ELIA entry uses only whitelist vocabulary — reviewer-checkable against the D-03 list in CONTEXT.md (also caught later by the plan-01-04 blocklist scan)
    - `node scripts/check-content-parity.ts` exits 0 (if plan 01-02 has landed; otherwise the tsc + tree-diff checks from Task 1 stand in)
  </acceptance_criteria>
  <done>All structured v1 content exists bilingually, typed and rendered — a visitor can review career, projects, skills, and contact in both languages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GitHub repo | All content files are world-readable on push (D-03) |
| Published personal data | Contact/career content becomes permanently public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Information Disclosure | ELIA project entry in projects.ts | high | mitigate | Whitelist-only vocabulary from CONTEXT.md D-03; the local reference document is explicitly NOT read in this plan; retroactive blocklist scan in plan 01-04 covers this file; end-of-phase human review |
| T-01-09 | Information Disclosure | Over-publishing personal data (contact.ts, career.ts) | medium | mitigate | Schema has no phone/birthdate/address fields (plan 01-01 contract); acceptance criterion greps confirm; role-relevant facts only per RESEARCH Security Domain |
| T-01-10 | Tampering | Content correctness (ASVS V5) | low | mitigate | satisfies-typed modules against shared Zod-derived types; tsc gate in CI |
| T-01-SC | Tampering | npm installs | high | mitigate | Zero new packages in this plan; any addition would require re-running the package legitimacy audit |
</threat_model>

<verification>
- tsc + build green; both home pages render all four structured sections with real bilingual content
- D-01 weighting present (flagship/deep/card distribution), D-02 arc modeled, CONT-05 shape respected
- DE/EN tree parity and message-key parity hold
</verification>

<success_criteria>
- Roadmap success criterion 1 (structured half): Werdegang, 5-7 Projekte-Metadaten, Skills, Kontakt vollständig in DE und EN im typisierten Modell
- No confidential vocabulary in any committed file
</success_criteria>

<output>
Create `.planning/phases/01-bilingual-content-foundation/01-03-SUMMARY.md` when done. Record: which old projects were selected as cards and why, the assumed years-per-skill mapping, and the open date question for the ITSC Software-Engineering transition (for the end-of-phase human check).
</output>
