---
phase: 03-design-direction-immersive-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/lib/motion-tokens.ts
  - src/app/[locale]/layout.tsx
  - src/components/motion/motion-provider.tsx
  - src/components/motion/hero-scene-slot.tsx
  - src/components/motion/reveal.tsx
  - src/components/motion/split-heading.tsx
  - src/app/[locale]/page.tsx
  - evals/immersive.spec.ts
autonomous: false
requirements: [WOW-03, WOW-04, MODE-02, TECH-02]

must_haves:
  truths:
    - "On first paint the hero shows name, role and value-prop as static SSR HTML before any JS runs — no preloader (WOW-04, D-17)"
    - "The hero H1 renders in the Bricolage Grotesque display face (D-03)"
    - "With prefers-reduced-motion: reduce the hero renders at final opacity/position with no animation and Lenis is never instantiated (MODE-02, D-18)"
    - "Lenis smooth scroll is active only when prefers-reduced-motion is no-preference AND pointer is fine (D-09, D-19, TECH-02)"
    - "There is exactly one animation engine (GSAP) and one motion-token source of truth (--motion-* in globals.css) (WOW-03, D-08)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "--motion-* custom properties (durations/easings/distances/staggers) as the single motion-token source"
      contains: "--motion-duration-base"
    - path: "src/lib/motion-tokens.ts"
      provides: "client-only reader that converts --motion-* CSS custom properties to GSAP-friendly numbers"
      exports: ["getMotionToken"]
    - path: "src/components/motion/motion-provider.tsx"
      provides: "root-mounted Lenis + gsap.ticker sync, gated via useSyncExternalStore"
      min_lines: 40
    - path: "src/components/motion/reveal.tsx"
      provides: "reveal-on-enter wrapper (useGSAP + gsap.matchMedia + ScrollTrigger)"
    - path: "src/components/motion/split-heading.tsx"
      provides: "SplitText headline wrapper with real DOM text underneath"
    - path: "src/components/motion/hero-scene-slot.tsx"
      provides: "empty reserved background layer for the Phase-4 3D canvas (Server Component)"
    - path: "src/app/[locale]/layout.tsx"
      provides: "Bricolage_Grotesque --font-display + MotionProvider mounted around children"
      contains: "MotionProvider"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/motion/motion-provider.tsx"
      via: "MotionProvider wraps the children div"
      pattern: "MotionProvider"
    - from: "src/components/motion/reveal.tsx"
      to: "src/lib/motion-tokens.ts"
      via: "reveal reads duration/distance/ease from getMotionToken"
      pattern: "getMotionToken"
    - from: "src/lib/motion-tokens.ts"
      to: "src/app/globals.css"
      via: "getComputedStyle reads --motion-* declared in :root"
      pattern: "--motion-"
---

## Phase Goal

**As a** visitor to lsiem.de, **I want to** move through the portfolio as a distinctive, engineered scroll-storytelling journey with felt craft, **so that** I remember Lasse as an engineer who builds and operates systems with taste — while never losing the one-click fast path to the facts.

*(This plan delivers the first visible slice of that journey: the engineered design direction and the "system booting" hero intro, on the motion foundation the whole phase reuses.)*

<objective>
Introduce the motion layer this repo does not have yet (zero animation deps today) and land the engineered design direction on the hero as the first end-to-end slice. Install GSAP + Lenis (single engine, D-08), add the Bricolage Grotesque display face (D-03), declare the central `--motion-*` motion tokens (D-08, single source of truth), build the MotionProvider (Lenis↔gsap.ticker sync, gated for reduced-motion/touch) plus the `Reveal`, `SplitHeading`, and reserved `HeroSceneSlot` primitives, and apply the hero intro sequence (D-12) — all while keeping name/role/value-prop as first-paint SSR HTML (WOW-04) and collapsing to the fully-designed static page under reduced-motion (MODE-02).

Purpose: Get the reduced-motion/touch conditional-mount gate right ONCE here so MODE-02 and TECH-02 are satisfied structurally for the rest of the phase; every later plan just consumes these primitives.
Output: Motion dependencies, motion tokens, the four hero-critical motion components, the wired layout, the engineered hero, and the first `evals/immersive.spec.ts` contracts.
</objective>

## Artifacts this phase produces (this plan)

- **Components:** `MotionProvider` (`src/components/motion/motion-provider.tsx`), `Reveal` (`src/components/motion/reveal.tsx`), `SplitHeading` (`src/components/motion/split-heading.tsx`), `HeroSceneSlot` (`src/components/motion/hero-scene-slot.tsx`)
- **Module:** `getMotionToken` + typed token names (`src/lib/motion-tokens.ts`)
- **CSS custom properties (new, in `globals.css`):** `--motion-duration-fast`, `--motion-duration-base`, `--motion-duration-slow`, `--motion-duration-chapter`, `--motion-ease-out`, `--motion-ease-in-out`, `--motion-ease-magnetic`, `--motion-distance-sm`, `--motion-distance-md`, `--motion-distance-lg`, `--motion-stagger-char`, `--motion-stagger-word`, `--motion-stagger-list`, plus the `--font-display` font-family binding
- **Font token:** `--font-display` (Bricolage Grotesque, self-hosted via `next/font/google`)
- **New dependencies:** `gsap`, `@gsap/react`, `lenis`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-direction-immersive-experience/03-CONTEXT.md
@.planning/phases/03-design-direction-immersive-experience/03-RESEARCH.md
@.planning/phases/03-design-direction-immersive-experience/03-UI-SPEC.md
@.planning/phases/03-design-direction-immersive-experience/03-PATTERNS.md
@src/components/theme-toggle.tsx
@src/app/[locale]/layout.tsx
@src/app/[locale]/page.tsx
@src/app/globals.css
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate — approve motion dependencies, then install</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/03-design-direction-immersive-experience/03-RESEARCH.md §"Package Legitimacy Audit" — `lenis` is flagged SUS (a "too-new" false-positive: `npm view lenis time.created` = 2023-04-03, 111 versions, 872K/wk, official darkroomengineering repo); `gsap` and `@gsap/react` are Approved
    - .planning/phases/03-design-direction-immersive-experience/03-RESEARCH.md §"Security Domain" — audit `postinstall` scripts before install
  </read_first>
  <action>
    Do NOT install anything before this gate is approved (package-legitimacy protocol; this checkpoint is never auto-approvable). Present the automated evidence in "how-to-verify" to the human. On explicit approval, run `pnpm add gsap@3.15.0 @gsap/react@2.1.2 lenis@1.3.25` (patch-only adjustments allowed if npm reports a newer compatible patch; never change majors; React stays `~19.2.0`), then confirm the three appear in package.json dependencies.
  </action>
  <verify>
    <automated>node -e "const d=require('./package.json').dependencies; if(!d.gsap||!d['@gsap/react']||!d.lenis) process.exit(1)"</automated>
  </verify>
  <what-built>
    Nothing executed before this gate. This is the mandatory package-legitimacy checkpoint (T-03-SC): `lenis` was auto-flagged SUS. Automated context below is provided for the human to confirm before any install runs.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/lenis and https://github.com/darkroomengineering/lenis — confirm the maintainer/org is `darkroomengineering` (the same lineage cited in the project's STACK research) and the package has a multi-year history, not a fresh slopsquat.
    2. Confirm `gsap` (github.com/greensock/GSAP) and `@gsap/react` (github.com/greensock/react) are the official GreenSock packages.
    3. Confirm none of the three declare a suspicious `postinstall` — RESEARCH reported `postinstall: null` for all three; the executor may re-run `npm view lenis scripts.postinstall`, `npm view gsap scripts.postinstall`, `npm view @gsap/react scripts.postinstall` to reconfirm (expect empty/undefined).
    4. On approval, the executor runs `pnpm add gsap@3.15.0 @gsap/react@2.1.2 lenis@1.3.25` (versions per RESEARCH Standard Stack; adjust patch only if npm reports a newer compatible patch — do NOT change majors, React stays `~19.2.0`).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize install, or describe concerns</resume-signal>
  <done>The three packages appear in package.json dependencies; `pnpm install` completes with no postinstall surprises; React pin unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Motion tokens, display face, MotionProvider + reserved 3D seam</name>
  <files>src/app/globals.css, src/lib/motion-tokens.ts, src/app/[locale]/layout.tsx, src/components/motion/motion-provider.tsx, src/components/motion/hero-scene-slot.tsx</files>
  <read_first>
    - src/components/theme-toggle.tsx — the EXACT `useSyncExternalStore` module-scope pub/sub + `getSnapshot`/`getServerSnapshot` pattern MotionProvider must copy (lines 15-43, 90)
    - src/app/globals.css — existing `:root` token block + the `@media (prefers-reduced-motion: no-preference)` precedent (lines 3-19, 64-68) to extend, not duplicate
    - src/app/[locale]/layout.tsx — existing `next/font/google` Geist wiring (lines 3, 33-41, 66) and the provider nesting where MotionProvider mounts (lines 70, 99)
    - 03-UI-SPEC.md §"Motion Tokens & Choreography" — the fixed token value table (durations/easings/distances/staggers) and the Lenis↔ScrollTrigger sync recipe
    - 03-RESEARCH.md Pattern 1 (Motion Provider) + Pitfall 4 (useSyncExternalStore mandatory) + Pitfall 6 + Open Question 1 — inspect `node_modules/lenis/dist/lenis-react.d.ts` for the real `ReactLenis` ref shape before finalizing the ticker wiring
    - 03-PATTERNS.md §motion-provider.tsx and §globals.css and §layout.tsx
  </read_first>
  <action>
    Add every `--motion-*` custom property from the UI-SPEC token table to the existing `:root` block in globals.css (durations in ms, distances in px, easings as cubic-bezier, staggers in ms). These token values encode the locked medium/expressive intensity (D-10) — noticeably animated but disciplined, not spectacle. Do NOT add `@theme inline` mirrors — these are consumed by GSAP via getComputedStyle, not by Tailwind utilities (UI-SPEC "pick one source of truth"). Add dense rationale comments citing D-08/D-09 in the existing comment style. Declare the single source of truth explicitly: globals.css holds the canonical values; motion-tokens.ts only reads them.

    Create src/lib/motion-tokens.ts as a client-only utility exporting `getMotionToken(name)` that reads a `--motion-*` property from `getComputedStyle(document.documentElement)` and returns a GSAP-friendly number (ms→seconds for durations, px number for distances, raw string for easings) — memoize per token name at module scope. Add a doc comment stating it is browser-only (never import from a Server Component), mirroring the client/server discipline noted in the github-heatmap/theme-toggle comment density. Export a typed union of the valid token names.

    In layout.tsx: import `Bricolage_Grotesque` from `next/font/google`, instantiate with `variable: "--font-display"` and the variable weight/width/optical-size axes per RESEARCH (fall back to weight-only if the axes API rejects), and append `.variable` to the `<html className>` exactly like `geistSans.variable`/`geistMono.variable`. Bind `--font-display` in globals.css `@theme inline` as a Tailwind font family (e.g. `--font-display: var(--font-display)`) so a `font-display` utility exists for the hero. Then wrap the existing `<div className="flex flex-1 flex-col">{children}</div>` with `<MotionProvider>` (import from `@/components/motion/motion-provider`) — leave header/footer nesting as-is unless the lenis d.ts inspection shows the root must wrap the whole body content.

    Create src/components/motion/motion-provider.tsx ("use client" on line 1): register `gsap.registerPlugin(ScrollTrigger)` ONCE at module scope (add the load-bearing comment per RESEARCH Pitfall/anti-pattern). Read the two gates (`prefers-reduced-motion: no-preference` AND `pointer: fine`) via `useSyncExternalStore(subscribeMotionGates, getMotionGatesSnapshot, getServerSnapshot)` where `getServerSnapshot` returns `false` (motion off) — copy theme-toggle's pattern verbatim in shape. When gates are false, render `<>{children}</>` (native scroll, no Lenis instance). When true, render `<ReactLenis root autoRaf={false} options={{ lerp: 0.1, duration: 1.2 }}>` and wire the ticker per the confirmed recipe: `lenis.on('scroll', ScrollTrigger.update)`, `gsap.ticker.add((time) => lenisRef.current?.lenis?.raf(time * 1000))`, `gsap.ticker.lagSmoothing(0)` — using the actual ref shape found in lenis-react.d.ts. Call `ScrollTrigger.refresh()` on mount AND inside `document.fonts.ready.then(...)` (RESEARCH Pitfall 3, font-swap reflow). Keep all setup inside effects (never in the render body) so first client render matches SSR (WOW-04, Pitfall 2).

    Create src/components/motion/hero-scene-slot.tsx as a Server Component (no "use client", no hooks) — a single `<div aria-hidden="true" className="absolute inset-0 -z-10 pointer-events-none" />` with a comment stating Phase 4 fills its children with the lazy capability-gated 3D `<Canvas>` (D-13); renders nothing visible in Phase 3.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--motion-duration-base' src/app/globals.css` is at least 1, and all 13 UI-SPEC motion tokens are present (spot-check `--motion-ease-magnetic`, `--motion-stagger-list`, `--motion-distance-lg`)
    - `src/lib/motion-tokens.ts` exports `getMotionToken`; `grep -c 'getComputedStyle' src/lib/motion-tokens.ts` is at least 1
    - `grep -c 'useSyncExternalStore' src/components/motion/motion-provider.tsx` is at least 1 and `grep -c 'useState' src/components/motion/motion-provider.tsx` is 0 (React-Compiler lint discipline, RESEARCH Pitfall 4)
    - `grep -c 'registerPlugin' src/components/motion/motion-provider.tsx` is at least 1 (registered at module scope)
    - `grep -c 'MotionProvider' src/app/[locale]/layout.tsx` is at least 1 and `grep -c 'Bricolage_Grotesque' src/app/[locale]/layout.tsx` is at least 1
    - `src/components/motion/hero-scene-slot.tsx` contains no `"use client"` directive (Server Component)
    - `pnpm build` succeeds (prebuild content-parity + CV gen still pass); `pnpm lint` reports no errors
  </acceptance_criteria>
  <done>Motion tokens, Bricolage display face, motion-tokens reader, MotionProvider (gated Lenis+ticker), and the reserved HeroSceneSlot exist and build cleanly; the app still renders identically since no section consumes motion yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reveal + SplitHeading primitives and the engineered hero intro</name>
  <files>src/components/motion/reveal.tsx, src/components/motion/split-heading.tsx, src/app/[locale]/page.tsx, evals/immersive.spec.ts</files>
  <read_first>
    - src/app/[locale]/page.tsx — the hero section (lines 84-127): H1 `{contact.name}` has NO nested link (safe SplitText target per RESEARCH Pattern 4), the value-prop `[data-testid="hero-value-prop"]`, and the anchor nav that must stay focusable/clickable throughout
    - 03-RESEARCH.md Pattern 2 (Reveal-on-enter, gsap.matchMedia branch) + Pattern 4 (SplitText nested-link trap, built-in aria since v3.13)
    - 03-UI-SPEC.md §"Hero intro sequence (D-12)" (the 0-1000ms beat timing) + §"Layout & Grid (D-04)" (12-col, max-w-1440, hero breaks wide) + §Typography (Display clamp, retire font-semibold from H1)
    - 03-PATTERNS.md §reveal/magnetic/split-heading and §page.tsx (do not restructure server markup; wrap only)
    - evals/home.spec.ts — existing Playwright shape (locale loop, selectors) to mirror
  </read_first>
  <behavior>
    - Test: on `/de` and `/en`, `#hero h1` is visible and its computed `font-family` includes "Bricolage" (display face applied, D-03)
    - Test: `#hero`, the H1, `[data-testid="hero-value-prop"]`, and the anchor nav are all visible immediately after `page.goto` (first-paint content present, no preloader — WOW-04)
    - Test: under `emulateMedia({ reducedMotion: 'reduce' })`, the hero value-prop computed `opacity` is `1` and the H1 text equals `contact.name` in full (reduced-motion renders final state, real DOM text — MODE-02, D-18)
    - Test: the anchor nav links (`nav a[href="#career"]`) remain clickable (no pointer-events:none gate) even immediately on load
  </behavior>
  <action>
    Create src/components/motion/reveal.tsx ("use client"): a `Reveal` wrapper taking `children` and optional `distance`/`duration`/`stagger` props that default to values from `getMotionToken` (`--motion-distance-md`, `--motion-duration-base`, `--motion-stagger-list`) — never hardcode the numbers. Use `useGSAP(() => { const mm = gsap.matchMedia(); ... return () => mm.revert(); }, { scope })` with a `(prefers-reduced-motion: reduce)` branch that `gsap.set`s the item to final `{opacity:1, y:0}` and a `(prefers-reduced-motion: no-preference)` branch that `gsap.from`s opacity/y with `scrollTrigger: { trigger: scope.current, start: "top 85%", once: true }` and ease `"expo.out"` (mirrors `--motion-ease-out`). Copy RESEARCH Pattern 2's shape exactly.

    Create src/components/motion/split-heading.tsx ("use client"): a `SplitHeading` wrapper that renders its children as a real DOM heading and, inside `useGSAP` + a `no-preference` matchMedia branch, applies `SplitText` (imported from `gsap/SplitText`) with per-word stagger (`--motion-stagger-word`). Rely on SplitText v3.13+ built-in aria (real text stays underneath). Add a comment: never pass a heading containing an interactive `<a>` (RESEARCH Pattern 4 nested-link trap) — the hero H1 is plain text and safe. Reduced-motion branch: render text at final state, create no SplitText.

    In page.tsx hero section (server markup, unchanged data): wrap the whole hero in a widened 12-column grid container (`max-w-[1440px]`, gutter `gap-6`, hero breaks wide per D-04) while keeping a reading-anchored column for the value-prop text; place `<HeroSceneSlot />` as the `-z-10` background layer inside a `relative` hero wrapper (D-13). Replace the H1 wrapper so `{contact.name}` renders via `<SplitHeading>` with the `font-display` Tailwind utility and the Display clamp size — REMOVE `font-semibold` from the H1 (Display weight now comes from Bricolage's variable axis, UI-SPEC weight discipline). Wrap the value-prop `<p>` in `<Reveal>`. Add a grid/tick draw-in overlay element (compositor-friendly `scaleX`/`scaleY`, `--motion-ease-out`) that is decorative + `aria-hidden`. Do NOT gate the nav or any interactive element behind `pointer-events:none` during the sequence (WOW-04). Keep the eyebrow "Portfolio" mono label and the anchor nav byte-identical in content.

    Create evals/immersive.spec.ts with the behaviors listed above (locale loop over de/en, using `page.emulateMedia({ reducedMotion })` and `getComputedStyle` reads for font-family/opacity), mirroring evals/home.spec.ts structure.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && pnpm exec playwright test evals/immersive.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec playwright test evals/immersive.spec.ts` passes all hero behaviors (first-paint content, Bricolage font-family on H1, reduced-motion final-state opacity=1 + full H1 text, nav clickable)
    - `grep -c 'getMotionToken' src/components/motion/reveal.tsx` is at least 1 (no hardcoded token numbers)
    - `grep -c 'matchMedia' src/components/motion/reveal.tsx` is at least 1 and `grep -c 'matchMedia' src/components/motion/split-heading.tsx` is at least 1
    - The hero H1 uses the `font-display` utility and the Display clamp size (weight from Bricolage's variable axis, not a Geist bold utility, per UI-SPEC weight discipline); confirm via `grep -c 'font-display' src/app/[locale]/page.tsx` at least 1
    - `grep -c 'HeroSceneSlot' src/app/[locale]/page.tsx` is at least 1
    - existing evals still green: `pnpm exec playwright test evals/home.spec.ts evals/a11y.spec.ts` passes
    - `pnpm build` succeeds; the CI LHCI gate (`resource-summary:script:size` ≤ 184,643 bytes, `largest-contentful-paint` ≤ 2500ms, `cumulative-layout-shift` ≤ 0.1) is the authoritative bundle/CWV gate — run `pnpm exec lhci autorun` locally if bundle headroom is in doubt (RESEARCH Pitfall 5 / Open Q2) before pushing
  </acceptance_criteria>
  <done>The hero plays the engineered intro on capable devices, renders the Bricolage display H1 with real DOM text, keeps all facts + nav present from first paint, and collapses to the static designed hero under reduced-motion — verified by evals/immersive.spec.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | New third-party motion packages enter the build/bundle |
| build → client bundle | Self-hosted JS/font bytes shipped to the visitor; no runtime third-party call (DSGVO) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | `pnpm add gsap @gsap/react lenis` | high | mitigate | Task 1 blocking-human package-legitimacy checkpoint (lenis SUS false-positive), pinned versions, `postinstall: null` reconfirmed before install |
| T-03-01 | Tampering (config) | GSAP/Lenis/Bricolage bundling | low | mitigate | All shipped as self-hosted bundled JS/CSS/font via npm + `next/font` build-time fetch — no runtime `<script src=cdn>`; keeps a future `script-src 'self'` CSP viable (CSP gap tracked separately, unchanged) |
| T-03-02 | Tampering (XSS) | SplitHeading / SplitText | low | mitigate | SplitText operates on rendered DOM text nodes, not raw HTML strings; no new `dangerouslySetInnerHTML` introduced; existing `personJsonLd` script tag unchanged, first-party content-model data only |
</threat_model>

<verification>
- `pnpm lint` — no errors (React-Compiler-aligned hook rules pass; MotionProvider uses useSyncExternalStore)
- `pnpm build` — succeeds including prebuild (content parity + CV generation)
- `pnpm exec playwright test evals/immersive.spec.ts evals/home.spec.ts evals/a11y.spec.ts` — green
- Bundle/CWV: CI LHCI gate enforces `resource-summary:script:size` ≤ 184,643 bytes, LCP ≤ 2500ms, CLS ≤ 0.1 on push; measure locally with `pnpm exec lhci autorun` if headroom is uncertain
- `git grep -c 'useState' src/components/motion/motion-provider.tsx` — 0 (browser-state via useSyncExternalStore only)
</verification>

<success_criteria>
- Motion foundation exists: GSAP single engine installed, `--motion-*` tokens declared, `getMotionToken` reader, MotionProvider gated for reduced-motion + touch, HeroSceneSlot reserved.
- The engineered hero intro plays on capable pointer:fine devices and is fully skippable: identity + value-prop + nav are first-paint SSR HTML with no preloader.
- Under `prefers-reduced-motion: reduce`, the same DOM renders statically with full content (MODE-02) and Lenis is never instantiated.
- Bricolage Grotesque display face applied to the hero H1; `font-semibold` retired from it.
</success_criteria>

<output>
Create `.planning/phases/03-design-direction-immersive-experience/03-01-SUMMARY.md` when done.
</output>
