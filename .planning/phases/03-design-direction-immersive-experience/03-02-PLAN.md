---
phase: 03-design-direction-immersive-experience
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/motion/career-spine.tsx
  - src/components/motion/project-bento.tsx
  - src/app/[locale]/page.tsx
  - evals/immersive.spec.ts
autonomous: true
requirements: [WOW-02, WOW-04, TECH-02]

must_haves:
  truths:
    - "The career reads as a scroll-linked storytelling journey — each org chapter reveals on enter, no pinning and no scrolljacking (WOW-02, D-05)"
    - "The ITSC arc (SysAdmin → Software Engineering → Product Owner) plays as an emphasized multi-beat sub-sequence, other orgs get lighter single reveals (D-06)"
    - "A vertical progress spine with tick/coordinate year labels orients the reader on lg+ screens and is hidden below lg without information loss (D-07, D-19)"
    - "The two deep case studies (ELIA + Vidama) render as the large featured pair in an asymmetric bento; the rest stay compact (D-14)"
    - "All career/project content is present at final state under reduced-motion and on touch (native scroll), no content gated behind motion (WOW-04, MODE-02, TECH-02)"
  artifacts:
    - path: "src/components/motion/career-spine.tsx"
      provides: "progress rail + coordinate ticks with ScrollTrigger onUpdate fill (lg+ only)"
    - path: "src/components/motion/project-bento.tsx"
      provides: "asymmetric 12-col bento grid; ELIA + Vidama featured, compact rest"
    - path: "src/app/[locale]/page.tsx"
      provides: "career section wrapped in progressive reveals + ITSC multi-beat; projects section rendered via ProjectBento"
      contains: "ProjectBento"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/motion/career-spine.tsx"
      via: "career section renders CareerSpine scoped to its scroll range"
      pattern: "CareerSpine"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/motion/project-bento.tsx"
      via: "projects section renders ProjectBento with the getProjects data"
      pattern: "ProjectBento"
    - from: "src/components/motion/career-spine.tsx"
      to: "src/components/motion/reveal.tsx"
      via: "reveals reuse the Reveal/matchMedia primitives from plan 01"
      pattern: "matchMedia"
---

## Phase Goal

**As a** visitor to lsiem.de, **I want to** move through the portfolio as a distinctive, engineered scroll-storytelling journey with felt craft, **so that** I remember Lasse as an engineer who builds and operates systems with taste — while never losing the one-click fast path to the facts.

*(This plan delivers the storytelling core of that journey: the career as scroll-linked scenes with the ITSC arc as centerpiece, plus the flagship bento project hierarchy.)*

<objective>
Turn the career and projects sections into the phase's storytelling centerpiece on the primitives built in plan 01. Add the `CareerSpine` (D-07 progress rail + coordinate ticks) and apply progressive reveal-on-enter to each career chapter with the ITSC role arc expanded into an emphasized multi-beat sub-sequence (D-05, D-06). Add the `ProjectBento` (D-14 featured hierarchy — ELIA + Vidama large, the rest compact) on the widened structural grid (D-04). Content stays server-rendered and fully present under reduced-motion, touch, and below `lg` (WOW-04, MODE-02, TECH-02); native scroll only, no pinning, no scrolljacking (D-05).

Purpose: Satisfy WOW-02 (scroll-verknüpfte Szenen pro Karriere-Kapitel) and deliver the deferred heavier flagship-project treatment, reusing plan 01's Reveal/matchMedia/token infrastructure rather than inventing new motion.
Output: `CareerSpine`, `ProjectBento`, the elevated career + projects sections, and career/bento eval contracts.
</objective>

## Artifacts this phase produces (this plan)

- **Components:** `CareerSpine` (`src/components/motion/career-spine.tsx`), `ProjectBento` (`src/components/motion/project-bento.tsx`)
- **Elevated sections (in `page.tsx`):** career progressive-reveal composition with the ITSC multi-beat sub-sequence; projects rendered as an asymmetric bento
- **No new dependencies, no new CSS tokens** — reuses plan 01's `--motion-*` tokens and `Reveal`/`SplitHeading`/`getMotionToken`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-direction-immersive-experience/03-CONTEXT.md
@.planning/phases/03-design-direction-immersive-experience/03-RESEARCH.md
@.planning/phases/03-design-direction-immersive-experience/03-UI-SPEC.md
@.planning/phases/03-design-direction-immersive-experience/03-PATTERNS.md
@src/app/[locale]/page.tsx
@src/components/github-heatmap.tsx
@.planning/phases/03-design-direction-immersive-experience/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Progress spine + career scroll-storytelling (ITSC multi-beat)</name>
  <files>src/components/motion/career-spine.tsx, src/app/[locale]/page.tsx, evals/immersive.spec.ts</files>
  <read_first>
    - src/app/[locale]/page.tsx — the career section (lines 129-178): `<section id="career">` → `<ol>` → `<li>` per org, `entry.roles` inner `<ol>`, `entry.techStack` chips, `formatMonth`. This markup + data-producing logic must stay byte-identical; only wrap in reveals. Note the org `<h3>` conditionally wraps `<a href={entry.orgUrl}>` — this must NOT be a SplitText target (nested-link trap)
    - src/components/github-heatmap.tsx — the "decorative marks aria-hidden + one sr-only summary" discipline (lines 55-78) CareerSpine ticks copy; the Server-Component-with-minimal-client-JS shape
    - 03-UI-SPEC.md §"Progress spine (D-07)" — lg+ only, 1px `var(--border)` rail scoped to the Career section, tick label format `{2-digit chapter index}·{year}` (e.g. `01·2019`), aria-hidden marks, accent active tick + accent fill segment (transform-based, not width/height)
    - 03-CONTEXT.md D-06 (ITSC arc = multi-beat centerpiece; Just Relate/Vidama/Freelance lighter) and 03-RESEARCH.md Pattern 2 + §"No Analog Found" (scroll-progress-fill has no in-repo analog; build from Pattern 2)
    - 03-UI-SPEC.md §"Reduced-Motion Contract" (spine renders statically, current position shown, no animated fill) + §"Mobile Contract" (spine hidden below lg, dates convey chapter/year)
  </read_first>
  <action>
    Create src/components/motion/career-spine.tsx. Render the tick/label list (one tick per career org entry: a small horizontal dash + mono label `NN·YYYY` where NN is the 2-digit 1-based chapter index and YYYY is the entry's start year) as static server-friendly markup marked `aria-hidden="true"` (the dates already live in the visible DOM — no duplicate screen-reader text, per github-heatmap discipline). The animated progress-fill segment is a thin "use client" wrapper using `useGSAP` + a `no-preference` matchMedia branch with a `ScrollTrigger` (`scrub`-style `onUpdate`) that transforms (scaleY / translateY) an accent fill segment across the career scroll range — reduced-motion branch renders the fill at the resting/current position with no animation. Position the rail in the reserved left margin column, visible `lg:` and up only (`hidden lg:block`), scoped to the Career section's height (not the whole page). Active tick label uses `text-foreground` + accent dash; inactive ticks `text-muted`. Accent usage limited to the active tick + fill (UI-SPEC exhaustive accent list item 3).

    In page.tsx career section: keep the `<section id="career">`, `<ol>`, per-entry `<li>`, `formatMonth`, `entry.roles`, and `techStack` markup structurally intact. Wrap each org `<li>` in `<Reveal>` (single lighter reveal for Just Relate / Vidama / Freelance). For the ITSC entry specifically, apply the multi-beat sub-sequence: each role transition in `entry.roles` (SysAdmin → Software Engineering → Product Owner) becomes its own emphasized reveal beat using the larger `--motion-distance-lg` offset and `--motion-duration-chapter` timing with a staggered timeline (`--motion-stagger-list`), so the role evolution reads as distinct beats rather than one block. Mount `<CareerSpine entries={...} />` inside the career section (reading its chapter/year data from the same `career` array). Do NOT introduce pinning or scroll-hijacking — reveals are `once: true` on native scroll (D-05). Keep the section H2 as the existing mono-eyebrow label (must NOT be upgraded to Display size, UI-SPEC).

    Add career behaviors to evals/immersive.spec.ts: career section + each org entry visible; ITSC roles all present as real text; under reduced-motion every career `<li>` and role has computed opacity 1; the spine tick list is present in DOM on desktop viewport and the marks carry `aria-hidden`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && pnpm exec playwright test evals/immersive.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CareerSpine' src/app/[locale]/page.tsx` is at least 1; `grep -c 'aria-hidden' src/components/motion/career-spine.tsx` is at least 1
    - `grep -c 'hidden lg:block' src/components/motion/career-spine.tsx` is at least 1 (spine hidden below lg per D-07/D-19)
    - Playwright: on `/de` and `/en`, `#career` and each org `<li>` are visible; the three ITSC role titles render as real DOM text; under `emulateMedia({ reducedMotion: 'reduce' })` career items have computed opacity 1
    - `pnpm exec playwright test evals/a11y.spec.ts` still green (career markup semantics preserved: `<section id="career">`, `<ol>`, dates intact)
    - No pinning: `grep -c 'pin:' src/components/motion/career-spine.tsx` is 0 and no ScrollTrigger uses `pin` (D-05 no scrolljacking)
    - `pnpm build` succeeds; CI LHCI CWV/script-size gate remains the authoritative budget check
  </acceptance_criteria>
  <done>The career plays as scroll-linked chapters with the ITSC arc as an emphasized multi-beat sub-sequence, a coordinate progress spine orients on desktop, and all career content is fully present under reduced-motion/touch/below-lg.</done>
</task>

<task type="auto">
  <name>Task 2: Featured bento project hierarchy (ELIA + Vidama)</name>
  <files>src/components/motion/project-bento.tsx, src/app/[locale]/page.tsx, evals/immersive.spec.ts</files>
  <read_first>
    - src/app/[locale]/page.tsx — the projects section (lines 180-228): the `projects.map`, the `project.depth === "flagship" | "deep"` → `getCaseStudy` case-study `<Link>` (which routes via `@/i18n/navigation`), `project.tags` chips, `project.url` external link. The data source (`getProjects`, `getCaseStudy`) must NOT change — bento is presentation only. Case-study links stay routed through `@/i18n/navigation`
    - 03-UI-SPEC.md §"Bento project grid (D-14)" — the full breakpoint table: mobile single column (ELIA/Vidama full-width, heavier `2px var(--border)` top border + larger Heading, NOT extra accent); tablet 640-1024 (ELIA row, Vidama row, compact 2-col); desktop ≥1024 12-col (ELIA spans 8/12 + 4/12 supporting panel, Vidama mirrors offset, compact `4/12`-per-item); stagger reveal via `--motion-stagger-list`
    - 03-CONTEXT.md D-14 (ELIA flagship + Vidama deep = large featured pair, owner-confirmed "ELIA/ITSC and Vidama featured, rest compact"; weighting matches Phase 1 D-01) and 03-RESEARCH.md §"No Analog Found" (bento is a genuinely new composition — build from UI-SPEC breakpoints, not an in-repo grid)
    - 03-PATTERNS.md §"Bento asymmetric grid" + §"Token-driven styling" (no new hex, no dark: utilities, accent scarcity)
  </read_first>
  <action>
    Create src/components/motion/project-bento.tsx ("use client" only if it needs the Reveal wrapper directly; prefer a Server Component that composes `<Reveal>` around cells to keep client JS minimal). Accept the `projects` array and a `caseStudyHrefFor`/rendered case-study link slot (or accept the already-resolved case-study link elements from the server) plus the i18n label strings as props — do NOT re-fetch content. Render an asymmetric CSS grid per the UI-SPEC breakpoint table: identify the two featured projects (ELIA flagship + Vidama deep, by `project.depth`/slug) and give them the large cells (desktop `col-span-8` + a `col-span-4` supporting tag/metric panel, alternating offset for visual rhythm per D-04), and render the remaining projects as compact `col-span-4` cells beneath. Featured signal = `2px var(--border)` top border + slightly larger Heading size — NOT extra accent (accent budget preserved). Wrap cells in `<Reveal stagger=...>` using `--motion-stagger-list`. Preserve every project's tags, summary, case-study `<Link>` (via `@/i18n/navigation`), and external `visit` link, and the accent-on-case-study-link hover pattern.

    In page.tsx projects section: replace the flat `<ul>/<li>` list with `<ProjectBento ... />`, passing the `projects` data and the resolved case-study links, keeping the `<section id="projects" aria-labelledby="projects-heading">` wrapper and its mono-eyebrow H2 unchanged. The existing home.spec.ts assertions (`#projects > ul > li` first item visible, `#projects ul li a` first link navigates to a case study) must continue to pass — either keep a `<ul>/<li>` semantic list structure inside the bento grid (grid applied via CSS on the `<ul>`/`<li>`), or update those two home.spec.ts assertions to the new structure; prefer preserving `<ul>/<li>` so no eval regresses.

    Add bento behaviors to evals/immersive.spec.ts: on `/de` and `/en` the projects region renders at least 3 project items; the two featured projects (ELIA, Vidama) are present and their case-study `<Link>`s resolve to `/case-studies/{slug}`; under reduced-motion all project cells have computed opacity 1.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && pnpm exec playwright test evals/immersive.spec.ts evals/home.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ProjectBento' src/app/[locale]/page.tsx` is at least 1
    - Bento routes internal links via next-intl: `grep -c 'next/link' src/components/motion/project-bento.tsx` is 0 (internal nav uses `@/i18n/navigation`)
    - No new palette colors: `grep -Ec '#[0-9a-fA-F]{3,6}' src/components/motion/project-bento.tsx` is 0 and `grep -c 'dark:' src/components/motion/project-bento.tsx` is 0 (D-02 token-only)
    - Playwright: featured ELIA + Vidama present; their case-study links match `/{locale}/case-studies/[^/]+`; `evals/home.spec.ts` (`#projects` list + case-study navigation) still green
    - under `emulateMedia({ reducedMotion: 'reduce' })` all project cells have computed opacity 1
    - `pnpm build` succeeds; CI LHCI gate remains authoritative
  </acceptance_criteria>
  <done>Projects render as an asymmetric bento with ELIA + Vidama as the large featured pair and the rest compact, staggering in on scroll, fully present and navigable under reduced-motion/touch, with no accent-budget or palette regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → client bundle | Additional client motion components (spine fill, bento reveals) ship as self-hosted JS; no runtime third-party call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering (XSS) | CareerSpine / ProjectBento | low | mitigate | Both render already-trusted, Zod-validated content-model data via React JSX (auto-escaped); no `dangerouslySetInnerHTML`, no user input |
| T-03-04 | Denial of Service (client) | ScrollTrigger spine fill | low | accept | Transform-only (compositor) fill, no per-frame layout thrash; reveals are `once:true`; touch uses native scroll (no Lenis) — INP/battery protected per D-19; CI CWV gate (TBT ≤ 200ms) enforces |
| T-03-05 | Information disclosure | ELIA case-study copy | low | accept | Presentation only; ELIA confidentiality (Phase 1 D-03: abstract-only) already enforced in the content model this plan reads — no new copy authored here |
</threat_model>

<verification>
- `pnpm lint && pnpm build` — clean
- `pnpm exec playwright test evals/immersive.spec.ts evals/home.spec.ts evals/a11y.spec.ts` — green (career/bento behaviors + no regression on existing home/a11y)
- Reduced-motion parity: career + project content all at final opacity under `emulateMedia({ reducedMotion: 'reduce' })`
- No pinning / no scrolljacking introduced (grep + manual: reveals are `once:true` on native scroll)
- CI LHCI: LCP ≤ 2500ms, TBT ≤ 200ms, CLS ≤ 0.1, script ≤ 184,643 bytes on push
</verification>

<success_criteria>
- Career reads as scroll-linked chapters (WOW-02) with the ITSC SysAdmin→SWE→PO arc as an emphasized multi-beat sub-sequence (D-06).
- A coordinate progress spine orients the reader on lg+ and is cleanly absent below lg without information loss (D-07, D-19).
- Projects render as an asymmetric bento with ELIA + Vidama featured and the rest compact (D-14).
- All career/project content is fully present under reduced-motion, on touch, and below lg — nothing gated behind motion (WOW-04, MODE-02, TECH-02); no pinning/scrolljacking (D-05).
</success_criteria>

<output>
Create `.planning/phases/03-design-direction-immersive-experience/03-02-SUMMARY.md` when done.
</output>
