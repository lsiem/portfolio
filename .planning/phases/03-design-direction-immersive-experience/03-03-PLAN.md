---
phase: 03-design-direction-immersive-experience
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/motion/magnetic.tsx
  - src/components/motion/transition-link.tsx
  - src/app/globals.css
  - src/app/[locale]/page.tsx
  - evals/immersive.spec.ts
autonomous: true
requirements: [WOW-03, TECH-02]

must_haves:
  truths:
    - "Interactive elements feel designed: nav/links, tech chips, and the CV button have intentional hover/focus/active states (WOW-03, D-11.3)"
    - "The CV button and contact links pull subtly toward the cursor on pointer:fine, and the magnetic wrapper is absent (not degraded) on touch/keyboard (D-11.1, D-19)"
    - "Navigating to a case study or /about plays a GSAP-orchestrated crossfade exit before the route commits, then the incoming page runs its own mount reveal (D-11.4)"
    - "The global :focus-visible accent ring is preserved/extended, never regressed, even where magnetic pull applies (TECH-03)"
    - "There is still exactly one animation engine — no second transition system (View Transitions API / a second lib) is introduced (D-08, WOW-03)"
  artifacts:
    - path: "src/components/motion/magnetic.tsx"
      provides: "pointer-only magnetic pull wrapper via useGSAP contextSafe"
    - path: "src/components/motion/transition-link.tsx"
      provides: "GSAP crossfade wrapper around @/i18n/navigation Link/useRouter"
    - path: "src/app/globals.css"
      provides: "designed hover/focus/active state rules (tech chip, links, CV button) gated under prefers-reduced-motion where they animate"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/motion/magnetic.tsx"
      via: "CV button + contact links wrapped in Magnetic (pointer:fine gated)"
      pattern: "Magnetic"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/motion/transition-link.tsx"
      via: "case-study + about links rendered via TransitionLink"
      pattern: "TransitionLink"
    - from: "src/components/motion/transition-link.tsx"
      to: "src/i18n/navigation"
      via: "wraps next-intl Link/useRouter, never raw next/navigation"
      pattern: "@/i18n/navigation"
---

## Phase Goal

**As a** visitor to lsiem.de, **I want to** move through the portfolio as a distinctive, engineered scroll-storytelling journey with felt craft, **so that** I remember Lasse as an engineer who builds and operates systems with taste — while never losing the one-click fast path to the facts.

*(This plan delivers the "everything feels designed" layer of that journey: micro-interactions and seamless section/route transitions — the felt-craft half of WOW-03.)*

<objective>
Layer the craft micro-interactions and seamless transitions on top of the storytelling built in plans 01-02, on the single GSAP engine. Add the `Magnetic` wrapper (D-11.1, pointer-only via `contextSafe`, absent on touch), the designed hover/focus/active states across nav links, org/project title links, tech/skill chips, and the CV button (D-11.3), and the `TransitionLink` GSAP crossfade for sub-route navigation to case studies and `/about` (D-11.4). Preserve the global `:focus-visible` accent ring (TECH-03) and the accent scarcity budget (D-02). No second animation engine — the crossfade is GSAP, not the View Transitions API (D-08).

Purpose: Complete the WOW-03 micro-interaction + seamless-transition surface while keeping mobile deliberate (magnetic absent on touch, TECH-02) and reduced-motion quiet (CSS motion gated).
Output: `Magnetic`, `TransitionLink`, the designed interaction-state CSS, and their eval contracts.
</objective>

## Artifacts this phase produces (this plan)

- **Components:** `Magnetic` (`src/components/motion/magnetic.tsx`), `TransitionLink` (`src/components/motion/transition-link.tsx`)
- **CSS (in `globals.css`):** designed hover/focus/active rules for tech chips (`border-foreground` + `color-mix` tint), org/project link accent hover, CV button `scale(0.98)` active — pure-CSS motion wrapped in `@media (prefers-reduced-motion: no-preference)` following the existing precedent
- **No new dependencies, no new motion tokens** — reuses `--motion-*` from plan 01

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-direction-immersive-experience/03-CONTEXT.md
@.planning/phases/03-design-direction-immersive-experience/03-RESEARCH.md
@.planning/phases/03-design-direction-immersive-experience/03-UI-SPEC.md
@.planning/phases/03-design-direction-immersive-experience/03-PATTERNS.md
@src/app/[locale]/page.tsx
@src/app/globals.css
@src/i18n/navigation.ts
@.planning/phases/03-design-direction-immersive-experience/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Magnetic buttons + designed hover/focus/active states</name>
  <files>src/components/motion/magnetic.tsx, src/app/globals.css, src/app/[locale]/page.tsx, evals/immersive.spec.ts</files>
  <read_first>
    - 03-RESEARCH.md Pattern 3 (Magnetic, `contextSafe` for event-handler tweens; gate the component's MOUNTING behind pointer:fine so no dead mousemove handlers ship on touch — "absent, not degraded")
    - 03-UI-SPEC.md §"Designed hover/focus/active states (D-11.3)" table (per-element rest/hover/focus/active) + §Color exhaustive accent list (magnetic max-pull affordance is item 5; do not spend accent elsewhere)
    - src/app/globals.css — the existing `:focus-visible` ring (lines 88-91, must NOT regress) and the `@media (prefers-reduced-motion: no-preference)` precedent (lines 64-68) to wrap any CSS motion
    - src/app/[locale]/page.tsx — the CV download button (lines 294-301), contact links (302-317), tech chips (167-173, 197-203), org/project title links — the targets for Magnetic + hover states
    - 03-PATTERNS.md §magnetic + §"Accent color scarcity" + §"Token-driven styling"
  </read_first>
  <action>
    Create src/components/motion/magnetic.tsx ("use client"): a `Magnetic` wrapper using `const { contextSafe } = useGSAP({ scope: el })`, with `onMouseMove`/`onMouseLeave` handlers wrapped in `contextSafe` that `gsap.to` translate the element toward the cursor within a ~60px radius, clamped to a max 12px displacement, ease from `--motion-ease-magnetic` (via getMotionToken), snapping back on leave. Attach NO touch handlers. The parent mounts `Magnetic` only when `pointer: fine` — read that gate via a small useSyncExternalStore-based check (reuse the gate approach from MotionProvider; do not use useState+useEffect) OR render Magnetic unconditionally but make it a no-op wrapper whose handlers never fire on touch — prefer non-mounting on touch so no handlers ship (D-19 absent-not-degraded). Keyboard focus is unaffected: the `:focus-visible` ring still applies.

    In globals.css: add the designed hover/focus/active rules per the UI-SPEC D-11.3 table — tech/skill chips gain `border-foreground` + `background: color-mix(in oklab, var(--foreground) 5%, transparent)` on hover; org/project title links go `text-accent` on hover (`--motion-duration-fast`); the CV button gains `scale(0.98)` on active. Any rule that animates (transitions) must be wrapped in `@media (prefers-reduced-motion: no-preference)` following the existing precedent so reduced-motion users get instant state changes. Do NOT touch or weaken the `:focus-visible` block. Use only existing token-backed colors (no new hex, no `dark:`).

    In page.tsx: wrap the CV download button and the contact email/GitHub/LinkedIn links (and, per UI-SPEC, nav) in `<Magnetic>` (mounted pointer:fine-only). Apply the tech-chip / link hover classes where they are not already covered by the globals.css rules. Keep all `href`/`download`/`aria-label`/`rel="noopener noreferrer"` attributes intact.

    Extend evals/immersive.spec.ts: with default (fine pointer) the CV button responds to a simulated mouse move (transform changes) and snaps back on mouse leave; the CV button still shows a visible `:focus-visible` outline when focused (ring not regressed); under `emulateMedia({ reducedMotion: 'reduce' })` interaction states do not animate (transform stays at rest). Assert `aria-label` on the CV button is still present.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && pnpm exec playwright test evals/immersive.spec.ts evals/a11y.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'contextSafe' src/components/motion/magnetic.tsx` is at least 1; `grep -Ec 'onTouch|ontouchstart' src/components/motion/magnetic.tsx` is 0 (pointer-only, no touch handlers)
    - `grep -c 'Magnetic' src/app/[locale]/page.tsx` is at least 1
    - `:focus-visible` ring preserved: `grep -c 'focus-visible' src/app/globals.css` is at least 1 and `evals/a11y.spec.ts` (visible-ring assertions) stays green
    - `grep -c 'prefers-reduced-motion' src/app/globals.css` is at least 2 (the existing scroll-behavior gate plus at least one new interaction-state gate)
    - No palette regression: `grep -c 'dark:' src/app/[locale]/page.tsx` is 0
    - Playwright: CV button transform changes on mouse move and returns on leave (fine pointer); no transform change under reduced-motion
    - `pnpm build` succeeds; CI LHCI gate authoritative
  </acceptance_criteria>
  <done>Nav/links, chips, and the CV button have designed hover/focus/active states; the CV button and contact links pull magnetically on pointer:fine and are inert on touch; the focus ring and accent budget are intact; reduced-motion strips interaction animation.</done>
</task>

<task type="auto">
  <name>Task 2: Seamless GSAP crossfade sub-route transitions</name>
  <files>src/components/motion/transition-link.tsx, src/app/[locale]/page.tsx, evals/immersive.spec.ts</files>
  <read_first>
    - 03-RESEARCH.md Pattern 5 (TransitionLink: intercept nav, play exit tween, `router.push` in `onComplete`; incoming page runs its own standard mount-reveal — not shared-element morphing) — the sketch uses raw `next/navigation`; this codebase MUST use `@/i18n/navigation` instead
    - src/i18n/navigation.ts — exports `{ Link, usePathname, useRouter, getPathname }` from `createNavigation(routing)`; `useRouter().push(href)` is the locale-aware router to call
    - 03-UI-SPEC.md §"Seamless transitions technique (D-11.4)" — GSAP crossfade (NOT View Transitions API), fade/slide the outgoing `<main>` out with `--motion-duration-base` + `--motion-ease-in-out` before navigation commits; reduced-motion = instant swap, no crossfade
    - src/app/[locale]/page.tsx — the case-study `<Link href={`/case-studies/${project.slug}`}>` (via ProjectBento after plan 02) and the about `<Link href="/about">` (lines 268-273) that become TransitionLink targets
    - 03-PATTERNS.md §transition-link (wrap `@/i18n/navigation`, never raw next/link; verify the useRouter signature)
  </read_first>
  <action>
    Create src/components/motion/transition-link.tsx ("use client"): a `TransitionLink` with the same public surface as the project's `Link` (at least `href` + `children` + `className`). Use `useRouter` from `@/i18n/navigation` and `const { contextSafe } = useGSAP()`. On activation, run a `contextSafe` GSAP tween that fades/slides the `<main>` element out (`opacity` → 0, small `y` per `--motion-distance-md` inverted, duration `--motion-duration-base`, ease `--motion-ease-in-out`) and calls `router.push(href)` in `onComplete`. Gate on reduced-motion via `gsap.matchMedia` — the `reduce` branch calls `router.push(href)` immediately (instant swap, no crossfade). Keep it keyboard-activatable and preserve accessible semantics (render an anchor/`Link`-equivalent, not a bare div — so it is focusable and the ring applies); the incoming page needs no special entry beyond its own `Reveal` mounts.

    In page.tsx: render the case-study links and the about "read more" link via `<TransitionLink>` (routing through `@/i18n/navigation`). Keep the visible label, the accent hover class, and the locale-prefixed href behavior. The existing home.spec.ts test "case study link navigates to detail page" must still pass (the URL still lands on `/{locale}/case-studies/{slug}`).

    Extend evals/immersive.spec.ts: clicking a case-study TransitionLink still navigates to `/{locale}/case-studies/[^/]+` (with default motion); under `emulateMedia({ reducedMotion: 'reduce' })` the same click navigates without waiting on a crossfade (navigation still succeeds). Assert no second animation engine dependency is present: read package.json and confirm the only animation runtime dep family is gsap/@gsap/react/lenis.
  </action>
  <verify>
    <automated>pnpm lint && pnpm build && pnpm exec playwright test evals/immersive.spec.ts evals/home.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/i18n/navigation' src/components/motion/transition-link.tsx` is at least 1 and `grep -c 'next/navigation' src/components/motion/transition-link.tsx` is 0 (locale-aware router only)
    - `grep -c 'TransitionLink' src/app/[locale]/page.tsx` is at least 1
    - Single-engine discipline: `node -e "const d=require('./package.json').dependencies; process.exit((d['framer-motion']||d['motion'])?1:0)"` exits 0 (no second animation runtime dep)
    - Playwright: case-study navigation succeeds under both default and reduced-motion; `evals/home.spec.ts` case-study navigation test still green
    - `pnpm build` succeeds; CI LHCI gate authoritative
  </acceptance_criteria>
  <done>Case-study and /about navigation plays a GSAP crossfade exit before committing on capable devices and swaps instantly under reduced-motion, all through the locale-aware router, with no second animation engine introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → client-side navigation | TransitionLink intercepts clicks and drives router.push; must not open the target to unsafe hrefs |
| build → client bundle | Magnetic/TransitionLink ship as self-hosted JS; no runtime third-party call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering (open redirect / unsafe scheme) | TransitionLink `router.push(href)` | low | mitigate | `href` values are first-party content-model routes only (case-study slugs, `/about`) passed as literals — no user-controlled URL; internal-only via `@/i18n/navigation`; external links keep native `<a target=_blank rel="noopener noreferrer">` and are NOT routed through TransitionLink |
| T-03-07 | Denial of Service (client) | Magnetic mousemove tweens | low | accept | `contextSafe` GSAP tweens are transform-only and throttled by GSAP's ticker; mounted pointer:fine-only so no handlers on touch (INP/battery protected, D-19); CI TBT ≤ 200ms gate enforces |
| T-03-01 | Tampering (config) | second-engine avoidance | low | mitigate | Crossfade uses GSAP (D-08); no View Transitions API / second animation lib added — asserted in Task 2 acceptance criteria |
</threat_model>

<verification>
- `pnpm lint && pnpm build` — clean
- `pnpm exec playwright test evals/immersive.spec.ts evals/home.spec.ts evals/a11y.spec.ts` — green (micro-interactions + transitions + no regression on navigation/focus)
- Focus ring intact (a11y eval); accent scarcity + token-only styling preserved (grep)
- Single animation engine confirmed (package.json check)
- Reduced-motion: interaction states do not animate; route swap is instant
- CI LHCI: LCP ≤ 2500ms, TBT ≤ 200ms, CLS ≤ 0.1, script ≤ 184,643 bytes on push
</verification>

<success_criteria>
- Designed hover/focus/active states across nav, links, chips, and the CV button (WOW-03, D-11.3).
- Magnetic pull on the CV button + contact links on pointer:fine, absent on touch/keyboard (D-11.1, D-19, TECH-02).
- Seamless GSAP crossfade to case studies and /about; instant swap under reduced-motion (D-11.4).
- `:focus-visible` ring preserved (TECH-03); accent budget + palette unchanged (D-02); exactly one animation engine (D-08).
</success_criteria>

<output>
Create `.planning/phases/03-design-direction-immersive-experience/03-03-SUMMARY.md` when done.
</output>
